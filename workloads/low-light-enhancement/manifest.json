{
  "manifestVersion": 1,
  "id": "low-light-enhancement",
  "version": "0.2.0",
  "capabilities": [
    "image-enhancement",
    "enhanced-image-generation"
  ],
  "requirements": {
    "runtimeApi": 1,
    "accelerator": "gpu",
    "acceleratorPreference": [
      "gpu"
    ],
    "minimumDevices": 1,
    "model": null
  },
  "ui": {
    "title": "Low-light enhancement",
    "group": "Local workflows",
    "description": "Enhance dark images with bounded Vulkan inference",
    "icon": "image-x-generic-symbolic",
    "order": 55
  },
  "defaults": {
    "enabled": false,
    "weight": 2
  },
  "pipeline": {
    "hostResponsibilities": [
      "Read images only from the configured input folder without modifying originals",
      "Decode and validate bounded image inputs",
      "Apply orientation and convert to sRGB before exact bicubic preprocessing",
      "Construct the RGB float32 NCHW tensor scaled to the unit range",
      "Validate finite enhanced RGB output with shape [1,3,256,256] in the unit range",
      "Resize validated enhanced RGB back to original geometry with bicubic interpolation",
      "Color-manage, encode, display, and route results",
      "Publish new files without overwrite only in a disjoint configured output folder"
    ]
  },
  "acceptance": [
    {
      "metric": "vulkan-cpu-fallback-rate",
      "comparator": "equal",
      "target": 0,
      "unit": "percent",
      "description": "The named hardware Vulkan run performs inference without CPU fallback"
    },
    {
      "metric": "portable-native-ssim",
      "comparator": "at-least",
      "target": 0.999,
      "unit": "ratio",
      "description": "The ncnn output matches the accepted portable Retinexformer output on the frozen holdout"
    },
    {
      "metric": "paired-holdout-ssim",
      "comparator": "at-least",
      "target": 0.8,
      "unit": "ratio",
      "description": "The complete enhanced output meets the paired normal-light holdout threshold"
    },
    {
      "metric": "output-range-violation-rate",
      "comparator": "equal",
      "target": 0,
      "unit": "ratio",
      "description": "No native output component is non-finite or outside the unit range"
    },
    {
      "metric": "color-error-ciede2000",
      "comparator": "at-most",
      "target": 3,
      "unit": "score",
      "description": "Median color error stays below the visible-quality threshold on the paired holdout"
    },
    {
      "metric": "p95-device-latency",
      "comparator": "at-most",
      "target": 50,
      "unit": "milliseconds",
      "description": "Warm 256x256 ncnn invocation on the named RX 6600 XT"
    },
    {
      "metric": "end-to-end-speedup",
      "comparator": "at-least",
      "target": 1.2,
      "unit": "ratio",
      "description": "Complete GPU pipeline beats the documented CPU enhancement baseline"
    }
  ]
}

# TODO

## Work groups

Items remain in the canonical lifecycle tables below; these ranges preserve the
audit's dependency order without weakening the required status schema.

- **Low-light delivery:** OMNI-0048, OMNI-0104–OMNI-0106
- **Profile model, pipeline, and acceptance:** OMNI-0085–OMNI-0098, OMNI-0101, OMNI-0108–OMNI-0115
- **System acceptance:** OMNI-0122–OMNI-0125
- **Privileged telemetry:** OMNI-0184, OMNI-0247
- **TPU lane:** OMNI-0198, OMNI-0235, OMNI-0289
- **Production pipeline consolidation:** OMNI-0258, OMNI-0259
- **Mutation gates:** OMNI-0296, OMNI-0343, OMNI-0297
- **Project split support:** OMNI-0344
- **New workloads:** OMNI-0351
- **Client workload defects (found end to end):** OMNI-0353, OMNI-0354
- **Workload tuning:** OMNI-0355
- **SonarCloud remediation:** OMNI-0334, OMNI-0337, OMNI-0338
- **2026-08-18 architecture audit:** OMNI-0387–OMNI-0477

## Findings

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0478 | in_progress | high | l | **An idle runtime must cost nothing.** The maintainer's requirement, stated 2026-08-18: OmniTensor may not use any resource, nor keep a heartbeat, unless it is actually in use. Discovery is done: `_rediscover` no longer polls, it sleeps on a `NETLINK_KOBJECT_UEVENT` socket behind the optional `DeviceChangeSource` port and reads sysfs only when the kernel reports an accel/apex/drm/pci/usb event, so an idle machine spends no wakeups on it; a host with no event source polls `DISCOVERY_INTERVAL_S` as before and logs that fallback with the wakeups it costs. **Still outstanding: (a)** the publisher, which ticks every `IDLE_PUBLISH_INTERVAL_S` (10 s) and rewrites the snapshot every `SNAPSHOT_HEARTBEAT_INTERVAL_S` (10 s) purely to prove the runtime is alive — that half cannot be implemented until OMNI-0479 decides whether an idle runtime may stop publishing at all; **(b)** done 2026-08-19: workers start on demand and stop after WORKER_IDLE_TIMEOUT_S. |
| OMNI-0499 | blocked | low | s | **Is per-request unload the intended cross-worker GPU-lease policy?** `providers/vulkan-runtime/.../runtime.py:160` — `generate()`'s `finally: self._release()` unloads the GGUF and drops the lease after every request, so the next request re-reads and re-offloads a multi-GB model and the cached `_load_report` fast path is almost never hit. Deliberate, so another worker can take the GPU between requests, or an accident? Maintainer's call. |
| OMNI-0500 | blocked | low | xs | **Was the eight-node cap in `discovery.py:102` a product limit or a defensive guess?** `_numbered_nodes(...)[:limit]` silently drops render/accel/apex nodes past eight, so a ninth GPU is invisible with no reason recorded. If it is not deliberate, stop slicing and let sysfs be the bound; if it is, record a `Device(available=False, reason=…)` rather than omitting. |
| OMNI-0501 | blocked | medium | m | **The shipped qualification receipts are stale, and re-measuring needs the GPU and the real models.** Root cause of all eight red provider tests, diagnosed 2026-08-19: the task definitions changed after the receipts were measured, so every `test_shipped_qualification.py` pair reports its own digest against the recorded one (e.g. ask-selected-files now hashes `74dcdf26…`, the receipt records `6d1711de…`), and `test_runtime.py`'s receipt tests see the matching `covers` refusal ("ask-selected-files has changed since qwen3-8b-q4-k-m was measured"). Not a code defect and not fixable from here: the pairs have to be re-qualified by running the shipped models on the GPU, which is a maintainer action on the host. Until then the eight failures are expected. |
| OMNI-0351 | open | medium | l | Integrate an OCR workload on the Vulkan lane by porting PaddleOCR models to ncnn, following the codexa-project evaluation (2026-08-15, checked against the live repos): Paddle has no Vulkan backend — `paddle/phi/backends` holds `cpu, custom, dynload, gpu, onednn, xpu` as of PaddleOCR v3.7.0 / Paddle v3.3.0 / Paddle-Lite v2.14-rc — so running PaddleOCR under this service's no-CPU-fallback rule needs a different runtime, and ncnn is the mature answer: a genuine Vulkan backend (`src/gpu.cpp`, `pipeline.cpp`, `command.cpp`), on PyPI (1.0.20260526), with existing PaddleOCR model ports, and already this service's GPU engine (`executors/vulkan.py` `VulkanGpuExecutor`), so the executor, confinement, and artifact machinery are reused rather than added. The rejected routes, recorded so they are not re-derived: Paddle's out-of-tree `backends/custom` plugin seam exists (`get_all_custom_device_type()` returns `[]` here) but means writing the kernels ourselves. Work shape: pick the detection+recognition model pair, obtain or build the ncnn ports with pinned sha256 artifacts, add a workload manifest with the tensor contracts, and gate acceptance on the GPU lane like every other profile. |
| OMNI-0344 | open | medium | s | The applet still mirrors contracts the runtime publishes. The premise of this row has moved: the Python client exists, depends on `omnitensor>=0.1,<1`, and vendors no schemas — that half is settled. What remains is the Cinnamon applet's hand-written JavaScript mirrors, and where the D-Bus method constants and the snapshot-path default live, which are still duplicated as prose in both repositories' contract tests. `d0a10c1` set the precedent for the remaining options: a second distribution built from this repository is now a known move, so a slim contracts package both consumers depend on is cheaper than it was when this row was written. |
| OMNI-0353 | open | high | m | `file-organizer` fails in bursts, on inputs that succeed minutes later. Measured: one burst of four consecutive single-source failures (`plan-invalid`, then `provider-output-invalid` on a retry) where the same file submitted alongside a second one succeeded; a later burst of two-source failures (`evidence-invalid`) inside a test run while the same pair succeeded standalone; and then eleven consecutive successes, single and paired, over the following ten minutes with no change to the inputs or the service. So the defect is not the single-source case it first looked like — it is a worker that intermittently produces a plan its own contract refuses, and recovers on its own. Worth catching in that state: `plan-invalid` and `evidence-invalid` come from different validators, so the burst is likely upstream of both, in whatever the worker holds between jobs. `tests/test_client_workload_e2e.py::TestFileOrganizer` covers the single and paired cases and fails during a burst. Seen again 2026-08-17 after the worker-bootstrap fixes: both e2e cases failed `provider-output-invalid` back to back on a worker in its first minutes, on the same fixtures that passed elsewhere — consistent with the burst living in per-worker state rather than in the inputs. |
| OMNI-0354 | open | medium | l | Five sources that plainly contain an event still come back refused, and **lengthening the prompt makes it dramatically worse**. Baseline on `qwen3-5-9b-iq4-xs`, sixteen cases, reproduced twice at exactly 8/16 and 61% of rules, 19.3s per case. The five refused are `event-written-month`, `event-two-in-one-document`, `event-time-without-date`, `event-online-meeting` and `event-cancelled`, and the model states its reasoning in `detail`: *"only a description of a recurring"* and *"The source text explicitly states that the event is cancelled"*. So it is applying its own notion of what counts as an event — a cancelled one is not one, a series without a first date is not one — rather than the contract's. **Measured attempt, reverted:** adding six clauses saying exactly that (a cancelled event is still an event, a series with no first date is an event whose date is unknown, several events yield several) took the instruction from 1,863 to 2,264 characters and the score from 8/16 to **2/16, rules 61% to 4%**, at 7.3s per case — it refused nearly everything, faster. More instruction is not the lever, and this is the second wrong approach eliminated by measurement after the grammar conditional. What has not been tried: shortening the instruction rather than extending it, since the two data points both point that way; a worked example in the prompt instead of more rules; and `event-two-in-one-document` specifically, where `at_least: 2` may be failing for a different reason than the refusal. Worse on 2026-08-17: the plainest case there is — two explicit dated, timed events in a three-line note — came back `no-event-found` / "Sources contain no event" three runs out of three on `qwen3-5-9b-iq4-xs`, with the task digest verified identical to the measured receipt, so the live model refuses even the baseline's passing shape now (`tests/test_client_workload_e2e.py::TestEventExtraction` reproduces it). |
| OMNI-0355 | open | medium | l | Let a person tune a workload without breaking its qualification. The constraint that shapes this: the task — prompt, schema, token budget — is digest-bound to the qualification receipt, so a user-editable prompt cannot *be* the task; a worker whose task does not match its receipt refuses to start, which is exactly the failure OMNI-0352 was. So tuning is an *addition* the qualified task carries, never a replacement: `PluginContext.configuration` already exists, `plugins/settings.py` is already revisioned schema-validated configuration persistence, and each manifest already declares a `configuration` schema (empty today). Missing: a control command to set a workload's configuration, service wiring to pass it to the worker and restart that worker when it changes, providers reading it, and manifests declaring the tunables. The client end is nearly free — `xpuwlm/ui/workload_panels.py` has carried a translator for a non-empty `configurationSchema` since it was written, unused because no plugin publishes one. Start with two tunables that need no new qualification: extra answer guidance appended as private user instruction (never as system rules — the grounding and citation rules stay authoritative), and answer length. |
| OMNI-0360 | open | high | l | A `document-translation` workload, because translation is a transformation of the documents and `ask-selected-files` returns a cited answer *about* them — different contracts, so a shared one would weaken both. Done 2026-08-17: the chunking half is in the domain layer and tested — `plugins/spans.py` splits on paragraph, then sentence, then whitespace boundaries with reassembly proved lossless, and `plugins/translation.py` sizes spans from the task's own output budget, translates each through an injected callable, reports progress as spans finished over spans total and takes cancellation between spans (`tests/test_translation.py`, 15 passing). Remaining is the workload itself, and nothing calls either module today: a `document-translation` plugin that extracts text per source and drives `translate_document`, a result schema and manifest with its input contract, a provider distribution and entry point, wiring the Hebrew route to the already-qualified `HebrewTranslationRuntime`, and a qualification receipt for the pair. |
| OMNI-0361 | open | medium | m | Qualify the pairs a person can actually choose. Done 2026-08-17: the four text workloads reissued against the uncapped tasks on `qwen3-8b-q4-k-m`, and `qwen3-5-9b-iq4-xs` added as the default with the weights installed digest-verified and declared in all four manifests. Remaining: `qwen3-4b-q4-k-m`, which beats every 9B on `file-organizer` (10/10 against 8/10) and is therefore worth offering for that workload, and `dictalm2-hebrew-q4-k-m` against the Hebrew path, which has never been measured through its own runtime — the benchmark drove the generic one and scored 0/10 in 1.2s, which is a harness defect rather than a verdict. Note the standing decision that a receipt records what is selectable rather than gating a person's choice: a model somebody selects is their responsibility. |
| OMNI-0374 | open | medium | m | `provider_id` is `qwen3-workloads-gpu` — a model's name inside a runtime identity, for a runtime that loads any GGUF. Renaming it is not free: `evaluation-corpora/selected-text-v1.json` carries `expectedProviderId` on all ten cases, and its sha256 (`2f574e01…`) is bound into `acceptance-evidence/selected-text-rx6600xt-evidence.json` as `corpusSha256`, so the corpus, the frozen evidence, `generation-models/qwen3-8b.json`, and the `document_acceptance.py:408` gate move together and the acceptance runs are re-executed. Decided 2026-08-17 to rename and redo the runs rather than keep a model name in a runtime identity. |
| OMNI-0375 | open | medium | l | Finish splitting the shared provider, which is still five concerns in one wheel — 1,720 lines over eight modules, split by file without being split by distribution: the llama.cpp/Vulkan adapter and its GBNF grammar (which belong), BGE embeddings on ncnn (a different engine), a DictaLM translation runtime (one named model), the qualification receipt reader (evidence, a host concern), grounding (domain), and factories for all four workloads (each wheel's own). `runtime.py` still branches on four workload ids to assemble prompts — `_grounding_hint`, `_selected_operation_hint`, `_event_grounding_hint`, `_citable_references` and the event reconsideration retry are workload policy living inside a GPU backend. Extract the embedder to its own distribution, dissolve `hebrew.py` into a translation task, move each factory into the wheel it belongs to, and give the adapter a prompt port so it stops knowing what an event is. |
| OMNI-0377 | open | low | s | `omnitensor.qwen_installation` and its console script `omnitensor-install-qwen-artifacts` name one vendor for a module that installs pinned artifacts, and `media_installation.py` takes `--accept-qwen-license` for what is really the generation model's licence. The module genuinely pins one model's digest and size, so this is naming rather than structure, but the console script is a published entry point and the installation docs name it throughout. |
| OMNI-0379 | open | medium | m | Four of the six provider distributions ship no test suite at all: `ask-selected-files`, `event-extraction`, `file-organizer` and `selected-text-tools`. Each is a five-line identity shim over `omnitensor-vulkan-runtime`, which is why they were left bare, but the shim is exactly where an entry point, a manifest id and a plugin id have to agree — and `tests/test_qwen_installation.py` checks that agreement from the root, which is a different repository boundary from the wheel that ships. `scripts/run-provider-suites.sh` now names them as having no suite on every run rather than passing over them in silence, since silence there reads exactly like success. |
| OMNI-0386 | open | low | s | The publisher tick still pays for two things it already knows. `_refresh_grants` reloads the grant ledger from disk on every tick (`GRANT_REFRESH_INTERVAL_S` is 0.25 s, shorter than any tick, so the gate never holds), and `_build_runtime_snapshot` revalidates the document against `runtime-snapshot.schema.json` — measured at 1.91 ms on the live 7.5 KB snapshot — even when the content is identical to the copy validated on the previous tick. With the idle tick now at 10 s this costs about 0.2% of one core on an idle desktop, down from 5%, so it is worth doing but not urgent. Gate the reload on the grants file's mtime, and skip revalidation when the built document matches the last validated one apart from `generatedAt`. |
| OMNI-0385 | open | high | s | A partial upgrade kills a worker and says only `truncated-frame`. The provider distributions import module-level names from the service package — `omnitensor_media_transcription/formats.py` does `from omnitensor.plugins.media_transcription import (...)` — so reinstalling `omnitensor` alone, without the providers built from the same tree, leaves an installed wheel importing a name that no longer exists. Observed on 2026-08-17: removing `MAX_VISUALS` and reinstalling only the root package left `media-transcription` dead at entry-point load with `ImportError: cannot import name 'MAX_VISUALS'`, surfacing to the applet as `worker handshake rejected: truncated-frame` — the same opaque symptom OMNI-0381 is about, from a different cause. Nothing detects the skew: `omnitensor-verify-install` (`omnitensor.acceptance`) checks schemas, sockets and artifacts but never that each installed provider can import what it declares. Import every installed provider entry point during verification and name the mismatch, and make the documented upgrade reinstall the providers alongside the service. |
| OMNI-0384 | open | high | m | The qualification receipt no longer describes the tasks that run. Removing the ceilings on what a person is told changed each workload's `outputSchema`, and the task digest covers it, so `ask-selected-files` and `selected-text-tools` both moved: `selected-text-tools` is now `7a87a474…` against the recorded `c6ab03dc…`. Eight tests in the `providers/vulkan-runtime` suite fail on the mismatch (`test_shipped_qualification.py::test_the_shipped_receipt_describes_the_task_that_will_run`, `test_runtime.py::test_receipt_binds_each_exact_task_and_model`). Nothing is gated by this — the receipt reports coverage rather than deciding what may run — so the workloads execute correctly; what is wrong is that the receipt now claims to have measured a task that no longer exists. The fix is a GPU acceptance run per pair and a re-recorded `qualification.json`; the tests' own docstrings forbid editing the expectation to match the code, and there is no re-qualification script in `scripts/`. |
| OMNI-0382 | open | medium | s | `docs/choosing-a-model.md` recommends `qwen3-4b-q4-k-m` for `file-organizer` — it beats every 9B there, 10/10 against 8/10 — but no manifest declares it, so `set-profile-model` refuses the very choice the guide recommends with "Model is not declared by this workload". Declaring it in `plugin-manifests/file-organizer.json` is the fix if the measurement stands; withdrawing the recommendation is the alternative. Found by the 2026-08-17 documentation audit, which corrected what it could verify and left this because it needs a product decision rather than a prose edit. |
| OMNI-0383 | open | medium | s | `scripts/collect-selected-text-acceptance.py` verifies `--qwen-model` against the live worker load receipt, and the documented invocation passes the 8B. That is internally consistent only while the documented install mounts the 8B alone: once OMNI-0380 lands and a worker runs the `qwen3-5-9b-iq4-xs` default, the documented collector command becomes refusable against a receipt naming the 9B. Take the model from the receipt, or require the operator to name the one the worker actually loaded. |
| OMNI-0380 | open | high | m | Nothing installs the model all four workloads default to. `providers/vulkan-runtime/src/omnitensor_vulkan_runtime/qualification.json` names `qwen3-5-9b-iq4-xs` as the `default` for `ask-selected-files`, `event-extraction`, `file-organizer` and `selected-text-tools`, and each manifest declares it as its first artifact — but `omnitensor.qwen_installation` still pins the 8B alone (`QWEN_REFERENCE`, `QWEN_SIZE_BYTES = 5_027_783_488`), `generation-models/` carries no catalog entry for the 9B, and `docs/qwen-workload-installation.md` documents only the 8B URL, digest and byte count. A host installed exactly as documented therefore has no 9B artifact mounted, and `_generation`'s `bootstrap.chosen_or(default_model(...))` refuses with `artifact-unavailable` unless the person explicitly chooses the 8B — the documented install produces four workloads that cannot start. Pin the 9B digest and size in an installation path, add its catalog entry, and document its download beside the 8B's. |
| OMNI-0391 | open | high | m | Alerts are cut and then silently evicted. `plugins/summaries.py:13-15,76-84` returns `redacted[:maximum]` — 160 chars for a title, 500 for a summary — with no signal anything was removed, though redaction rather than length is the privacy control here; `publish` (131-136,185-198) evicts an existing summary once `len(self._summaries) >= self._max_summaries`, and `max_summaries` is validated against `MAX_RESULT_SUMMARIES` = 100 so a caller cannot opt into more. `schemas/runtime-snapshot.schema.json:152,182` bakes in `maxItems: 100` and `maxLength: 500`. Alerts are the runtime's only channel for 'something went wrong', so the cap discards signal exactly during the incident producing it. Keep the redacted text whole and retain every alert. |
| OMNI-0392 | blocked | critical | m | Collectors truncate below the allowlist the user configured. `plugins/collection.py:29,153,238,251` does `selected = eligible[: self._max_items]` and reports the loss only as an integer, and the defaults sit *below* the allowlist sizes the same modules accept — `peripheral_collection.py:250-251,376` validates 64 device ids but defaults `max_devices=32`; `network_collection.py:21,109` validates 256 links but defaults `max_links=64`. A person who deliberately allowlists 40 peripherals is shown 32. `schemas/collected-output.schema.json` reinforces it at `/$defs/items maxItems: 1024`, `/$defs/truncated maximum: 1024` (a ceiling on the number reporting a ceiling) and `/properties/churn/{added,removed,changed} maxItems: 1024` — the last directly reinstating the truncation `collection.py:239-241`'s own comment exists to prevent. Emit every eligible item, drop the `truncated*` fields, and coordinate the schema change with the applet repository. **Blocked 2026-08-19, maintainer decision needed:** dropping the `truncated*` fields and raising `maxItems` is a breaking change to `collected-output.schema.json`, which the sibling applet repository consumes and this repository does not own. Which is wanted — (a) emit every eligible item and delete `truncated*` in one coordinated schema version, or (b) keep the fields for one release, defaulting the per-collector maxima up to the allowlist sizes each module already validates (64 peripherals, 256 links) so no configured item is silently dropped, and remove them next? Nothing here is implemented until that is answered. |
| OMNI-0394 | open | high | l | `plugins/media_transcription.py:33-37,370-380,389-417,483-505` refuses a 65-page PDF or 65-slide deck (`MAX_DOCUMENT_PAGES`/`MAX_PRESENTATION_SLIDES` = 64) and an 11-minute recording (`MAX_DURATION_MS` = 600_000; video 300_000), in the same file whose comment at 447-449 argues against exactly this for segments. `schemas/media-transcription-result.schema.json:30,50-51,60-62` mirrors them as `maximum` on `slideNumber`, `pageNumber`, `durationMs`, `startMs`, `endMs`, `timestampMs`. The legitimate selection bound already exists separately (`MAX_SOURCE_BYTES` = 128 MiB). Remove the ceilings and transcribe pages and time windows in accumulating passes. |
| OMNI-0395 | open | high | m | `plugins/document_qa.py:344-351` treats `ExtractionOutcome.TRUNCATED` exactly like `SUCCEEDED` and discards `dropped_pages`/`dropped_characters` (`extraction.py:36-39,304-321`), so a 6 MB or 2,500-page selection is answered from its first 5,000,000 characters and the person is told nothing. Derive the character bound from the file's own size or extract in accumulating passes; until then refuse rather than answer, naming what was dropped. |
| OMNI-0397 | open | high | s | Accelerators past a fixed count are silently dropped, twice. `snapshot.py:409,478` publishes `devices[:MAX_DEVICE_ENTRIES]` against `schemas/runtime-snapshot.schema.json:26` `maxItems: 16`, so a 17th card is invisible to the applet and unselectable; `input_roots_document` twelve lines away refuses to truncate for exactly this reason and says so in a comment. `discovery.py:19-21,86-98` is worse: `sorted(matched, ...)[:limit]` with `MAX_PCIE_DEVICES`/`MAX_ACCEL_DEVICES`/`MAX_RENDER_DEVICES` = 8 means a 9th render node is never detected at all, so no executor and no scheduler lane exist for it. Remove all four bounds and the schema `maxItems`. |
| OMNI-0398 | open | high | m | One refusal reason is cut at four uncoordinated ceilings on its way out: `job_ports.py:8,45` truncates at 240, `job_lifecycle.py:157,331,344` at 200 twice and then 500, `control.py:37,360` at 240, `profile_selection.py:87,102,111,178` at 240; `schemas/runtime-acknowledgement.schema.json:42`, `runtime-job-acknowledgement.schema.json:35` and `runtime-snapshot.schema.json:80,128,276` bake `maxLength: 240` in. `scheduler.py:91-93` deliberately joins all three backends' rejection reasons with '; ' so a person learns why each candidate failed — the string most likely to be cut mid-word, at the moment it is most needed. Delete the slices and lift the schema bounds, coordinating with the applet. |
| OMNI-0402 | blocked | medium | xs | `sdk/testing.py:331-336` reduces any exception to `f"local pipeline failed: {type(error).__name__}"` and `sdk/smoke.py:70-71,78-79` does the same for entry-point enumeration and plugin load, so a plugin author debugging their own preprocess step is told only 'local pipeline failed: ValueError'. **Blocked 2026-08-19, maintainer decision needed:** including `str(error)` contradicts a contract the suite states deliberately — `tests/test_local_plugin_runner.py::test_runner_maps_artifact_and_pipeline_failures_without_private_details` asserts `"private" not in failed.result.detail`, and `tests/test_external_plugin_ci.py` pins both smoke strings to the type name alone. Which rules: the plugin result detail is a published field that must carry no arbitrary exception text (keep as is, and diagnose through logs instead), or the SDK harness and smoke CLI are author-facing tools where the message wins (then those three tests change with it)? Nothing here is implemented until that is answered. |
| OMNI-0404 | open | high | s | Citations into page 2001 are unrepresentable. `schemas/document-question-answer.schema.json:33,39-40`, `document-question-result.schema.json:35,41-42`, `file-organizer-answer.schema.json:20-21,31` and `file-organizer-result.schema.json:22-23,34` all carry `page maximum: 2000` and `span.start`/`span.end maximum: 5000000`, so a grounded answer whose evidence is on page 2400 of a standards PDF the person selected cannot be expressed — the provider must fabricate a wrong citation or fail the whole answer, in a workload whose manifest promises every cited page and span matches the input bytes exactly. Remove the `maximum` keywords; `minimum` carries the real invariant. |
| OMNI-0407 | open | high | s | `supervisor_recovery.py:375-376,403-478` stores `asyncio.create_task(host._recover(...))` in `host._recoveries` and neither awaits it nor attaches a done-callback, while `recover_worker` and `_mark_exhausted` index `host._restart_attempts[plugin_id]` and `host._statuses[plugin_id]` with bare `[]` — a `revoke()` racing a recovery pops both (`supervisor.py:295-296,309-310`), so the KeyError is reachable. When it fires the task dies, the finished task stays in `_recoveries` forever, the worker sits in FAILED with no `WorkerDiagnostic`, `monitor_worker` can never fire again, and the only trace is a GC-time 'Task exception was never retrieved'. Attach a done-callback recording a RESTART_FAILED diagnostic, and use `.get(..., 0)`. |
| OMNI-0408 | open | high | s | `plugins/offloop.py:25-60` creates and starts a `threading.Thread` per call and its `finally` is `if not worker.is_alive(): worker.join()` — it joins only threads that already finished and abandons every one still running. `worker.py:151,168` calls it once per received IPC frame, and `_refresh_permission_grants` calls it every `GRANT_REFRESH_SECONDS` = 0.25 (~345,000 thread creations a day, forever); `service.py:732,738,742,757-760,857` and `control.py:138,189` add more. A caller cancelled mid-call abandons a daemon thread still inside `shutil.rmtree`, a file copy or `publisher.publish()`, which the interpreter kills mid-write at exit. The docstring's justification is wrong — `loop.run_in_executor`/`asyncio.to_thread` also park on a future without polling. Replace with one long-lived executor injected at the constructor boundary and shut down in `stop()`. |
| OMNI-0409 | open | high | s | `plugins/loading.py:277-287` loops `while not cleanup.done(): try: await asyncio.shield(cleanup) except asyncio.CancelledError: continue`, swallowing each delivered cancellation, and its trailing bare `raise` re-raises the original exception rather than the cancellation. A job cancelled while its staging directory is being cleaned therefore loses its cancellation entirely: it returns to the caller as a plain failure and continues into `_execute_with_live_grants` for a job the person withdrew. Record that a cancel arrived, shield once under `contextlib.suppress`, then re-raise `CancelledError` in preference to the original. |
| OMNI-0411 | open | high | s | `service.py:398-417,889-903` adds affected profile ids to `_pending_device_profiles`, which `_admits` (333-336) uses to refuse admission, and clears the set only at 903 after the loop — the `except Exception` at 895-897 returns without clearing. Any transient failure in `reload_accelerator_devices()` therefore wedges those profiles forever: every job is refused, `plugin_profile_statuses` still reports them idle/'Ready', and nothing retries. Clear in a `finally`, and on failure either retry with backoff or mark the profiles unavailable with a reason that reaches the published snapshot. |
| OMNI-0413 | open | low | xs | `service.py:780-819` calls `self._scheduler.start()` at 784 but `await self._scheduler.stop()` lives in the inner `finally` at 811, reachable only once the inner `try` at 793 is entered, so anything raising between the two leaves per-backend worker tasks spawned and every queued job's future unresolved. In the same function `_stopping` is never cleared and `_publish_loop` never reset to `None`, making `run()` silently non-re-entrant, and `_publish_loop` is assigned after `_transport.start()` so `request_publish()` is a no-op during startup. |
| OMNI-0414 | open | high | s | `benchmark/bench_cli.py:78-159` constructs `LlamaVulkanRuntime` with a lease at 92 and calls `asyncio_run(runtime.terminate('__startup__'))` on the straight-line path at 158 with no `try/finally`; `confirm()` raising `DeviceError` (106), `case_files.load()` raising `CaseError` (115) or `run_cases()` raising all skip it, and `measure()` swallows the exception at 217 and loops to the next model — so the next `runtime.load()` runs while the previous model still holds VRAM and the cross-worker lease, exactly the double-load the lease exists to prevent. `benchmark/prompt_trial.py:126-182` has the same gap at 172. |
| OMNI-0415 | open | high | m | `training/document_model_installation.py:77-99` writes `model.reference.onnx` and the ncnn export and then raises `native-parity-failed` at 99 without removing either, while the same function refuses to start when `portable.exists()` (83-84, `producer-conflict`) — so the second attempt cannot run until the operator manually deletes files they were never told about. `training/production_pipeline.py:26-58` already exists to 'Run one producer, removing every partial output on any failure'; route this sequence through it. |
| OMNI-0416 | open | medium | m | `training/installation.py:111-136` and `training/numeric_promotion.py:242-268` loop `install_prepared`/`install_trusted` over every compiled variant and publish the binding only after the loop, so a failure on the second of three leaves the first artifact in `artifact_root` with no binding referencing it — an orphan nothing cleans and a retry re-installs. Collect the landed paths and remove them in an `except BaseException` before re-raising, matching `production_pipeline.py:54-57`. |
| OMNI-0417 | open | low | m | `executors/tpu.py:87-102` calls `load_delegate(EDGETPU_DELEGATE)` for each model built while `ModelCache` holds up to `DEFAULT_MAX_CACHED_MODELS` = 4 interpreters (`executors/base.py:122-166`), so up to four live delegates contend for one physical Edge TPU, which normally admits one; LRU eviction drops the interpreter without an explicit close, leaving release to GC finalisers and potentially leaving a USB Coral claimed. Separately 95-96 clears `_delegate_error` before `Interpreter(...)` and `allocate_tensors()` run, so a failure there is not latched, `availability()` reports True and every later run retries the full delegate load. |
| OMNI-0418 | open | low | xs | `guard.py:357-372` runs `guard.admit(...)`, which increments `window.in_flight`, in `__init__` rather than `__enter__`. Every current use is `with guarded(...)`, but constructing one without entering it charges a concurrency slot `__exit__` never releases, locking that caller out with `concurrency-limit-exceeded` permanently. Related: `release` (307-311) no-ops when the LRU evicted the window (338-347), so an evicted-then-readmitted caller under-counts in-flight calls. |
| OMNI-0419 | open | medium | m | `plugins/orchestration.py:58,121,281,376` calls `_RUNNING_JOB.set(...)` and discards the token, and `_infer_stage`/`_deliver_stage` read the ContextVar. Two jobs awaited in the same task — a caller awaiting two `RunnerBackedDispatcher.dispatch()` coroutines without wrapping them in tasks — make the second `set` overwrite the first, so the first job's infer and deliver stages run under the second job's id and payload: the wrong tensor reaches the accelerator and the result is delivered against the wrong job. Nothing in the code or docstring enforces the one-task-per-submission rule it depends on. Reset the token in a `finally` and carry `(job_id, payload)` on the stage-output chain rather than through ambient context. |
| OMNI-0420 | open | medium | s | `plugins/flow.py:403-408,431-436` catches `BaseException` — including `CancelledError` — from the primary submitter and does `future.set_exception(error)`, so every coalesced caller awaiting `asyncio.shield(running)` has `CancelledError` raised in its own context and asyncio treats that task as cancelled: an unrelated job aborts silently with no stable refusal code and upstream `except Exception` handlers miss it entirely. The comment at 434-435 states the intent this defeats. Convert cancellation of the primary into a stable `FlowRefusedError` for waiters, or re-submit for them. |
| OMNI-0425 | open | high | s | `plugins/kernel_telemetry.py:145-161,239` fills a missing histogram `unit` with `'ns'` while `scheduler_features` ignores `histogram.unit` entirely and emits `kernelRunQueueP50UpperUs`/`kernelBlockIoP95UpperUs` straight from the bucket index. A helper exporting nanoseconds — the parser's own default — therefore produces scheduler inputs wrong by 1000x under a name asserting microseconds. Convert by declared unit before computing the percentile, or refuse the aggregate when the unit is not `us`. |
| OMNI-0426 | open | medium | s | `plugins/kernel_telemetry.py:312-350` sets `remaining = max_aggregate_bytes + 1` and exits only on EOF or `remaining == 0`, so a helper writing exactly `max_aggregate_bytes` without half-closing leaves `remaining == 1` and the final `recv(1)` blocks the whole `timeout_seconds`, raising `socket.timeout` — an `OSError` caught at 322 and reported as `HELPER_UNREACHABLE` although a complete valid document arrived. The oversize path at 349 is mislabelled identically, and `ResourceSchedulerCollector` pays the full timeout per tick. Length-prefix or newline-delimit the payload instead of relying on EOF, and give oversize its own state. |
| OMNI-0428 | open | medium | m | `hardware_collection.py:80-88`, `storage_collection.py:367-373`, `resource_collection.py:172-178` and `desktop_collection.py:267-273` are four copy-pasted `_validate_snapshot` overrides differing only in which `*_sample_error` they call, and all four raise bare `CollectionError` rather than `self.error_type` — so a profile declaring a specific type, as `NetworkCollectionError` and `PeripheralCollectionError` do, leaks the wrong class and breaks the contract `collection.py:124` advertises. `ReplaySource`, `source_health_detail` and the source Protocol each exist three times (`collection.py:60-104,371-376`; `network_collection.py:39-76,197-202`; `peripheral_collection.py:306-343,535-540`), making `collection.py`'s 'Every collector gets one of these for free' false for two of six. Add a `sample_error(item)` hook to `BoundedCollector`, raise `self.error_type` from the base, and delete the four overrides and the two copies. |
| OMNI-0429 | open | medium | s | `plugins/desktop_collection.py:249-254` gates `collect` on `DESKTOP_CONSENT_PERMISSION` but inherits `readiness()` unchanged from `collection.py:195-201`, which checks only `metadata_permission`. A host with the metadata grant and no desktop consent answers `SourceStatus.READY`/'desktop context metadata is ready', the trigger coordinator schedules collection on that basis, and every collection raises `consent-missing` — so consent withdrawal, the scenario the module docstring is written around, is invisible to readiness. Give the base a `required_permissions` tuple checked identically in both. |
| OMNI-0430 | open | medium | xs | `supervisor_session.py:283-294` builds the graceful shutdown CANCEL frame with the hardcoded `FRAME_FORMAT_VERSION` while every other service-to-worker write stamps the negotiated version (`with_protocol`, `supervisor.py:182`; `cancel_request`, `supervisor_session.py:259`). Once the protocol range widens past 1, a worker that negotiated 2 rejects the version-1 shutdown and every stop escalates through SIGTERM then SIGKILL at `DEFAULT_STOP_TIMEOUT_SECONDS` = 0.5 each, losing whatever the plugin's `stop()` would have flushed. Use `slot.agreement.protocol_version`. |
| OMNI-0431 | open | medium | s | `plugins/ipc.py:244-329` — `parse_execute`, `parse_progress` and `parse_result` check frame type and field set but never `frame.version`, though `parse_handshake` and `parse_ready` (189-213,332-347) both do. `with_protocol` exists solely to stamp the negotiated version outbound, so negotiation is decorative in one direction and unenforced in the other: a worker built against protocol 2 that negotiated down to 1 can send v2-shaped payloads that parse silently as v1. Reject any post-handshake frame whose version is not the agreed one. |
| OMNI-0432 | open | high | m | `runtime_api.py:81-110` returns `refusal.text()` — a `runtime-refusal.schema.json` document — as the method *result* when `ControlGuard` refuses, and `socket_transport.py:293-296` passes it through, so a rate-limited `submit-job` answers with `{version,status,code,message,method}` where the client expects `runtime-job-acknowledgement`, and a refused `describe-plugins` answers with a refusal where the applet expects `plugin-inventory`. A client validating the reply against the method's own schema fails with no envelope `error` to branch on, contradicting `socket_transport.py:20-26`. Map `GuardRefusedError` to an envelope error with the guard's stable code, or render the refusal in the method's own acknowledgement schema — not both shapes on one channel. |
| OMNI-0433 | open | high | s | `socket_transport.py:264-269` lets `encode_frame` raise `ControlSocketError('frame-too-large')` for an oversized *reply* into a handler whose comment assumes an inbound framing violation ('there is no frame boundary left to write a reply into'). For a reply the boundary is intact: the service simply drops the connection, the client cannot distinguish that from a crashed daemon, and the job's result is lost undiagnosed. Write an `_error_reply(request_id, 'result-too-large', ...)`, which always fits — or, with OMNI-0396, chunk the result across frames instead of refusing it. |
| OMNI-0434 | open | medium | s | `scheduler.py:376-397` populates `self._degraded[backend]` and exposes `degraded_backends()`, which has zero callers in `src/`. So when a backend fails outside job execution and every queued job is failed with '<backend> scheduling failed', the published snapshot still reports those profiles as watching/'Serving on gpu' (`profile_selection.py:114-119` consults only `select_backend`) — the person sees a healthy profile that fails every job. `stop()` (274-287) also never clears `_degraded`. Thread `degraded_backends()` into `profile_status` and clear on stop. |
| OMNI-0435 | open | medium | xs | `plugin_admission.py:50-51,81,97-99,132-140` uses `_max_waiting` only in `has_room()`, which is called only from `dispatch_routing.admit_plugin_job:41`; `_acquire`/`_push` never check it. Any path reaching `PluginAwareDispatcher.dispatch` without going through `admit` — a `JobSubmissionService` built with `admission=None`, the documented default at `job_lifecycle.py:133-135`, or a runner-driven re-dispatch — queues without bound, so the module docstring's promise that a job which cannot queue is refused before an id is minted is advisory rather than enforced. Raise `JobDispatchError('plugin-queue-full', ...)` in `_push` and keep `has_room()` as the cheap pre-check. |
| OMNI-0438 | open | medium | xs | `dispatch.py:58-64` declares `ArtifactSource` (runtime-checkable) with both `resolve` and `resolve_active`, but `dispatch_routing.py:151-153` substitutes `CachedArtifactSource` whenever `resolve_artifact` is supplied — which `service.py:467` always does — and `CachedArtifactSource` (`dispatch_routing.py:20-27`) implements only `resolve`. Nothing isinstance-checks it, so the first dispatch path to call `resolve_active` gets an `AttributeError` surfacing as `internal-error`. Drop `resolve_active` from the protocol or implement it by delegation, and isinstance-check in `InferenceJobDispatcher.__init__` so the next divergence fails at wiring time. |
| OMNI-0440 | open | low | xs | `mutation_manifest.py:275` derives a module name with `selector.split('.x', 1)[0]`, so any dotted path containing a segment beginning with `x` yields the wrong module and `_validated_scope` rejects a correct manifest with 'onlyMutate must exactly match selector modules'. Split on the separators `_module_selectors` actually emits at 88 and 96. |
| OMNI-0444 | open | low | s | `benchmark/cases.py:20` and `benchmark/bench_cli.py:42` resolve the case root as `Path(__file__).resolve().parents[3] / 'benchmarks'`, which only exists inside the source tree — `omnitensor.benchmark` ships in the wheel (`pyproject.toml:89`) while `benchmarks/` does not. From an installed package `cases.available()` returns `()`, and because it is an argparse *default* (`bench_cli.py:230`) the run degrades silently to zero workloads and exits 'nothing was measured' rather than naming the missing root. Resolve from an explicit env var or packaged data dir and refuse with the named path when absent. |
| OMNI-0446 | open | high | xs | `plugin-manifests/ask-selected-files.json:39`, `event-extraction.json:38`, `file-organizer.json:28` and `selected-text-tools.json:34` give `qwen3-5-9b-iq4-xs` a `sourceUri` of `.../resolve/main/Qwen3.5-9B-IQ4_XS.gguf` while every other artifact in the same manifests pins an immutable commit. When upstream repacks the quant on `main` the URL serves bytes that no longer match the declared `sha256` and installation fails with a digest mismatch that reads as tampering rather than upstream drift. `scripts/fetch-benchmark-candidates.sh:33,36,41,46` has the same unpinned revision. Resolve the commit the declared digests were taken from and substitute it in all five places. |
| OMNI-0447 | open | high | m | `packaging/omnitensor-training/pyproject.toml:101-102` sets `[tool.hatch.build.targets.sdist] only-include = ['pyproject.toml','README.md']` while the wheel target sources `../../src/omnitensor/training`, `../../model-recipes` and `license-files = ['../../LICENSE']`. An sdist restricted to two files contains none of them, so building from it yields a wheel with zero modules and thirteen console scripts that all `ImportError`, and ships unlicensed. The distribution is installable only from a full checkout, contradicting the stated goal at root `pyproject.toml:82-84` that a producer environment is reproducible from the lock. Either include the real sources or move `training/` and `model-recipes/` under the packaging directory so no build target escapes the project root. |
| OMNI-0448 | open | medium | xs | `scripts/provider-requirements.py:31-34` reads `Path(sys.argv[1])` with no length check, so invoked with no argument it dies on `IndexError`. CI calls it inside a process substitution (`.github/workflows/quality.yml:37`), where a traceback becomes an empty requirements file and a silently under-installed provider environment — the exact failure the script's docstring was written to prevent. Guard `len(sys.argv) != 2` with a usage `SystemExit` and exit non-zero when the path does not exist. |
| OMNI-0449 | open | medium | s | `scripts/run-provider-suites.sh` runs pytest without the `uv pip install --no-deps -e` plus `provider-requirements.py` loop that lives only in `.github/workflows/quality.yml:32-40`, though both scripts exist because missing provider dependencies once hid 35 failures of 107 that no gate reported. A developer running the script locally exactly as its header invites reproduces those failures and concludes the provider is broken. Move the install loop into the script behind a flag and reduce the CI step to calling it. |
| OMNI-0450 | open | medium | s | `acceptance_probes.py:25-39` documents `_runtime_verdict` as 'Why this runtime cannot be used, or None when it can' but returns `None` when `availability.code == DEVICE_ABSENT` — when the executor has just said the hardware is not there. `acceptance_checks.py:345-378` then places it in `usable` and the report reads 'PASS backends: accelerator runtimes usable: gpu:ncnn' on a machine that will refuse the first job with device-absent. Report the two states separately so the check is honest without failing an install on absent hardware. |
| OMNI-0451 | open | high | m | `plugins/cancellation.py:146-267` — `JobCancellationRegistry` is a domain object performing `read_json_bounded`/`write_json_atomic`/`remove_durable` on a `Path` directly, against the rule that domain logic stays free of persistence. Two costs beyond testability: `_persist` is called synchronously from `track()`, so a full or read-only disk makes *job submission* fail though the in-memory registry is fine; and `release()` pops the token before persisting, so a failed write leaves a stale journal entry and the next start reports a completed job as INTERRUPTED. It is also an O(n) read plus fsync plus rename per job start and finish. Define a `CancellationJournalStore` port, inject a JSON adapter, and keep the registry pure. |
| OMNI-0452 | open | high | s | `supervisor_recovery.py:276-278` defines `_facade_value`, which resolves `MAX_RESTARTS`, `_validate_timeout` and the exception *type* `_WorkerStartError` out of `sys.modules['omnitensor.plugins.supervisor']` — the module that imports it — at 290, 297, 312 and 429. A policy module inverting its dependency through the module registry purely to accommodate monkeypatching. Concrete failure: `except start_error_type` at 432 resolves the class at runtime, so anything rebinding `supervisor._WorkerStartError` makes `recover_worker` stop catching launch failures and the recovery task dies silently (OMNI-0407's path). `WorkerRecoveryPolicy.__post_init__` and `delay()` also pay a `sys.modules` lookup per call. Inject the limits and the error type at the constructor boundary and delete `_facade_value`. |
| OMNI-0453 | open | medium | s | The cgroup confinement subsystem is unreachable: `budgets.py:286-401` (`CgroupWorkerUsageProbe`, `create_worker_cgroup`, `configure_worker_cgroup`, `current_process_cgroup`, `join_worker_cgroup`), all 25 lines of `cgroup_exec.py`, and `supervisor_process.py:73-111,116-146`. `AsyncioSubprocessLauncher.launch` passes `cgroup=None` unconditionally (146) and the comment at 116-129 explains why cgroups were abandoned, so `_SubprocessWorker.kill()`'s cgroup branch and `wait()`'s `remove_worker_cgroup` can never be taken and `cgroup_exec.main` is an entry point nothing wires. Roughly 150 lines that read as an active safety control and enforce nothing. Delete it or re-wire it; leaving it is the worst option. |
| OMNI-0454 | open | high | s | `budgets.py:115-116,181-210,213-283` — `_wait` calls `_sample_usage()` every `resource_poll_seconds` = 0.05, so with the 600 s generative-call timeout (`worker_budgets.py:33`) that is 12,000 samples per call, each walking the whole process tree (`/proc/<pid>/task/*/children` for every thread), reading `/proc/<pid>/status` and enumerating `/proc/<pid>/fd` — then discarding the result under `contextlib.suppress(Exception)`. Nothing consumes the probe's output: `WorkerBudgetEnforcer.snapshot()` has no callers in `src/` and `WorkerBudgetCode.PROCESS`/`MEMORY`/`DESCRIPTOR`/`USAGE_UNAVAILABLE` are never raised, so the docstring's 'The probe is kept because telemetry reports the figures' is false. Either surface the samples through `WorkerBudgetSnapshot` into telemetry, making the docstring true, or delete the sampling, the probes and the four dead enum members and wait with a single `wait_for`. |
| OMNI-0455 | open | medium | s | `plugins/orchestration.py:430` wires the policy gate's readiness condition as `artifact_ready=lambda _profile: (True, '')`, so `PolicyRefusal.ARTIFACT_NOT_READY` is unreachable in production even though `plugins/policy.py:131-135,183,216-225` documents the gate as failing closed on each of four conditions — two of which default to permissive lambdas. Uninstall or corrupt an artifact while the service runs and the job is admitted, consumes a flow-control slot, is retried by `_run_with_retries`, and only then fails at `_resolve_stage` with `artifact-unavailable`: the exact outcome `orchestration.py:417-418` claims to avoid. Pass the real resolver-backed readiness check. |
| OMNI-0456 | open | medium | s | `plugins/worker.py:90-135` is a second implementation of the worker protocol loop beside `serve_worker_requests` (137-190): it cannot execute anything (every EXECUTE gets `unsupported-message`) and calls `asyncio.run()` three separate times around one plugin instance, so `plugin.start()` and `plugin.stop()` each get a different event loop and any asyncio primitive created in `start()` is bound to a loop closed before `stop()` runs. It has no callers in `src/` — only tests — and keeping it means the protocol has two definitions that can drift. Delete it and point its tests at `serve_worker_requests`. |
| OMNI-0457 | open | medium | m | `plugins/resource_collection.py:26,115-131` — a domain collector imports the concrete `UnixSocketAggregateSource` and instantiates it as its own default (`kernel_source or UnixSocketAggregateSource()`), against the rule that consumers depend on Protocols in `omnitensor.ports`. `kernel_source` is untyped and unvalidated, so any object with a `.read` attribute is accepted and a wrong one fails deep inside `asyncio.to_thread`. The class also does two jobs — cgroup/systemd/PSI sampling and eBPF aggregate fetching over a socket — and `AbsentAggregateSource` (`kernel_telemetry.py:353-360`) exists for 'no helper configured' but is used nowhere, so a host without a helper pays a `Path.exists()` syscall plus a thread hop per tick. Declare a `KernelAggregateSource` Protocol, type and check the parameter, and wire `AbsentAggregateSource` at the composition root. |
| OMNI-0458 | open | medium | m | Deficit-round-robin is implemented twice, verbatim: `scheduler.py:143-207` (`_BackendQueue.push`/`pop_weighted`/`_clamp_held_out`) and `plugin_admission.py:151-249` (`PluginAdmissionQueue._push`/`_next_profile`/`_clamp_held_out`) share the newcomer clamp `min(passes.values(), default=0.0)`, the `min(pending, key=lambda p: (passes[p], order.index(p)))` selection, the `1/max(1, min(5, weight))` stride and a textually identical `_clamp_held_out`. The drift has already begun — the stride ceiling is a bare literal 5 in the scheduler and the named `MAX_STRIDE_WEIGHT` in the admission queue, and only the admission copy defends against a non-int weight (230). Extract one `StrideQueue[T]`; about 100 lines collapse to one implementation with one test suite. |
| OMNI-0459 | open | medium | l | `service.py:134-903` — `OmniTensorService` has ~55 methods and at least seven responsibilities: composition root (it constructs `GrantLedger`, `ArtifactResolver`, `InstalledPluginRuntime`, `PolicyStore`, `JobCancellationRegistry`, `JobResultStore` and `UnixSocketAggregateSource` directly at 176-259, duplicating `composition.py`); the publish loop with idle backoff, change detection and heartbeat (638-753); plugin lifecycle and accelerator reload (821-903); policy-change diffing (359-389); an artifact-resolution facade (567-595); device-lane forwarding (448-458); and the asyncio supervision tree (780-819). Several are already extracted as *functions* in sibling modules while the class kept the state and sequencing, so the extraction bought indirection rather than separation. Split incrementally: a `SnapshotPublishLoop` owning the publish state and taking a builder callable, a `PluginLifecycle` owning `_plugin_reload_task`/`_plugin_lifecycle_lock`/`_pending_device_profiles` (which makes OMNI-0411 a local invariant), and all adapter construction moved into `composition.build_service_from_env` so the constructor takes ports only. |
| OMNI-0460 | open | medium | s | `profile_selection.py:25-27` and `runtime_api.py:17-23` resolve production values by `sys.modules.get('omnitensor.service')` plus `getattr` at call time — every status string in `profile_status` and `plugin_profile_status` across 11 call sites, and `RUNTIME_METHODS` in `_runtime_methods()`. Leaf domain modules therefore depend on the composition module invisibly, through the module registry, so that tests can monkeypatch `omnitensor.service.SERVING`. The three `__module__ = 'omnitensor.service'` reassignments (`telemetry_observation.py:94`, `profile_selection.py:183`, `runtime_api.py:113`) are the same impulse and make tracebacks name a module the class is not defined in. Use the module-level constants directly and let tests inject. |
| OMNI-0461 | open | low | s | `device_lanes.py:65-87` and `dispatch_routing.py:94-108` route by `getattr(executors, 'for_device', None)` plus `callable(...)` with a silent fallback, so the capability contract exists only as four string literals and a genuinely broken adapter degrades to 'backend name as lane' instead of failing. Declare `DeviceAwareExecutors` and `PreparedLaneDispatcher` as runtime-checkable Protocols in `ports.py`/`job_ports.py` and use `isinstance` — same behaviour, one named contract, statically visible. |
| OMNI-0462 | open | low | xs | `jobs.py:1-41` calls itself a 'stable facade' while re-exporting 20 underscore-prefixed names (`_parse_request`, `_ActiveJob`, `_RequestError`, `_terminal_status`, `_validate_code`, `_clamped` and more) — a second import path for the implementation rather than a boundary; `dispatch_routing.py:11` already imports `UnavailableJobDispatcher` from it while `job_lifecycle.py` defines it. Keep only the genuinely public names and point tests needing internals at `job_lifecycle`/`job_codec`. |
| OMNI-0463 | open | low | xs | `control.py:365-376` — `build_control_service` is called only by tests, constructs a `PolicyStore` (a persistence concrete) inside the domain module, and forwards neither `on_applied` nor `profile_models`, so a service built this way never notifies the scheduler or publisher of a policy change and falls back to `lambda _: ()` at 97, making `_model_choice_error` reject every `set-profile-model` with 'Model is not declared by this workload'. Delete it or give it the full parameter set; the `PolicyStore` import belongs in `composition.py`. |
| OMNI-0464 | open | low | xs | `control.py:44-51` — the `_CHOOSERS` module-level dispatch table calls underscore-prefixed methods on an instance passed in as a parameter, purely to avoid two `if` branches in `_execute`, which already has that shape for `set-profile-enabled` immediately below. It defeats the privacy convention, is invisible to static analysis and is longer than what it replaces. |
| OMNI-0465 | open | low | s | `executors/npu.py:64-79` — `availability()` calls `self._ensure_core().available_devices`, re-scanning OpenVINO's plugin set, once per profile per snapshot tick via `select_backend` and again synchronously on the event loop in `dispatch.py:242`'s `availability_for_model` during every dispatch. OpenVINO device enumeration is tens of milliseconds, so this is an event-loop stall on the job submission path. Cache with a short TTL, the shape `executors/tpu.py:30` already uses, invalidated on discovery change. |
| OMNI-0467 | open | medium | xs | `training/hardware.py:22` and `training/desktop.py:14` do `from .build import _fit_output`, and `training/numeric_promotion.py:41` imports `_validated_targets` from `.installation` — three modules reaching into siblings' private names. `build._fit_output` (445-460) is behaviourally identical to `tabular.fit_output` (`training/tabular/fit.py:123-138`), same 240 iterations and same defaults, so the hardware and desktop trainers drag in the build trainer's whole import graph for one line. Import `fit_output` from `.tabular`, delete `build._fit_output`, and promote `_validated_targets` to a public name. |
| OMNI-0468 | open | medium | m | `benchmark/prompt_trial.py:113-182` is a near-copy of `benchmark/bench_cli.py:68-159`: both duplicate the deferred provider imports, `os.environ['GGML_VK_VISIBLE_DEVICES']`, `MemoryFragmentStore()`, the `LlamaVulkanRuntime(store, lease_file(...))` construction, the timed `runtime.load((path,), 'gpu')`, `confirm(device, runtime.physical_device)`, `run_cases(...)` with a per-case `say` lambda differing only in a field width, `terminate('__startup__')` and an identical `Run(...)` down to `cache='q8_0'`. `prompt_trial` already imports three names from `bench_cli`, so the coupling exists and the body was copied anyway; unlike OMNI-0349's two-line dispatch wrappers these are two 45-line bodies. Extract `load_and_run(...) -> Run` into `harness.py` carrying OMNI-0414's `try/finally`. |
| OMNI-0469 | open | medium | s | `training/document_model.py:185-188` branches on `PortableBgeRunner is ProducerCpuBgeReferenceRunner`, but `training/document_model_cpu_reference.py:78` binds them to the same object and `document_model.py` imports both from that one module, so the comparison is unconditionally True and the `else` branch is unreachable in production — it exists only so a test can rebind a module global, and would raise `cpu-reference-forbidden` if it ever ran. The `_PRODUCER_CAPABILITY` sentinel is likewise not a restriction: `_producer_cpu_reference_runner` is importable by anyone and re-exported through `document_model:58`. Delete the dead branch and call it directly; express the producer-only restriction as a documented invariant rather than a sentinel any caller can route around. |
| OMNI-0470 | open | low | xs | `training/numeric_promotion.py:125-133` and `:145-155` contain character-for-character the same `isinstance(model, dict)` / missing-key / `sha256` type / `format == 'onnx'` / `filename == 'model.onnx'` checks with the same two error details, and `NumericTrainingReport.load` calls both in sequence at 79 and 82. Keep the block once in `_portable_model_identity`. |
| OMNI-0471 | open | low | m | `training/document_model_installation.py:41-59` — `DocumentModelInstallationDependencies` has 17 fields each annotated `object`, so nothing can tell what any of them must implement, and `install_document_model` (62-137) calls them positionally-by-convention across a 65-line body with five distinct responsibilities (export, gate, artifact prep, report, bind); `binding_builder` additionally breaks the pattern by defaulting to `None` and being resolved at 121. Declare Protocols for the five roles and split the function along the same seams. |
| OMNI-0472 | open | low | xs | `document_model_types.py:23` defines `PRODUCER_CPU_REFERENCE_BACKEND` in a top-level core module though its only consumers are `training/document_model.py:28` and `training/document_model_cpu_reference.py:9,19`. It is a build-time numerical-equivalence reference rather than a runtime execution path, so it does not violate the no-CPU rule as written, but its placement puts a CPU backend identifier one import away from the service and makes it read like one. Move it into `training/`. |
| OMNI-0473 | open | low | xs | `scripts/generate-media-acceptance-fixtures.py:303-310` defines a private `_sha256` with a local `import hashlib`, duplicating the chunked digest helper that the sibling `scripts/collect-selected-text-acceptance.py:17` imports from `omnitensor.preparation`; `:282` likewise uses `__import__('json')` instead of a module-level import. Two digest implementations in one directory is one that can drift from the digests the installer verifies against. |
| OMNI-0474 | open | low | xs | `scripts/benchmark-transport-codecs.py:41` copies `DEFAULT_STATE_PATH = '~/.local/state/xpu-workload-manager/state.json'` verbatim from `composition.py:15` though the script already imports from `omnitensor` at 57, so if the canonical path moves the benchmark silently reports 'no readable state file' instead of following it. |

## Blocked

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0479 | blocked | high | m | **Whether an idle runtime may stop publishing at all, which the staleness contract currently forbids.** OMNI-0478 wants no heartbeat when nothing is happening. Readers disagree by contract: `cinnamon-xpuwlm` treats a snapshot older than `STALE_AFTER_MS` (15 000) as stale, and `xpuwlm/src/xpuwlm/domain/snapshot/staleness.py` does the same, so a runtime that stops publishing while idle is indistinguishable from one that died — the panel would show "stale" on a perfectly healthy idle desktop. **Decision needed, and it is the maintainer's:** (a) readers stop inferring liveness from document age and learn it another way (the control socket answering, or a systemd unit state), which is a change in all three repositories; or (b) an idle runtime retracts the snapshot outright, so readers observe honest absence rather than staleness — the same path OMNI-0011 already built for zero devices; or (c) the heartbeat stays and "costs nothing when idle" is accepted as one write per 10 s. Nothing can be implemented for OMNI-0478's publisher half until this is answered. |
| OMNI-0297 | blocked | high | xl | Run and archive a repository-complete mutation campaign with every callable at or above 80%. — blocked: first finish OMNI-0296, choose and implement OMNI-0247's socket-access policy, OMNI-0258's bounded parity fixture and real portable/native runners, and OMNI-0259's versioned producer-request/holdout contract and reviewed adapters; then define complete core, provider, and template mutation scope and archive per-shard plus consolidated commit/version/selector evidence. The former 54-entry dependency list was pruned to the four rows that still exist and that the prose actually names. |
| OMNI-0048 | blocked | high | s | Produce and publish the `low-light-enhancement` 0.2.0 Retinexformer ncnn artifact with exact graph/weights digests, tensor/output contracts, publisher identity, portable/native parity, and hardware Vulkan proof. — blocked: requires a licensed digest-pinned source/checkpoint, an authentic paired holdout, a concrete Retinex source with portable/native producer and runner, signed ncnn artifacts, and a no-CPU Vulkan execution report. |
| OMNI-0104 | blocked | medium | s | Connect the 0.2.0 enhanced-image pipeline to the existing disjoint-folder, atomic no-overwrite publisher; preserve allowed metadata, expose progress/result evidence, and clean only partial outputs on cancellation or failure. — blocked: requires product decisions for output codec and naming, a conservative metadata/privacy allowlist, and the job/integration surface, plus a real OMNI-0048 artifact for closure. |
| OMNI-0105 | blocked | high | s | Freeze an authentic, license-reviewed paired low/normal-light holdout and record 0.2.0 portable/native SSIM, paired-target SSIM, median CIEDE2000, noise, color, range, and artifact measurements with test-only corpus bytes and source provenance. — blocked: requires the authentic paired corpus, the missing CIEDE2000/noise/color/artifact metrics, and OMNI-0048 native evidence; the existing synthetic CC0 corpus is test coverage, not fidelity evidence. |
| OMNI-0258 | blocked | high | m | Wire a `promote_main` console entry point for the five numeric trainers so a trained domain fixture can be promoted into a schema-valid binding; retain the separate forecast report path. — blocked: choose and implement a bounded parity fixture plus concrete portable ONNX and native Vulkan/OpenVINO-NPU runners; reports alone cannot prove parity, so a fake CLI is not an implementation. |
| OMNI-0259 | blocked | high | m | After production-pipeline consolidation, add one recipe-dispatched `omnitensor-produce-source` CLI that reaches embedding, CLIP, Retinexformer, and foundation-forecast producers. — blocked: choose and version a producer-request/holdout contract and concrete reviewed adapters for text, CLIP, Retinex, and forecast; a fake-injected CLI is unusable. |
| OMNI-0247 | blocked | medium | s | Harden BPF helper IPC and bound client handling. — blocked: an `AF_UNIX` stream client needs write permission on the socket path, so root-owned mode `0644` would deny every unprivileged collector. Choose a dedicated group with `0660`, a scoped ACL, or socket activation/ownership before changing access; client timeouts remain pending that decision. |
| OMNI-0085 | blocked | high | s | `hardware-health` model: define labeled fault/normal evaluation data and metrics, qualify a real accelerator-compatible anomaly model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0086 | blocked | high | s | `hardware-health` pipeline: implement feature windows, preprocessing, anomaly inference, calibrated scoring, evidence extraction, and bounded alerts while keeping firmware/shutdown/repair actions deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0087 | blocked | high | s | `hardware-health` acceptance: record fault-injection recall/false-positive, privacy, missing-sensor, latency, and named-hardware evidence before marking the plugin operational. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0088 | blocked | high | s | `storage-intelligence` model: define labeled trend/anomaly data and metrics, qualify a real accelerator-compatible model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0089 | blocked | high | s | `storage-intelligence` pipeline: implement feature windows, inference, calibrated trend/anomaly results, confidence/evidence, and cache/tiering/maintenance advice without destructive actions. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0090 | blocked | high | s | `storage-intelligence` acceptance: record replay accuracy, device-loss recovery, privacy, false-positive, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0091 | blocked | high | s | `resource-scheduler` model: define workload traces, target decisions, fairness/utility metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0092 | blocked | high | s | `resource-scheduler` pipeline: implement preprocessing and placement/timing inference, expose bounded recommendations, and keep enforcement, starvation prevention, and safety limits deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0093 | blocked | high | s | `resource-scheduler` acceptance: record utility, starvation/fairness, policy-change, overload, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0094 | blocked | high | s | `build-advisor` model: define a provenance-safe labeled corpus and ranking/risk metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0095 | blocked | high | s | `build-advisor` pipeline: implement feature extraction, inference, calibrated regression-risk/check ranking, and explainable advice without silently omitting mandatory checks. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0096 | blocked | high | s | `build-advisor` acceptance: record corpus quality, ranking/risk accuracy, adversarial-path, privacy, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0097 | blocked | high | s | `visual-library` models: define labeled classification/retrieval evaluation data and metrics, qualify real accelerator-compatible classifier/embedding models, and publish verified artifacts and tensor contracts. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0098 | blocked | high | s | `visual-library` inference: implement deterministic resize/normalization, classification/embedding inference, calibrated tags, batching, and bounded failure handling. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0101 | blocked | high | s | `visual-library` acceptance: record malformed-media isolation, index recovery, classification/retrieval quality, privacy, throughput, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0106 | blocked | high | s | Record 0.2.0 performance on the named RX 6600 XT: hardware Vulkan execution with zero CPU fallback, warm p95 ≤50 ms, and ≥1.2 measured end-to-end speedup over the documented CPU enhancement baseline. Blocked until the artifact, pipeline, and fidelity corpus exist. |
| OMNI-0108 | blocked | high | s | `network-peripherals` model: define privacy-reviewed anomaly data and metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0109 | blocked | high | s | `network-peripherals` pipeline: aggregate bounded windows, preprocess, infer and calibrate anomalies, correlate sources, and extract evidence without per-packet inference. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0110 | blocked | high | s | `network-peripherals` output: publish bounded alerts/recommendations and permit only explicitly granted, allowlisted, reversible responses outside model control. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0111 | blocked | high | s | `network-peripherals` acceptance: record privacy, hotplug/replay recovery, false-positive/recall, restricted-response, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0112 | blocked | high | s | `desktop-context` model: define consented redacted evaluation data and suggestion-quality metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0113 | blocked | high | s | `desktop-context` pipeline: preprocess context, infer calibrated context/layout suggestions, suppress stale-session results, and attach bounded explanations without capturing content. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0114 | blocked | high | s | `desktop-context` consumer: deliver only allowlisted suggestions to Cinnamon and require explicit user confirmation before any window-manager action; never manipulate windows silently. — blocked: needs coordinated work in the Cinnamon applet repository, which this repository cannot change. |
| OMNI-0115 | blocked | high | s | `desktop-context` acceptance: record consent/revocation, privacy/redaction, session churn, suggestion quality, user-action safety, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0122 | blocked | high | s | system acceptance provisioning: install, verify, configure, consent, and smoke-test every bundled plugin and artifact with representative real input and user-visible output. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0123 | blocked | high | s | system acceptance resilience: exercise device/source loss and recovery, worker crash, backpressure, cancellation, service/Cinnamon restart, and in-flight cleanup across representative plugins. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0124 | blocked | high | s | system acceptance lifecycle: verify plugin/artifact/service upgrade, configuration migration, rollback, revocation, cache cleanup, and offline startup without losing valid prior state. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0125 | blocked | high | s | system acceptance hardware: run the reproducible TPU/NPU/GPU matrix, archive evidence, and prevent any profile from claiming operational readiness until its own model, pipeline, safety, and acceptance gates pass. — blocked: the GPU lane is available (Vulkan/ncnn) but no TPU or NPU device is present, so the full matrix cannot be run. Document Intelligence's named-GPU acceptance is complete; the remaining profile rows and absent TPU/NPU hardware still block the system-wide matrix. |
| OMNI-0184 | blocked | medium | s | the eBPF helper's privileged path is unverified: the loader, map reads, and socket service were written and compile-checked, but nothing on this host can run them — loading needs CAP_BPF and `bpftool prog load` fails EPERM as this user, so `load_probes`/`read_histogram` have never executed against a real kernel; verify on a host where the helper can be installed as root before relying on the data. |
| OMNI-0198 | blocked | medium | l | TPU lane parity — produce and accept an Edge TPU artifact: the fail-closed compiler adapter now exists, but every recipe still needs a representative-calibrated fully-int8 TFLite export, the externally distributed `edgetpu_compiler`, 100% Edge TPU operation mapping, portable/native semantic parity, and named Coral execution evidence. Decide whether releases use a pinned compiler container or trusted precompiled artifacts. This host has neither the compiler nor a Coral device, so production and execution remain unverified. |
| OMNI-0235 | blocked | medium | l | Coral event-extraction alternative: Qwen2.5-VL is not a realistic Edge TPU artifact, so evaluate a small fully-int8 candidate-line/token classifier combined with deterministic calendar parsing under the same event result contract. Qualify it only with representative calibration, full compiler mapping, multilingual grounded-event quality, parity of safety/refusal semantics, and named Coral evidence; blocked here on a selected model/corpus, external compiler, and hardware. |
| OMNI-0289 | blocked | low | xs | Decide whether generation intentionally excludes TPU despite the global TPU>NPU>GPU preference. Until the product decision is made, document the absent lane at `GenerationRouter` and do not change routing. |
| OMNI-0296 | blocked | medium | s | Narrow `mutation-selectors.json` to parser and validator boundaries before spending another run. The recorded baseline (`mutation-baselines/2026-08-14.json`, 3,584 mutants, 92.0%) was taken at `88d9294` and 157 `src/` modules have changed since, so its per-callable figures no longer describe HEAD — but the applet's equivalent campaign showed where the spend pays: it measured 46 targets, raised ten above the threshold, and found no defect, with the value concentrated where a silently wrong value escapes (a date pattern, a digest, octet arithmetic) and ceremony over orchestration code the suite already asserts. So the re-scope is the work: keep `registry`, `contract`, `snapshot`, `tensorcontract`, `outputcontract`, `job_codec`-class selectors, drop orchestration-only ones, and record the rationale per dropped shard. One practical trap for the eventual run: `mutants/` is a gitignored working copy mutmut reuses, and a stale one fails every shard with "could not find any test case for any mutant" — remove it first. — blocked: mutation campaigns are created and executed by hand, never by this assistant, so nothing here can advance them.|
| OMNI-0343 | blocked | medium | m | After OMNI-0296 lands, run the narrowed non-incremental baseline, archive it beside `mutation-baselines/2026-08-14.json` with the commit it describes, and burn any callable under 80% down or record its survivors as equivalent with domain reasoning — no regressions for logging-shape or prose-only mutants. The fail-fast shard runner stops at the first failure by design; a baseline sweep drives each shard once and continues, which is what the applet's rebaseline did. — blocked: mutation campaigns are created and executed by hand, never by this assistant, so nothing here can advance them.|

## Rejected / Won't fix

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0349 | rejected | low | xs | Architecture review non-findings, kept so later audits do not re-derive them. **Trainer duplication is false:** the five trainer CLIs are two-line wrappers over one parameterized path (`training_train_cli.py` `dispatch_trainer` + `TRAINER_DISPATCH`). **Validation is single-engine for the canonical contracts:** `registry.validate_document` is the only validator of anything in `schemas/`; `snapshot.py:114` and `contract.py:93` both delegate to it. Schemas a plugin declares at runtime (a task's output schema, a workload's configuration) are necessarily validated where they are declared — `plugins/generation.py`, `plugins/settings.py`, `acceptance_checks.py` — which is a different contract, not a second engine for the same one. **Providers are correctly separated:** four of six are five-line identity shims over the shared `omnitensor-vulkan-runtime` dependency, with zero copy-pasted files by digest comparison. **Mutation tooling stays per-language:** 176 lines of JS around Stryker and 557 of Python around mutmut wrap engines that cannot be shared; only the selector-file concept overlaps, and a shared format would couple the repositories for no behavioural gain. |
| OMNI-0334 | wont_fix | medium | xs | Sonar reports missing lock files for the seven separately published provider and external-template library projects. Retain their ranged package metadata: Python library locks are not consumed when downstream applications install wheels, and each deployment environment must resolve those ranges into its own lock. The service and repository CI now use the root `uv.lock`; mark the seven library findings won't-fix after the next scan. |
| OMNI-0337 | wont_fix | medium | xs | Retain the reviewed caller-selected paths behind the remaining Sonar S8705/S8707 reports: mutation manifests and source roots plus media-fixture targets/tool discovery are explicit offline developer CLI inputs; `atomicio` is the generic path-owning persistence boundary; `file_digest` reads operator-selected artifacts; and privileged BPF helpers now receive only installed constants from `main` while tests inject temporary paths. None is reachable from a model, plug-in payload, or service request; mark any surviving reports false-positive after the next scan. |
| OMNI-0338 | wont_fix | medium | xs | Retain the reviewed semantic hotspots: the reported `tokenizer.json` value is a public SHA-256 digest, bidi controls are the sanitization denylist and its regression fixture, sandbox `/tmp` is a new private tmpfs inside the worker namespace, and `random.Random(seed)` builds a reproducible bounded conformance corpus rather than a secret. Mark those six findings false-positive. Keep the BPF socket-mode finding open under OMNI-0247 until group/ACL/socket-activation ownership is chosen. |

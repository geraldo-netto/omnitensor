# TODO

## Work groups

Items remain in the canonical lifecycle tables below; these ranges preserve the
audit's dependency order without weakening the required status schema.

- **Low-light delivery:** OMNI-0048, OMNI-0104–OMNI-0106
- **Profile model, pipeline, and acceptance:** OMNI-0085–OMNI-0098, OMNI-0101, OMNI-0108–OMNI-0115
- **System acceptance:** OMNI-0122–OMNI-0125
- **Privileged telemetry:** OMNI-0184, OMNI-0247
- **TPU lane:** OMNI-0198, OMNI-0235, OMNI-0289
- **Production pipeline consolidation:** OMNI-0258, OMNI-0259
- **Mutation gates:** OMNI-0296, OMNI-0343, OMNI-0297
- **Project split support:** OMNI-0344
- **New workloads:** OMNI-0351
- **Client workload defects (found end to end):** OMNI-0353, OMNI-0354
- **Workload tuning:** OMNI-0511–OMNI-0513, OMNI-0541, OMNI-0542
- **SonarCloud remediation:** OMNI-0334, OMNI-0337, OMNI-0338
- **2026-08-18 architecture audit:** OMNI-0387–OMNI-0477

## Findings

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0603 | open | low | xs | `SnapshotPublishLoop.await_next` can swallow a `request_publish` wake: after `asyncio.wait` returns by timeout, the `finally` block awaits `asyncio.gather(*waits)` (src/omnitensor/publish_loop.py:209) — a yield point where a `call_soon_threadsafe(self.wake.set)` scheduled by `request_publish` can run — and then unconditionally `self.wake.clear()` (publish_loop.py:210) erases it. The state change is only published at the next timer tick, up to `IDLE_PUBLISH_INTERVAL_S` (10 s) later, defeating the on-change wakeup for that window. Clear only when the wake future actually completed, or re-check `wake.is_set()` after the gather and skip the clear. |
| OMNI-0604 | open | low | s | `SocketControlTransport.start` has a check/unlink/bind TOCTOU (src/omnitensor/socket_transport.py:199-205): two instances starting concurrently can both pass `_is_served()` before either binds; the second then unlinks the first's just-bound socket file and binds its own, leaving the first serving an unlinked inode that no client can reach while both believe they own the path — and the first's `stop()` (`_bound` is True for both) later unlinks the second's live socket (socket_transport.py:224). Serialize startup with an `flock` on a sibling lock file (or bind under `O_TMPFILE`-style staging) so the second instance is refused instead of racing. |
| OMNI-0605 | open | low | xs | `call_control` leaks raw OS errors past its stable-code contract: the docstring promises it "raises ControlSocketError carrying the envelope error's code otherwise" (src/omnitensor/socket_transport.py:332), but `asyncio.open_unix_connection` failures inside `exchange()` (socket_transport.py:342) — `ConnectionRefusedError` when no service is running, `FileNotFoundError` when the socket path is absent — propagate as bare OSError. A client branching on `ControlSocketError.code` (the pattern the module documents) misses the most common failure, "service not running". Wrap connection-setup OSError in `ControlSocketError("connect-failed", ...)` naming the path. |
| OMNI-0606 | open | low | xs | `input_roots_document` and `selected_files_document` (src/omnitensor/snapshot.py:40-73 and 80-105) are line-for-line duplicates — same `list(roots)`, same `MAX_PUBLISHED_INPUT_ROOTS` guard, same `{roots, maxBytes}` shape — differing only in the over-limit message and default `max_bytes`. The bound check already exists twice and the next edit will land in one copy only (the docstrings deliberately differ; the mechanics need not). Extract one private `_roots_document(roots, max_bytes, over_limit_message)` helper both call. |

## Blocked

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0575 | blocked | high | m | **The six collectors read what six workloads are about, and nothing carries it into a corpus.** `omnitensor-record-runtime-snapshot` validates the published snapshot and accepts exactly two selectors, `queueDepth` and `runningProfiles` — the scheduler's aggregates. So the only recording path that runs unattended can describe `resource-scheduler` and no other workload: a `hardware-health` model fitted to queue depth is a model of nothing. The generic `omnitensor-record-training-sample` takes arbitrary numeric features, which is the honest route for the rest — and the numbers it wants are already read by `hardware_collection.py`, `storage_collection.py`, `network_collection.py`, `resource_collection.py`, `desktop_collection.py` and `peripheral_collection.py`, none of which is wired to anything (OMNI-0574). **Decide** whether a collector may write training samples for its own profile — which makes the corpus a by-product of running the machine rather than a thing somebody assembles by hand, and is also the moment this service starts keeping a history of what the person's hardware did. That is a privacy decision before it is an engineering one, which is why it is not made here. Found 2026-08-20 by reviewing the setup steps `../xpuwlm` shows, which had been telling every workload to train on the scheduler's two numbers. |
| OMNI-0574 | blocked | high | m | **Six advisory workloads have their collectors written and nothing to answer with.** `hardware-health`, `storage-intelligence`, `network-peripherals`, `resource-scheduler`, `build-advisor` and `desktop-context` are published profiles whose manifests declare `requirements.model` absent, so the runtime reports `no-model` and nothing runs them — while `plugins/hardware_collection.py`, `storage_collection.py`, `network_collection.py`, `resource_collection.py`, `desktop_collection.py` and `peripheral_collection.py` are written, permission-gated and read by nothing outside the export barrel. Each has a model row (OMNI-0085–OMNI-0098) waiting on a labelled corpus that does not exist. **Decide** which of two: keep the rule that a workload answers through a qualified model, in which case each of these waits on data somebody has to gather and licence — that is the honest cost, and it is not code; or let an advisory workload answer from measurements it already collects, with the arithmetic stated and bounded, and no model at all. The second makes `hardware-health` and `storage-intelligence` implementable immediately, and it is a change to what a workload *is* in this service, which is why it is not made here. Found 2026-08-20 while answering why seven profiles do not work. |
| OMNI-0567 | blocked | medium | s | **One input bound is stated three times, and the copies have already disagreed.** Each workload's field bounds live in its manifest's `schemas.input`, again in `../xpuwlm`'s `workflows/specs/catalog.py` as `maximum=`/`max_length=`, and a third time in this repository's own `tests/test_client_workload_contracts.py` as `CLIENT_SOURCE_BOUNDS` — a hand-kept table checking a hand-kept transcription. OMNI-0564 is the disagreement that already happened. The client cannot stop transcribing while the `input` schema is unreachable: `describe-plugins` publishes `configurationSchema` and not `input`, and reading manifests off disk works only for a bundled workload. **Decide** whether `plugin-inventory.schema.json` carries each plugin's `input` schema — the same decision `../xpuwlm` G5 asks from the other side. If it does, two of the three copies go: the client derives its fields from what it is told, and this test derives its table from the manifests instead of restating them. Considered and not chosen here: a third distribution holding the workload contract. `../xpuwlm` already imports six modules from this wheel and declares it a hard dependency, so the shared library exists — what is missing is not a package but the schema's reach, and a new distribution would add a version to keep in step without removing a copy. |
| OMNI-0564 | blocked | low | xs | **The client refuses at sixteen documents and the manifest states no bound.** `document-translation`'s input schema declares `minItems: 1` and no `maxItems`, alone among the five: `ask-selected-files` and `file-organizer` state 16, `event-extraction` 32, `media-transcription` 1. `../xpuwlm`'s spec bounds its chooser at sixteen and its hint says so, so today a person using the window is refused at seventeen by a number no published contract carries — which is the client enforcing a rule the runtime does not apply, the one thing its own AGENTS.md tells it not to do — while a caller using the socket directly may send any number. Found 2026-08-20 while retiring OMNI-0544. **Decide** which way they agree: the manifest declares `maxItems: 16` like its siblings, which refuses a submission the runtime accepts today; or the client drops its bound and lets a person translate as many documents as they select, which is what the standing rule about ceilings would say if this were a ceiling on an answer rather than on a selection. Not guessed at here — one of them takes something away from somebody. |
| OMNI-0552 | blocked | medium | l | **`OmniTensorService` step 1d of 3: the constructor takes ports only.** Delete the path arguments and every `or ...` default from `__init__` and move all fourteen construction sites onto `composition.build_service`, including the `build_service` helpers in `test_service.py`, `test_service_run.py` and `test_device_events.py`. **Blocked 2026-08-20: this half cannot be split.** The moment a default is deleted every caller that relied on it stops working, so the deletion and the fourteen sites are one commit — there is no ordering that leaves the tree green in between, which is the property every other row in this file has. Unpacked into OMNI-0560 and OMNI-0561, which are the parts that can be done one at a time; what is left here is the switch to throw once they are. **2026-08-20: OMNI-0560 and OMNI-0561 are done, so the root now builds the plugin runtime and the control service around each other and hands both over. What is left here is exactly the switch: delete the `or ...` defaults and the path arguments, and move the fourteen sites in the same commit.** |
| OMNI-0559 | blocked | medium | s | **A command operation this service does not accept is indistinguishable from a broken service, which is what the handshake exists to prevent.** `describe-contract` reports method names and the version each wire document pins (`contract.py`), and `set-profile-configuration` (OMNI-0541) changed neither: it is a new `operation` inside `runtime-command`, whose `version` is still `2`. So a client that offers workload tuning to a person — `../xpuwlm` T1 — and meets a service one release older gets "Command does not match the version 2 contract", the exact wrong diagnosis this module was written for; the docstring already says a name says nothing about the body behind it, and now a body version says nothing about the operations behind *it*. Found 2026-08-20 while reviewing the command that caused it. The fix is to publish the operations `apply-command` accepts, derived from the schema's own enum so it cannot drift — but **decide first**, because the handshake cannot be extended without answering it: `runtime-contract.schema.json` is `additionalProperties: false` over a `version` of `const 1`, so adding the field makes the document version 2 and every client validating against its own copy of version 1 refuses the handshake outright. Either that document gains a forward-compatible rule — unknown keys ignored, version a minimum rather than a constant — or the operations are announced somewhere already open. The first is the one that stops this recurring for the next additive change. |
| OMNI-0558 | blocked | high | s | **A workload's configuration can be set and never read back.** `set-profile-configuration` (OMNI-0541) stores what a person chose and `plugin_inventory_entry` publishes the `configurationSchema` that says what may be set — but nothing publishes the *values*, so no client can draw a control at the setting it is actually at: it would have to show the manifest's defaults whatever the store holds, which is the guess `../xpuwlm`'s own rule forbids, and a second client would never see the first one's change. `../xpuwlm` T1 is the client half and is blocked on this. **Blocked 2026-08-20 on a decision that is yours, because it reverses a stated one:** `plugin-inventory.schema.json` opens by declaring "Contains no configuration values, because a value can be a resolved secret." What has since become true is that the store never holds a resolved secret — `validate_secret_references` requires a `{provider, key}` reference at every path the manifest marks, and resolution happens at the worker — so the values could be published with the declared secret paths withheld. **Decide** between: publish the stored configuration and its revision per plugin, withholding every path in `secretConfigurationKeys`, and rewrite that sentence; or keep the inventory value-free and give the read-back its own method, which is a second call for a client that already reads this one. Not guessed at here: the first widens the document every client parses, and the sentence it contradicts was written deliberately. |
| OMNI-0478 | blocked | high | l | **An idle runtime must cost nothing.** The maintainer's requirement, stated 2026-08-18: OmniTensor may not use any resource, nor keep a heartbeat, unless it is actually in use. Discovery is done: `_rediscover` no longer polls, it sleeps on a `NETLINK_KOBJECT_UEVENT` socket behind the optional `DeviceChangeSource` port and reads sysfs only when the kernel reports an accel/apex/drm/pci/usb event, so an idle machine spends no wakeups on it; a host with no event source polls `DISCOVERY_INTERVAL_S` as before and logs that fallback with the wakeups it costs. **Still outstanding: (a)** the publisher, which ticks every `IDLE_PUBLISH_INTERVAL_S` (10 s) and rewrites the snapshot every `SNAPSHOT_HEARTBEAT_INTERVAL_S` (10 s) purely to prove the runtime is alive — that half cannot be implemented until OMNI-0479 decides whether an idle runtime may stop publishing at all; **(b)** done 2026-08-19: workers start on demand and stop after WORKER_IDLE_TIMEOUT_S. **Blocked 2026-08-20: (b) is done and (a) is the whole remainder, and (a) cannot be written until OMNI-0479 — itself blocked on the maintainer — says whether an idle runtime may stop publishing at all.** |
| OMNI-0518 | blocked | medium | s | **Split the shared provider, step 2 of 3: dissolve `hebrew.py`.** One named model inside a general runtime. Express it as a translation task the generic adapter runs, so adding a second language is a task rather than a module. **Blocked 2026-08-20 on the hardware:** the module exists because DictaLM is prompted in Hebrew and answers as text, while the generic adapter asks for grammar-constrained JSON and binds grounding metadata. Moving the translation onto it changes what a shipped model is asked to produce, and the failure mode is a worse Hebrew answer rather than an error — which only a measured run on the GPU can tell apart. Same measurement OMNI-0501 and OMNI-0523 are waiting for. |
| OMNI-0554 | blocked | high | m | **Nothing in the runtime, in its protocol or in any client resolves an alert, so the count only grows.** `SummaryStore.resolve` (`src/omnitensor/plugins/summaries.py:122`) is written, tested and has no production caller: grep finds no command, no service method and no client path that reaches it, and `_summaries` is an unbounded dict with no eviction and no expiry. The live desk today publishes three unresolved alerts, all from `visual-library`, all the same result — "carpenter's kit · 5 candidates, best 0.218" — from three separate jobs, timestamps 1787215166090, 1787215194941 and 1787215197243. Nothing is wrong: no plugin health entry needs attention and the runtime reads `connected`. But `cinnamon-xpuwlm` counts unresolved alerts and the panel therefore says "3 items need review", and no surface anywhere can make it say anything else. **Decide** which the alert list is: a record a person acknowledges, which needs a `resolve-alert` command in the protocol and a surface that sends it (`../xpuwlm` K7 is the client half, and its `Alert` normalization is already written and unread); or a rolling window of recent results, which needs a retention rule here — count, age, or both — and no command at all. Not guessed at: the first makes an alert a thing somebody owes an answer to, the second makes it a thing they may have missed, and the two clients read the same field either way. **Blocked 2026-08-20: the decision above is the maintainer's and nothing here can be written until it is made.** |
| OMNI-0553 | blocked | medium | xs | The control envelope's `internal-error` carries `f"{type(error).__name__}: {error}"` (`socket_transport.py:319`), so an exception a method did not contain reaches any local client as raw platform text — a `FileNotFoundError` out of `describe-plugins` renders the absolute artifact path, and an `OSError` renders the platform's own wording. This repository states the opposite rule everywhere else: every refusal carries a stable versioned code, and `test_local_plugin_runner.py::test_runner_maps_artifact_and_pipeline_failures_without_private_details` asserts a plugin result detail carries no arbitrary exception text. **Blocked 2026-08-20, maintainer decision needed, and it is the same decision as OMNI-0402:** `tests/test_socket_transport.py::test_a_handler_failure_is_an_internal_error_not_a_dead_connection` deliberately pins `"scheduler exploded" in error.detail`, so the exception text in the envelope is a contract somebody wrote on purpose. Which rules — the envelope message is a published field that carries a stable code and nothing else (log the exception, tell the caller the type at most, and that test changes with it), or a same-uid local client is a debugging peer and the text stays? Nothing here is implemented until that is answered. |
| OMNI-0499 | blocked | low | s | **Is per-request unload the intended cross-worker GPU-lease policy?** `providers/vulkan-runtime/.../runtime.py:160` — `generate()`'s `finally: self._release()` unloads the GGUF and drops the lease after every request, so the next request re-reads and re-offloads a multi-GB model and the cached `_load_report` fast path is almost never hit. Deliberate, so another worker can take the GPU between requests, or an accident? Maintainer's call. |
| OMNI-0392 | blocked | critical | m | Collectors truncate below the allowlist the user configured. `plugins/collection.py:29,153,238,251` does `selected = eligible[: self._max_items]` and reports the loss only as an integer, and the defaults sit *below* the allowlist sizes the same modules accept — `peripheral_collection.py:250-251,376` validates 64 device ids but defaults `max_devices=32`; `network_collection.py:21,109` validates 256 links but defaults `max_links=64`. A person who deliberately allowlists 40 peripherals is shown 32. `schemas/collected-output.schema.json` reinforces it at `/$defs/items maxItems: 1024`, `/$defs/truncated maximum: 1024` (a ceiling on the number reporting a ceiling) and `/properties/churn/{added,removed,changed} maxItems: 1024` — the last directly reinstating the truncation `collection.py:239-241`'s own comment exists to prevent. Emit every eligible item, drop the `truncated*` fields, and coordinate the schema change with the applet repository. **Blocked 2026-08-19, maintainer decision needed:** dropping the `truncated*` fields and raising `maxItems` is a breaking change to `collected-output.schema.json`, which the sibling applet repository consumes and this repository does not own. Which is wanted — (a) emit every eligible item and delete `truncated*` in one coordinated schema version, or (b) keep the fields for one release, defaulting the per-collector maxima up to the allowlist sizes each module already validates (64 peripherals, 256 links) so no configured item is silently dropped, and remove them next? Nothing here is implemented until that is answered. |
| OMNI-0402 | blocked | medium | xs | `sdk/testing.py:331-336` reduces any exception to `f"local pipeline failed: {type(error).__name__}"` and `sdk/smoke.py:70-71,78-79` does the same for entry-point enumeration and plugin load, so a plugin author debugging their own preprocess step is told only 'local pipeline failed: ValueError'. **Blocked 2026-08-19, maintainer decision needed:** including `str(error)` contradicts a contract the suite states deliberately — `tests/test_local_plugin_runner.py::test_runner_maps_artifact_and_pipeline_failures_without_private_details` asserts `"private" not in failed.result.detail`, and `tests/test_external_plugin_ci.py` pins both smoke strings to the type name alone. Which rules: the plugin result detail is a published field that must carry no arbitrary exception text (keep as is, and diagnose through logs instead), or the SDK harness and smoke CLI are author-facing tools where the message wins (then those three tests change with it)? Nothing here is implemented until that is answered. |
| OMNI-0432 | blocked | high | s | Runtime side done 2026-08-19: `GuardRefusedError` propagates out of `RuntimeAPI` and `socket_transport._reply_for` renders it as a `control-reply` envelope error carrying the guard's stable code, so one shape travels per channel and every `result` validates against its own method's schema. Remaining, and outside this repository: the applet's `runtime-control-gateway.js` and `guard-refusal-regression.test.js` read the refusal codes out of the *result* document; they must read `error.code` instead. Blocked on the sibling applet checkout / maintainer. |
| OMNI-0446 | blocked | high | xs | **Which upstream commit were the declared `sha256` digests taken from?** `plugin-manifests/ask-selected-files.json:39`, `event-extraction.json:38`, `file-organizer.json:28` and `selected-text-tools.json:34` give `qwen3-5-9b-iq4-xs` a `sourceUri` of `.../resolve/main/Qwen3.5-9B-IQ4_XS.gguf` while every other artifact in the same manifests pins an immutable commit. When upstream repacks the quant on `main` the URL serves bytes that no longer match the declared `sha256` and installation fails with a digest mismatch that reads as tampering rather than upstream drift. `scripts/fetch-benchmark-candidates.sh:33,36,41,46` has the same unpinned revision. Resolve the commit the declared digests were taken from and substitute it in all five places. Blocked on that answer: nothing in this tree records the revision, and the digests can only be matched against `unsloth/Qwen3.5-9B-GGUF` upstream. Pinning a guessed commit would swap an upstream-drift failure for a permanent one, so the maintainer (or a host with network access to the repo's commit history) has to supply the revision the digests belong to; the substitution into the four manifests and `scripts/fetch-benchmark-candidates.sh:33,36,41,46` is mechanical once it is known. |
| OMNI-0506 | blocked | medium | s | **OCR on the Vulkan lane, step 1 of 4: choose the model pair.** From the codexa-project evaluation (2026-08-15, checked against the live repos): Paddle has no Vulkan backend — `paddle/phi/backends` holds `cpu, custom, dynload, gpu, onednn, xpu` as of PaddleOCR v3.7.0 / Paddle v3.3.0 / Paddle-Lite v2.14-rc — so PaddleOCR cannot run under the no-CPU-fallback rule, and ncnn is the mature answer: a genuine Vulkan backend (`src/gpu.cpp`, `pipeline.cpp`, `command.cpp`), on PyPI (1.0.20260526), with existing PaddleOCR ports, and already this service's GPU engine (`executors/vulkan.py`). Rejected and recorded so it is not re-derived: Paddle's out-of-tree `backends/custom` seam exists (`get_all_custom_device_type()` returns `[]` here) but means writing the kernels. This item: name the detection and recognition models and the ncnn ports to use, with their licences. **Blocked 2026-08-20:** which ncnn OCR pair to ship is a decision the maintainer owns, and it cannot be settled from here — every candidate has to be run on the RX 6600 XT against a corpus before it can be chosen, and there is no GPU on this host.
| OMNI-0507 | blocked | medium | s | **OCR on the Vulkan lane, step 2 of 4: pin the artifacts.** Obtain or build the ncnn ports chosen by OMNI-0506 and add them to a model recipe with immutable source URIs, sha256 digests and byte counts, the way `model-recipes/` pins every other native artifact. **Blocked 2026-08-20:** the ncnn ports do not exist as pinned artifacts yet; obtaining or building them needs the conversion toolchain and the model pair OMNI-0506 has not chosen.
| OMNI-0508 | blocked | medium | s | **OCR on the Vulkan lane, step 3 of 4: the workload manifest.** Declare the OCR profile in a manifest with its tensor contracts, output contract and input contract, validated against `schemas/workload-manifest.schema.json`, so the existing executor, confinement and artifact machinery run it without new machinery. **Blocked 2026-08-20:** the manifest states the tensor contracts of a model pair that OMNI-0506 has not chosen, so writing one now would be a contract invented from nothing.
| OMNI-0509 | blocked | medium | s | **OCR on the Vulkan lane, step 4 of 4: acceptance.** Gate the OCR profile on the GPU lane like every other profile — a corpus with checkable expectations, a receipt recorded on the named device. Needs the hardware and the artifacts from OMNI-0507. **Blocked 2026-08-20:** needs the hardware and the artifacts from OMNI-0507.
| OMNI-0479 | blocked | high | m | **Whether an idle runtime may stop publishing at all, which the staleness contract currently forbids.** OMNI-0478 wants no heartbeat when nothing is happening. Readers disagree by contract: `cinnamon-xpuwlm` treats a snapshot older than `STALE_AFTER_MS` (15 000) as stale, and `xpuwlm/src/xpuwlm/domain/snapshot/staleness.py` does the same, so a runtime that stops publishing while idle is indistinguishable from one that died — the panel would show "stale" on a perfectly healthy idle desktop. **Decision needed, and it is the maintainer's:** (a) readers stop inferring liveness from document age and learn it another way (the control socket answering, or a systemd unit state), which is a change in all three repositories; or (b) an idle runtime retracts the snapshot outright, so readers observe honest absence rather than staleness — the same path OMNI-0011 already built for zero devices; or (c) the heartbeat stays and "costs nothing when idle" is accepted as one write per 10 s. Nothing can be implemented for OMNI-0478's publisher half until this is answered. |
| OMNI-0297 | blocked | high | xl | Run and archive a repository-complete mutation campaign with every callable at or above 80%. — blocked: first finish OMNI-0296, choose and implement OMNI-0247's socket-access policy, OMNI-0258's bounded parity fixture and real portable/native runners, and OMNI-0259's versioned producer-request/holdout contract and reviewed adapters; then define complete core, provider, and template mutation scope and archive per-shard plus consolidated commit/version/selector evidence. The former 54-entry dependency list was pruned to the four rows that still exist and that the prose actually names. |
| OMNI-0048 | blocked | high | s | Produce and publish the `low-light-enhancement` 0.2.0 Retinexformer ncnn artifact with exact graph/weights digests, tensor/output contracts, publisher identity, portable/native parity, and hardware Vulkan proof. — blocked: requires a licensed digest-pinned source/checkpoint, an authentic paired holdout, a concrete Retinex source with portable/native producer and runner, signed ncnn artifacts, and a no-CPU Vulkan execution report. |
| OMNI-0104 | blocked | medium | s | Connect the 0.2.0 enhanced-image pipeline to the existing disjoint-folder, atomic no-overwrite publisher; preserve allowed metadata, expose progress/result evidence, and clean only partial outputs on cancellation or failure. — blocked: requires product decisions for output codec and naming, a conservative metadata/privacy allowlist, and the job/integration surface, plus a real OMNI-0048 artifact for closure. |
| OMNI-0105 | blocked | high | s | Freeze an authentic, license-reviewed paired low/normal-light holdout and record 0.2.0 portable/native SSIM, paired-target SSIM, median CIEDE2000, noise, color, range, and artifact measurements with test-only corpus bytes and source provenance. — blocked: requires the authentic paired corpus, the missing CIEDE2000/noise/color/artifact metrics, and OMNI-0048 native evidence; the existing synthetic CC0 corpus is test coverage, not fidelity evidence. |
| OMNI-0258 | blocked | high | m | Wire a `promote_main` console entry point for the five numeric trainers so a trained domain fixture can be promoted into a schema-valid binding; retain the separate forecast report path. — blocked: choose and implement a bounded parity fixture plus concrete portable ONNX and native Vulkan/OpenVINO-NPU runners; reports alone cannot prove parity, so a fake CLI is not an implementation. |
| OMNI-0259 | blocked | high | m | After production-pipeline consolidation, add one recipe-dispatched `omnitensor-produce-source` CLI that reaches embedding, CLIP, Retinexformer, and foundation-forecast producers. — blocked: choose and version a producer-request/holdout contract and concrete reviewed adapters for text, CLIP, Retinex, and forecast; a fake-injected CLI is unusable. |
| OMNI-0247 | blocked | medium | s | Harden BPF helper IPC and bound client handling. — blocked: an `AF_UNIX` stream client needs write permission on the socket path, so root-owned mode `0644` would deny every unprivileged collector. Choose a dedicated group with `0660`, a scoped ACL, or socket activation/ownership before changing access; client timeouts remain pending that decision. |
| OMNI-0085 | blocked | high | s | `hardware-health` model: define labeled fault/normal evaluation data and metrics, qualify a real accelerator-compatible anomaly model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys`, checked 2026-08-20 — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0086 | blocked | high | s | `hardware-health` pipeline: implement feature windows, preprocessing, anomaly inference, calibrated scoring, evidence extraction, and bounded alerts while keeping firmware/shutdown/repair actions deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0087 | blocked | high | s | `hardware-health` acceptance: record fault-injection recall/false-positive, privacy, missing-sensor, latency, and named-hardware evidence before marking the plugin operational. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0088 | blocked | high | s | `storage-intelligence` model: define labeled trend/anomaly data and metrics, qualify a real accelerator-compatible model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys`, checked 2026-08-20 — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0089 | blocked | high | s | `storage-intelligence` pipeline: implement feature windows, inference, calibrated trend/anomaly results, confidence/evidence, and cache/tiering/maintenance advice without destructive actions. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0090 | blocked | high | s | `storage-intelligence` acceptance: record replay accuracy, device-loss recovery, privacy, false-positive, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0091 | blocked | high | s | `resource-scheduler` model: define workload traces, target decisions, fairness/utility metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys`, checked 2026-08-20 — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0092 | blocked | high | s | `resource-scheduler` pipeline: implement preprocessing and placement/timing inference, expose bounded recommendations, and keep enforcement, starvation prevention, and safety limits deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0093 | blocked | high | s | `resource-scheduler` acceptance: record utility, starvation/fairness, policy-change, overload, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0094 | blocked | high | s | `build-advisor` model: define a provenance-safe labeled corpus and ranking/risk metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys`, checked 2026-08-20 — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0095 | blocked | high | s | `build-advisor` pipeline: implement feature extraction, inference, calibrated regression-risk/check ranking, and explainable advice without silently omitting mandatory checks. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0096 | blocked | high | s | `build-advisor` acceptance: record corpus quality, ranking/risk accuracy, adversarial-path, privacy, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0097 | blocked | high | s | `visual-library` models: define labeled classification/retrieval evaluation data and metrics, qualify real accelerator-compatible classifier/embedding models, and publish verified artifacts and tensor contracts. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys`, checked 2026-08-20 — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0098 | blocked | high | s | `visual-library` inference: implement deterministic resize/normalization, classification/embedding inference, calibrated tags, batching, and bounded failure handling. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0101 | blocked | high | s | `visual-library` acceptance: record malformed-media isolation, index recovery, classification/retrieval quality, privacy, throughput, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0106 | blocked | high | s | Record 0.2.0 performance on the named RX 6600 XT: hardware Vulkan execution with zero CPU fallback, warm p95 ≤50 ms, and ≥1.2 measured end-to-end speedup over the documented CPU enhancement baseline. Blocked until the artifact, pipeline, and fidelity corpus exist. |
| OMNI-0108 | blocked | high | s | `network-peripherals` model: define privacy-reviewed anomaly data and metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys`, checked 2026-08-20 — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0109 | blocked | high | s | `network-peripherals` pipeline: aggregate bounded windows, preprocess, infer and calibrate anomalies, correlate sources, and extract evidence without per-packet inference. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0110 | blocked | high | s | `network-peripherals` output: publish bounded alerts/recommendations and permit only explicitly granted, allowlisted, reversible responses outside model control. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0111 | blocked | high | s | `network-peripherals` acceptance: record privacy, hotplug/replay recovery, false-positive/recall, restricted-response, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0112 | blocked | high | s | `desktop-context` model: define consented redacted evaluation data and suggestion-quality metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys`, checked 2026-08-20 — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0113 | blocked | high | s | `desktop-context` pipeline: preprocess context, infer calibrated context/layout suggestions, suppress stale-session results, and attach bounded explanations without capturing content. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0114 | blocked | high | s | `desktop-context` consumer: deliver only allowlisted suggestions to Cinnamon and require explicit user confirmation before any window-manager action; never manipulate windows silently. — blocked: needs coordinated work in the Cinnamon applet repository, which this repository cannot change. |
| OMNI-0115 | blocked | high | s | `desktop-context` acceptance: record consent/revocation, privacy/redaction, session churn, suggestion quality, user-action safety, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0122 | blocked | high | s | system acceptance provisioning: install, verify, configure, consent, and smoke-test every bundled plugin and artifact with representative real input and user-visible output. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0123 | blocked | high | s | system acceptance resilience: exercise device/source loss and recovery, worker crash, backpressure, cancellation, service/Cinnamon restart, and in-flight cleanup across representative plugins. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0124 | blocked | high | s | system acceptance lifecycle: verify plugin/artifact/service upgrade, configuration migration, rollback, revocation, cache cleanup, and offline startup without losing valid prior state. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0125 | blocked | high | s | system acceptance hardware: run the reproducible TPU/NPU/GPU matrix, archive evidence, and prevent any profile from claiming operational readiness until its own model, pipeline, safety, and acceptance gates pass. — blocked: the GPU lane is available (Vulkan/ncnn) but no TPU or NPU device is present, so the full matrix cannot be run. Document Intelligence's named-GPU acceptance is complete; the remaining profile rows and absent TPU/NPU hardware still block the system-wide matrix. |
| OMNI-0184 | blocked | medium | s | the eBPF helper's privileged path is unverified: the loader, map reads, and socket service were written and compile-checked, but nothing on this host can run them — loading needs CAP_BPF and `bpftool prog load` fails EPERM as this user, so `load_probes`/`read_histogram` have never executed against a real kernel; verify on a host where the helper can be installed as root before relying on the data. |
| OMNI-0198 | blocked | medium | l | TPU lane parity — produce and accept an Edge TPU artifact: the fail-closed compiler adapter now exists, but every recipe still needs a representative-calibrated fully-int8 TFLite export, the externally distributed `edgetpu_compiler`, 100% Edge TPU operation mapping, portable/native semantic parity, and named Coral execution evidence. Decide whether releases use a pinned compiler container or trusted precompiled artifacts. This host has neither the compiler nor a Coral device, so production and execution remain unverified. |
| OMNI-0235 | blocked | medium | l | Coral event-extraction alternative: Qwen2.5-VL is not a realistic Edge TPU artifact, so evaluate a small fully-int8 candidate-line/token classifier combined with deterministic calendar parsing under the same event result contract. Qualify it only with representative calibration, full compiler mapping, multilingual grounded-event quality, parity of safety/refusal semantics, and named Coral evidence; blocked here on a selected model/corpus, external compiler, and hardware. |
| OMNI-0289 | blocked | low | xs | Decide whether generation intentionally excludes TPU despite the global TPU>NPU>GPU preference. Until the product decision is made, document the absent lane at `GenerationRouter` and do not change routing. |
| OMNI-0296 | blocked | medium | s | Narrow `mutation-selectors.json` to parser and validator boundaries before spending another run. The recorded baseline (`mutation-baselines/2026-08-14.json`, 3,584 mutants, 92.0%) was taken at `88d9294` and 157 `src/` modules have changed since, so its per-callable figures no longer describe HEAD — but the applet's equivalent campaign showed where the spend pays: it measured 46 targets, raised ten above the threshold, and found no defect, with the value concentrated where a silently wrong value escapes (a date pattern, a digest, octet arithmetic) and ceremony over orchestration code the suite already asserts. So the re-scope is the work: keep `registry`, `contract`, `snapshot`, `tensorcontract`, `outputcontract`, `job_codec`-class selectors, drop orchestration-only ones, and record the rationale per dropped shard. One practical trap for the eventual run: `mutants/` is a gitignored working copy mutmut reuses, and a stale one fails every shard with "could not find any test case for any mutant" — remove it first. — blocked: mutation campaigns are created and executed by hand, never by this assistant, so nothing here can advance them.|
| OMNI-0343 | blocked | medium | m | After OMNI-0296 lands, run the narrowed non-incremental baseline, archive it beside `mutation-baselines/2026-08-14.json` with the commit it describes, and burn any callable under 80% down or record its survivors as equivalent with domain reasoning — no regressions for logging-shape or prose-only mutants. The fail-fast shard runner stops at the first failure by design; a baseline sweep drives each shard once and continues, which is what the applet's rebaseline did. — blocked: mutation campaigns are created and executed by hand, never by this assistant, so nothing here can advance them.|
| OMNI-0502 | blocked | medium | xs | The applet has not been told the reason bounds are gone. OMNI-0398 removed every truncation on a refusal reason here and dropped `maxLength` from `runtime-acknowledgement`, `runtime-job-acknowledgement`, `runtime-job-result`, `control-reply` and the three `runtime-snapshot` detail/reason fields, so a joined multi-backend reason now arrives whole. `cinnamon-xpuwlm` mirrors these schemas in its own contract tests and lays out `detail` in fixed-width labels; both need the matching change, and that repository is not editable from here. Blocked on the maintainer applying the mirror.
| OMNI-0380 | blocked | high | m | Nothing installs the model all four workloads default to. `providers/vulkan-runtime/src/omnitensor_vulkan_runtime/qualification.json` names `qwen3-5-9b-iq4-xs` as the `default` for `ask-selected-files`, `event-extraction`, `file-organizer` and `selected-text-tools`, and each manifest declares it as its first artifact — but `omnitensor.qwen_installation` still pins the 8B alone (`QWEN_REFERENCE`, `QWEN_SIZE_BYTES = 5_027_783_488`), `generation-models/` carries no catalog entry for the 9B, and `docs/qwen-workload-installation.md` documents only the 8B URL, digest and byte count. A host installed exactly as documented therefore has no 9B artifact mounted, and `_generation`'s `bootstrap.chosen_or(default_model(...))` refuses with `artifact-unavailable` unless the person explicitly chooses the 8B — the documented install produces four workloads that cannot start. Pin the 9B digest and size in an installation path, add its catalog entry, and document its download beside the 8B's. **Blocked 2026-08-20:** pinning the 9B needs the upstream commit and byte count OMNI-0446 is waiting on — its `sourceUri` is `.../resolve/main/...`, so an installation path written today would pin a mutable ref.
| OMNI-0384 | blocked | high | m | The qualification receipt no longer describes the tasks that run. Removing the ceilings on what a person is told changed each workload's `outputSchema`, and the task digest covers it, so `ask-selected-files` and `selected-text-tools` both moved: `selected-text-tools` is now `7a87a474…` against the recorded `c6ab03dc…`. Eleven tests in the `providers/vulkan-runtime` suite fail on the mismatch (`test_shipped_qualification.py::test_the_shipped_receipt_describes_the_task_that_will_run`, `test_runtime.py::test_receipt_binds_each_exact_task_and_model`). Nothing is gated by this — the receipt reports coverage rather than deciding what may run — so the workloads execute correctly; what is wrong is that the receipt now claims to have measured a task that no longer exists. The fix is a GPU acceptance run per pair and a re-recorded `qualification.json`; the tests' own docstrings forbid editing the expectation to match the code, and there is no re-qualification script in `scripts/`. **Blocked 2026-08-20:** re-recording the receipt is a GPU acceptance run on the host, the same maintainer action OMNI-0501 waits on.
| OMNI-0353 | blocked | high | m | `file-organizer` fails in bursts, on inputs that succeed minutes later. Measured: one burst of four consecutive single-source failures (`plan-invalid`, then `provider-output-invalid` on a retry) where the same file submitted alongside a second one succeeded; a later burst of two-source failures (`evidence-invalid`) inside a test run while the same pair succeeded standalone; and then eleven consecutive successes, single and paired, over the following ten minutes with no change to the inputs or the service. So the defect is not the single-source case it first looked like — it is a worker that intermittently produces a plan its own contract refuses, and recovers on its own. Worth catching in that state: `plan-invalid` and `evidence-invalid` come from different validators, so the burst is likely upstream of both, in whatever the worker holds between jobs. `tests/test_client_workload_e2e.py::TestFileOrganizer` covers the single and paired cases and fails during a burst. Seen again 2026-08-17 after the worker-bootstrap fixes: both e2e cases failed `provider-output-invalid` back to back on a worker in its first minutes, on the same fixtures that passed elsewhere — consistent with the burst living in per-worker state rather than in the inputs. **Blocked 2026-08-20:** the burst is only observable while it is happening, and reproducing it needs the GPU with the shipped generation models mounted — which the documented install does not do until OMNI-0380 is answered. Static reading of `providers/vulkan-runtime/.../runtime.py` found no per-worker state surviving a job: `generate()` releases the model in its `finally`, so each request reloads. Nothing left to try from the source alone.
| OMNI-0382 | blocked | medium | s | `docs/choosing-a-model.md` recommends `qwen3-4b-q4-k-m` for `file-organizer` — it beats every 9B there, 10/10 against 8/10 — but no manifest declares it, so `set-profile-model` refuses the very choice the guide recommends with "Model is not declared by this workload". Declaring it in `plugin-manifests/file-organizer.json` is the fix if the measurement stands; withdrawing the recommendation is the alternative. Found by the 2026-08-17 documentation audit, which corrected what it could verify and left this because it needs a product decision rather than a prose edit. **Blocked 2026-08-20:** declaring the 4B or withdrawing the recommendation is the product decision the row names, and nothing here can make it.
| OMNI-0383 | blocked | medium | s | `scripts/collect-selected-text-acceptance.py` verifies `--qwen-model` against the live worker load receipt, and the documented invocation passes the 8B. That is internally consistent only while the documented install mounts the 8B alone: once OMNI-0380 lands and a worker runs the `qwen3-5-9b-iq4-xs` default, the documented collector command becomes refusable against a receipt naming the 9B. Take the model from the receipt, or require the operator to name the one the worker actually loaded. **Blocked 2026-08-20:** the collector command only becomes wrong once OMNI-0380 mounts the 9B, which is itself blocked on the upstream pin.
| OMNI-0374 | blocked | medium | m | `provider_id` is `qwen3-workloads-gpu` — a model's name inside a runtime identity, for a runtime that loads any GGUF. Renaming it is not free: `evaluation-corpora/selected-text-v1.json` carries `expectedProviderId` on all ten cases, and its sha256 (`2f574e01…`) is bound into `acceptance-evidence/selected-text-rx6600xt-evidence.json` as `corpusSha256`, so the corpus, the frozen evidence, `generation-models/qwen3-8b.json`, and the `document_acceptance.py:408` gate move together and the acceptance runs are re-executed. Decided 2026-08-17 to rename and redo the runs rather than keep a model name in a runtime identity. **Blocked 2026-08-20:** the rename is decided; carrying it out re-executes the acceptance runs on the GPU with the real models, which is a maintainer action on the host. One S step here (rename the identity across the corpus, catalogue, evidence and gate), one external step (re-run acceptance and re-record the digests).
| OMNI-0361 | blocked | medium | m | Qualify the pairs a person can actually choose. Done 2026-08-17: the four text workloads reissued against the uncapped tasks on `qwen3-8b-q4-k-m`, and `qwen3-5-9b-iq4-xs` added as the default with the weights installed digest-verified and declared in all four manifests. Remaining: `qwen3-4b-q4-k-m`, which beats every 9B on `file-organizer` (10/10 against 8/10) and is therefore worth offering for that workload, and `dictalm2-hebrew-q4-k-m` against the Hebrew path, which has never been measured through its own runtime — the benchmark drove the generic one and scored 0/10 in 1.2s, which is a harness defect rather than a verdict. Note the standing decision that a receipt records what is selectable rather than gating a person's choice: a model somebody selects is their responsibility. **Blocked 2026-08-20:** both remaining pairs need the GPU and the shipped models: `qwen3-4b-q4-k-m` on file-organizer, and `dictalm2-hebrew-q4-k-m` driven through its own runtime rather than the generic one. Two acceptance runs, no code.
| OMNI-0354 | blocked | medium | l | Five sources that plainly contain an event still come back refused, and **lengthening the prompt makes it dramatically worse**. Baseline on `qwen3-5-9b-iq4-xs`, sixteen cases, reproduced twice at exactly 8/16 and 61% of rules, 19.3s per case. The five refused are `event-written-month`, `event-two-in-one-document`, `event-time-without-date`, `event-online-meeting` and `event-cancelled`, and the model states its reasoning in `detail`: *"only a description of a recurring"* and *"The source text explicitly states that the event is cancelled"*. So it is applying its own notion of what counts as an event — a cancelled one is not one, a series without a first date is not one — rather than the contract's. **Measured attempt, reverted:** adding six clauses saying exactly that (a cancelled event is still an event, a series with no first date is an event whose date is unknown, several events yield several) took the instruction from 1,863 to 2,264 characters and the score from 8/16 to **2/16, rules 61% to 4%**, at 7.3s per case — it refused nearly everything, faster. More instruction is not the lever, and this is the second wrong approach eliminated by measurement after the grammar conditional. What has not been tried: shortening the instruction rather than extending it, since the two data points both point that way; a worked example in the prompt instead of more rules; and `event-two-in-one-document` specifically, where `at_least: 2` may be failing for a different reason than the refusal. Worse on 2026-08-17: the plainest case there is — two explicit dated, timed events in a three-line note — came back `no-event-found` / "Sources contain no event" three runs out of three on `qwen3-5-9b-iq4-xs`, with the task digest verified identical to the measured receipt, so the live model refuses even the baseline's passing shape now (`tests/test_client_workload_e2e.py::TestEventExtraction` reproduces it). **Blocked 2026-08-20:** every next step is a measurement: the sixteen cases have to be re-run on the GPU against a changed prompt to learn anything, and the models the documented install mounts are the ones OMNI-0380 is blocked on.
| OMNI-0344 | blocked | medium | s | The applet still mirrors contracts the runtime publishes. The premise of this row has moved: the Python client exists, depends on `omnitensor>=0.1,<1`, and vendors no schemas — that half is settled. What remains is the Cinnamon applet's hand-written JavaScript mirrors, and where the D-Bus method constants and the snapshot-path default live, which are still duplicated as prose in both repositories' contract tests. `d0a10c1` set the precedent for the remaining options: a second distribution built from this repository is now a known move, so a slim contracts package both consumers depend on is cheaper than it was when this row was written. **Blocked 2026-08-20:** which shape the shared contracts take - a slim package both distributions depend on, or the applet keeping its mirrors - is a decision spanning `cinnamon-xpuwlm`, a repository this ledger does not write to.

## Rejected / Won't fix

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0573 | rejected | medium | l | **Porting `../video-doc-transcriber` as a workload of its own.** Reviewed 2026-08-20 and not planned: `media-transcription` already accepts every input it does — audio, video, images, PDF, and presentations it does not — with pinned artifacts on the granted GPU. What that repository has and this one will not is how it reads images: Tesseract and PaddleOCR, both CPU-only inference, with PaddleOCR downloading its detection and recognition models into `~/.paddleocr` on first use. That is two rules at once — no CPU execution path anywhere, and every artifact pinned by digest before it is loaded — and OCR already has a plan that keeps both, OMNI-0506–OMNI-0509 on the Vulkan lane. What was worth taking is taken: OMNI-0571's language setting shipped, and `.docx` is OMNI-0572. |
| OMNI-0565 | rejected | critical | m | **Three of four workloads run a task nobody measured.** Rejected 2026-08-20 by the maintainer: the model in use is good enough, and qualification is not the axis worth maintaining while it may change again. The receipt stops being a claim about the task, so a digest that has moved is no longer a finding. What replaces it is behavioural: the workloads are pinned by contract gates that need no GPU (OMNI-0570), and the labelled corpus stays available for the day a model actually changes (OMNI-0568). |
| OMNI-0566 | rejected | high | s | **A person is never told their answer came from an unmeasured pair.** Rejected 2026-08-20: with qualification dropped as a claim, there is no measurement to report and nothing to label. `MeasuredEvidence` keeps its one true job — recording what loaded — and stops carrying a verdict nobody acts on. |
| OMNI-0569 | rejected | critical | m | **Making the receipt stay true without a separate act.** Rejected 2026-08-20: the three rules stay as they are because the thing they keep in step is no longer maintained. Recorded for the day a model changes: re-measuring one workload on the qualified card took 2 minutes 45 seconds, so cost was never the obstacle — the runner simply never wrote back what it measured. |
| OMNI-0501 | rejected | medium | m | **The shipped qualification receipts are stale.** Rejected 2026-08-20: the receipts are no longer a claim about the task, so a stale one is not a defect. The six red tests this row explained are gone with the binding they asserted (OMNI-0570); the receipt keeps only what it can still be right about — the device and layer count a load reported. |
| OMNI-0523 | rejected | high | s | **`document-translation` qualification.** Rejected 2026-08-20 with the rest of the qualification work: there is no longer a measured/unmeasured distinction for an answer to carry. |
| OMNI-0349 | rejected | low | xs | Architecture review non-findings, kept so later audits do not re-derive them. **Trainer duplication is false:** the five trainer CLIs are two-line wrappers over one parameterized path (`training_train_cli.py` `dispatch_trainer` + `TRAINER_DISPATCH`). **Validation is single-engine for the canonical contracts:** `registry.validate_document` is the only validator of anything in `schemas/`; `snapshot.py:114` and `contract.py:93` both delegate to it. Schemas a plugin declares at runtime (a task's output schema, a workload's configuration) are necessarily validated where they are declared — `plugins/generation.py`, `plugins/settings.py`, `acceptance_checks.py` — which is a different contract, not a second engine for the same one. **Providers are correctly separated:** four of six are five-line identity shims over the shared `omnitensor-vulkan-runtime` dependency, with zero copy-pasted files by digest comparison. **Mutation tooling stays per-language:** 176 lines of JS around Stryker and 557 of Python around mutmut wrap engines that cannot be shared; only the selector-file concept overlaps, and a shared format would couple the repositories for no behavioural gain. |
| OMNI-0334 | wont_fix | medium | xs | Sonar reports missing lock files for the seven separately published provider and external-template library projects. Retain their ranged package metadata: Python library locks are not consumed when downstream applications install wheels, and each deployment environment must resolve those ranges into its own lock. The service and repository CI now use the root `uv.lock`; mark the seven library findings won't-fix after the next scan. |
| OMNI-0337 | wont_fix | medium | xs | Retain the reviewed caller-selected paths behind the remaining Sonar S8705/S8707 reports: mutation manifests and source roots plus media-fixture targets/tool discovery are explicit offline developer CLI inputs; `atomicio` is the generic path-owning persistence boundary; `file_digest` reads operator-selected artifacts; and privileged BPF helpers now receive only installed constants from `main` while tests inject temporary paths. None is reachable from a model, plug-in payload, or service request; mark any surviving reports false-positive after the next scan. |
| OMNI-0338 | wont_fix | medium | xs | Retain the reviewed semantic hotspots: the reported `tokenizer.json` value is a public SHA-256 digest, bidi controls are the sanitization denylist and its regression fixture, sandbox `/tmp` is a new private tmpfs inside the worker namespace, and `random.Random(seed)` builds a reproducible bounded conformance corpus rather than a secret. Mark those six findings false-positive. Keep the BPF socket-mode finding open under OMNI-0247 until group/ACL/socket-activation ownership is chosen. |

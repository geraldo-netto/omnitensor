# TODO

## Open

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0635 | open | medium | s | Restore agreement between the shipped generation catalog and its loader so the catalog loads and event artifact verification can run; the loader currently rejects the published provider lanes as incomplete. |

## Blocked / Deferred

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0630 | blocked | medium | s | Wire `ocr_extraction.py` into `visual-library` indexing so exact OCR text and coordinates become searchable alongside embeddings. Blocked until the workload's models, inference, and acceptance tracked by OMNI-0097, OMNI-0098, and OMNI-0101 exist. |
| OMNI-0629 | blocked | medium | s | **document-translation ships unmeasured — closes with the operations core.** Decided path (2026-08-21): stage 3 retires document-translation, and translate is measured under the file side. The remainder is a live GPU acceptance run of `ask-selected-files` with installed artifacts — the stage-2 prompt moved its task digest, so both Qwen pairs and the new translate metrics (span-coverage, target-script-integrity, provider-route-integrity) sit `unmeasured` in `qualification.json` until that run happens. |
| OMNI-0613 | blocked | medium | s | Decide whether `media-transcription` keeps Qwen3.5-9B as its general default or returns to 7B and reserves 9B for the OCR lane. Either choice changes fresh-install behavior. |
| OMNI-0575 | blocked | high | m | Decide whether each collector may persist training samples for its own profile. Existing unattended snapshot recording exposes only scheduler aggregates; profile-specific persistence would create a history of personal hardware activity and requires an explicit privacy decision. |
| OMNI-0574 | blocked | high | m | Decide whether six advisory workloads must wait for qualified models and licensed labeled corpora or may answer from their existing permission-gated measurements using explicit deterministic arithmetic. This changes the definition of an advisory workload. |
| OMNI-0552 | blocked | medium | l | Delete path arguments and `or ...` defaults from `OmniTensorService.__init__`, and move all fourteen construction sites to `composition.build_service` in one atomic commit. Splitting the switch would leave callers broken. |
| OMNI-0559 | blocked | medium | s | Decide how `describe-contract` advertises accepted `apply-command` operations without making older clients reject the handshake: make `runtime-contract` forward-compatible or announce operations through an existing open document. Derive the list from the command schema enum. |
| OMNI-0478 | blocked | high | l | Stop idle snapshot ticks and heartbeat rewrites so an unused runtime consumes no resources. Blocked until OMNI-0479 decides how readers distinguish healthy idle state from a dead runtime without snapshot age. |
| OMNI-0518 | blocked | medium | s | Replace the model-specific `hebrew.py` provider with a translation task on the generic adapter. Blocked on a GPU measurement because grammar-constrained JSON and grounding metadata may degrade DictaLM's Hebrew output without producing an explicit error. |
| OMNI-0554 | blocked | high | m | Decide whether alerts require acknowledgement through a new `resolve-alert` command and client surface or expire through a count/age retention rule. `SummaryStore.resolve` has no production caller and `_summaries` has no eviction, so unresolved alerts accumulate indefinitely. |
| OMNI-0499 | blocked | low | s | **Is per-request unload the intended cross-worker GPU-lease policy?** `providers/vulkan-runtime/.../runtime.py:226-228` — `generate()`'s `finally: ... self._release()` unloads the GGUF and drops the lease after every request, so the next request re-reads and re-offloads a multi-GB model and the cached `_load_report` fast path is almost never hit. Deliberate, so another worker can take the GPU between requests, or an accident? Maintainer's call. |
| OMNI-0402 | blocked | medium | xs | Decide whether SDK and smoke failures may include `str(error)` for plugin authors or must suppress arbitrary exception text and rely on logs. Existing privacy tests require type-only messages, while those messages hinder debugging. |
| OMNI-0479 | blocked | high | m | **Whether an idle runtime may stop publishing at all, which the staleness contract currently forbids.** OMNI-0478 wants no heartbeat when nothing is happening. Readers disagree by contract: `cinnamon-xpuwlm` treats a snapshot older than `STALE_AFTER_MS` (15 000) as stale, and `xpuwlm/src/xpuwlm/domain/snapshot/staleness.py` does the same, so a runtime that stops publishing while idle is indistinguishable from one that died — the panel would show "stale" on a perfectly healthy idle desktop. **Decision needed, and it is the maintainer's:** (a) readers stop inferring liveness from document age and learn it another way (the control socket answering, or a systemd unit state), which is a change in all three repositories; or (b) an idle runtime retracts the snapshot outright, so readers observe honest absence rather than staleness — the same path OMNI-0011 already built for zero devices; or (c) the heartbeat stays and "costs nothing when idle" is accepted as one write per 10 s. Nothing can be implemented for OMNI-0478's publisher half until this is answered. |
| OMNI-0297 | blocked | high | xl | Run and archive a repository-complete mutation campaign with every callable at or above 80%. Blocked until OMNI-0296 narrows scope, OMNI-0247 settles socket access, OMNI-0258 supplies portable/native parity runners, and OMNI-0259 supplies the producer-request and holdout contract. |
| OMNI-0048 | blocked | high | s | Produce and publish the `low-light-enhancement` 0.2.0 Retinexformer ncnn artifact with exact graph/weights digests, tensor/output contracts, publisher identity, portable/native parity, and hardware Vulkan proof. — blocked: requires a licensed digest-pinned source/checkpoint, an authentic paired holdout, a concrete Retinex source with portable/native producer and runner, signed ncnn artifacts, and a no-CPU Vulkan execution report. |
| OMNI-0104 | blocked | medium | s | Connect the 0.2.0 enhanced-image pipeline to the existing disjoint-folder, atomic no-overwrite publisher; preserve allowed metadata, expose progress/result evidence, and clean only partial outputs on cancellation or failure. — blocked: requires product decisions for output codec and naming, a conservative metadata/privacy allowlist, and the job/integration surface, plus a real OMNI-0048 artifact for closure. |
| OMNI-0105 | blocked | high | s | Freeze an authentic, license-reviewed paired low/normal-light holdout and record 0.2.0 portable/native SSIM, paired-target SSIM, median CIEDE2000, noise, color, range, and artifact measurements with test-only corpus bytes and source provenance. — blocked: requires the authentic paired corpus, the missing CIEDE2000/noise/color/artifact metrics, and OMNI-0048 native evidence; the existing synthetic CC0 corpus is test coverage, not fidelity evidence. |
| OMNI-0258 | blocked | high | m | Wire a `promote_main` console entry point for the five numeric trainers so a trained domain fixture can be promoted into a schema-valid binding; retain the separate forecast report path. — blocked: choose and implement a bounded parity fixture plus concrete portable ONNX and native Vulkan/OpenVINO-NPU runners; reports alone cannot prove parity, so a fake CLI is not an implementation. |
| OMNI-0259 | blocked | high | m | After production-pipeline consolidation, add one recipe-dispatched `omnitensor-produce-source` CLI that reaches embedding, CLIP, Retinexformer, and foundation-forecast producers. — blocked: choose and version a producer-request/holdout contract and concrete reviewed adapters for text, CLIP, Retinex, and forecast; a fake-injected CLI is unusable. |
| OMNI-0247 | blocked | medium | s | Harden BPF helper IPC and bound client handling. — blocked: an `AF_UNIX` stream client needs write permission on the socket path, so root-owned mode `0644` would deny every unprivileged collector. Choose a dedicated group with `0660`, a scoped ACL, or socket activation/ownership before changing access; client timeouts remain pending that decision. |
| OMNI-0085 | blocked | high | s | `hardware-health` model: define labeled fault/normal evaluation data and metrics, qualify a real accelerator-compatible anomaly model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys` — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0086 | blocked | high | s | `hardware-health` pipeline: implement feature windows, preprocessing, anomaly inference, calibrated scoring, evidence extraction, and bounded alerts while keeping firmware/shutdown/repair actions deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0087 | blocked | high | s | `hardware-health` acceptance: record fault-injection recall/false-positive, privacy, missing-sensor, latency, and named-hardware evidence before marking the plugin operational. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0088 | blocked | high | s | `storage-intelligence` model: define labeled trend/anomaly data and metrics, qualify a real accelerator-compatible model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys` — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0089 | blocked | high | s | `storage-intelligence` pipeline: implement feature windows, inference, calibrated trend/anomaly results, confidence/evidence, and cache/tiering/maintenance advice without destructive actions. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0090 | blocked | high | s | `storage-intelligence` acceptance: record replay accuracy, device-loss recovery, privacy, false-positive, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0091 | blocked | high | s | `resource-scheduler` model: define workload traces, target decisions, fairness/utility metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys` — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0092 | blocked | high | s | `resource-scheduler` pipeline: implement preprocessing and placement/timing inference, expose bounded recommendations, and keep enforcement, starvation prevention, and safety limits deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0093 | blocked | high | s | `resource-scheduler` acceptance: record utility, starvation/fairness, policy-change, overload, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0094 | blocked | high | s | `build-advisor` model: define a provenance-safe labeled corpus and ranking/risk metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys` — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0095 | blocked | high | s | `build-advisor` pipeline: implement feature extraction, inference, calibrated regression-risk/check ranking, and explainable advice without silently omitting mandatory checks. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0096 | blocked | high | s | `build-advisor` acceptance: record corpus quality, ranking/risk accuracy, adversarial-path, privacy, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0097 | blocked | high | s | `visual-library` models: define labeled classification/retrieval evaluation data and metrics, qualify real accelerator-compatible classifier/embedding models, and publish verified artifacts and tensor contracts. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys` — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0098 | blocked | high | s | `visual-library` inference: implement deterministic resize/normalization, classification/embedding inference, calibrated tags, batching, and bounded failure handling. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0101 | blocked | high | s | `visual-library` acceptance: record malformed-media isolation, index recovery, classification/retrieval quality, privacy, throughput, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0106 | blocked | high | s | Record 0.2.0 performance on the named RX 6600 XT: hardware Vulkan execution with zero CPU fallback, warm p95 ≤50 ms, and ≥1.2 measured end-to-end speedup over the documented CPU enhancement baseline. Blocked until the artifact, pipeline, and fidelity corpus exist. |
| OMNI-0108 | blocked | high | s | `network-peripherals` model: define privacy-reviewed anomaly data and metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys` — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0109 | blocked | high | s | `network-peripherals` pipeline: aggregate bounded windows, preprocess, infer and calibrate anomalies, correlate sources, and extract evidence without per-packet inference. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0110 | blocked | high | s | `network-peripherals` output: publish bounded alerts/recommendations and permit only explicitly granted, allowlisted, reversible responses outside model control. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0111 | blocked | high | s | `network-peripherals` acceptance: record privacy, hotplug/replay recovery, false-positive/recall, restricted-response, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0112 | blocked | high | s | `desktop-context` model: define consented redacted evaluation data and suggestion-quality metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus. GPU inference is available (Vulkan/ncnn) and the publisher identity exists — `load_or_create_publisher_identity()` answers `xpuwlm.local` from the Ed25519 pair in `~/.local/share/omnitensor/publisher-keys` — so neither hardware nor signing is the blocker. The labelled data is, and nothing here may invent it: a model qualified against a corpus somebody made up measures the fiction rather than the machine. |
| OMNI-0113 | blocked | high | s | `desktop-context` pipeline: preprocess context, infer calibrated context/layout suggestions, suppress stale-session results, and attach bounded explanations without capturing content. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0114 | blocked | high | s | `desktop-context` consumer: deliver only allowlisted suggestions to Cinnamon and require explicit user confirmation before any window-manager action; never manipulate windows silently. — blocked: needs coordinated work in the Cinnamon applet repository, which this repository cannot change. |
| OMNI-0115 | blocked | high | s | `desktop-context` acceptance: record consent/revocation, privacy/redaction, session churn, suggestion quality, user-action safety, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0122 | blocked | high | s | system acceptance provisioning: install, verify, configure, consent, and smoke-test every bundled plugin and artifact with representative real input and user-visible output. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0123 | blocked | high | s | system acceptance resilience: exercise device/source loss and recovery, worker crash, backpressure, cancellation, service/Cinnamon restart, and in-flight cleanup across representative plugins. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0124 | blocked | high | s | system acceptance lifecycle: verify plugin/artifact/service upgrade, configuration migration, rollback, revocation, cache cleanup, and offline startup without losing valid prior state. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0125 | blocked | high | s | system acceptance hardware: run the reproducible TPU/NPU/GPU matrix, archive evidence, and prevent any profile from claiming operational readiness until its own model, pipeline, safety, and acceptance gates pass. — blocked: the GPU lane is available (Vulkan/ncnn) but no TPU or NPU device is present, so the full matrix cannot be run. Document Intelligence's named-GPU acceptance is complete; the remaining profile rows and absent TPU/NPU hardware still block the system-wide matrix. |
| OMNI-0184 | blocked | medium | s | the eBPF helper's privileged path is unverified: the loader, map reads, and socket service were written and compile-checked, but nothing on this host can run them — loading needs CAP_BPF and `bpftool prog load` fails EPERM as this user, so `load_probes`/`read_histogram` have never executed against a real kernel; verify on a host where the helper can be installed as root before relying on the data. |
| OMNI-0198 | blocked | medium | l | TPU lane parity — produce and accept an Edge TPU artifact: the fail-closed compiler adapter now exists, but every recipe still needs a representative-calibrated fully-int8 TFLite export, the externally distributed `edgetpu_compiler`, 100% Edge TPU operation mapping, portable/native semantic parity, and named Coral execution evidence. Decide whether releases use a pinned compiler container or trusted precompiled artifacts. This host has neither the compiler nor a Coral device, so production and execution remain unverified. |
| OMNI-0235 | blocked | medium | l | Coral event-extraction alternative: Qwen2.5-VL is not a realistic Edge TPU artifact, so evaluate a small fully-int8 candidate-line/token classifier combined with deterministic calendar parsing under the same event result contract. Qualify it only with representative calibration, full compiler mapping, multilingual grounded-event quality, parity of safety/refusal semantics, and named Coral evidence; blocked here on a selected model/corpus, external compiler, and hardware. |
| OMNI-0289 | blocked | low | xs | Decide whether generation intentionally excludes TPU despite the global TPU>NPU>GPU preference. Until the product decision is made, document the absent lane at `GenerationRouter` and do not change routing. |
| OMNI-0296 | blocked | medium | s | Narrow `mutation-selectors.json` to parser and validator boundaries such as `registry`, `contract`, `snapshot`, `tensorcontract`, `outputcontract`, and `job_codec`; drop orchestration-only shards with rationale. Blocked because mutation campaigns are maintainer-run. Remove stale ignored `mutants/` state before the next run. |
| OMNI-0343 | blocked | medium | m | After OMNI-0296, run and archive the narrowed non-incremental mutation baseline, then raise every selected callable to 80% or justify equivalent survivors. Blocked because mutation campaigns are maintainer-run. |
| OMNI-0353 | blocked | high | m | Reproduce and diagnose intermittent `file-organizer` `plan-invalid`, `provider-output-invalid`, and `evidence-invalid` bursts covered by `TestFileOrganizer`. Requires a GPU with shipped generation models; static inspection found no per-worker state surviving `generate()`. |
| OMNI-0382 | blocked | medium | s | Decide whether to declare `qwen3-4b-q4-k-m` in `file-organizer` or withdraw its recommendation from `docs/choosing-a-model.md`. Current guidance recommends a model that `set-profile-model` refuses as undeclared. |
| OMNI-0374 | blocked | medium | m | Rename generic provider identity `qwen3-workloads-gpu` across corpus, catalogue, evidence, and acceptance gate, then rerun acceptance and record new digests. Blocked on GPU access with real models because evidence binds the provider ID and corpus digest. |
| OMNI-0361 | blocked | medium | m | Qualify `qwen3-4b-q4-k-m` for `file-organizer` and `dictalm2-hebrew-q4-k-m` through its own Hebrew runtime. Both acceptance runs require a GPU and shipped model artifacts. |
| OMNI-0354 | blocked | medium | l | Improve event extraction for explicit multi-event, recurring, undated, online, and cancelled cases. Test shorter instructions, a worked example, and the multi-event cardinality separately; longer prompts degraded results. Blocked on rerunning the corpus with Qwen3.5-9B on a GPU with installed model artifacts. |
| OMNI-0344 | blocked | medium | s | Decide whether Cinnamon applet contract mirrors remain hand-written or move into a slim shared contracts package. D-Bus method constants and snapshot-path defaults are duplicated across repository tests, so this requires cross-repository coordination. |

## Rejected / Won't fix

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| OMNI-0640 | rejected | low | xs | False positive: overlapping scan ranges printed the snapshot-forecast explanation line twice; the source and history contain it once. |
| OMNI-0509 | wont_fix | medium | s | Do not gate jobs on OCR qualification: OMNI-0609 defines OCR as best-effort, so a gate would contradict lane semantics. Evidence remains in `scripts/measure-ocr-enrichment.py`, VulkanOCR corpus results, dispatcher tests, and OMNI-0624 sandbox verification. |
| OMNI-0508 | wont_fix | medium | s | Do not add a standalone OCR workload manifest. OCR is an enrichment lane inside `media-transcription` and `ask-selected-files`; another manifest would duplicate their artifacts, contracts, and permissions. |
| OMNI-0603 | rejected | low | xs | **The cleared wake is subsumed by the tick that follows.** `publish_loop.py:192-210`: a `request_publish` landing in `await_next`'s finally-gather window is indeed cleared without being consumed — but every return from `await_next` is followed by a full tick (`run:141-157`) that re-observes the mutated state and publishes it, and `request_publish` is documented to be called after the state change. No interleaving loses a change or delays it beyond the tick already underway; clearing there is what folds a burst of wakes into one rebuild. |
| OMNI-0573 | rejected | medium | l | Do not port `../video-doc-transcriber` as another workload: `media-transcription` covers its supported inputs with pinned GPU artifacts, while its Tesseract/PaddleOCR path violates no-CPU-backend and digest-pinned-artifact rules. |
| OMNI-0565 | rejected | critical | m | Do not maintain task-qualification receipts for current generation workloads. Behavioral contract gates pin required behavior without GPU runs; labeled corpora remain for future model changes. |
| OMNI-0566 | rejected | high | s | Do not label answers as measured or unmeasured because qualification is not maintained as a product claim. `MeasuredEvidence` records what loaded, not a verdict. |
| OMNI-0569 | rejected | critical | m | Do not add automatic receipt synchronization for qualification data that is no longer maintained as a product claim. |
| OMNI-0501 | rejected | medium | m | Do not treat stale qualification receipts as defects because receipts no longer claim task quality; retain only device and layer-count load evidence. |
| OMNI-0523 | rejected | high | s | Do not add separate `document-translation` qualification because answers no longer carry a measured/unmeasured distinction. |
| OMNI-0349 | rejected | low | xs | Reject architecture findings for trainer duplication, duplicate canonical-schema validation, provider duplication, and shared mutation tooling: trainers share `TRAINER_DISPATCH`, canonical schemas share `registry.validate_document`, provider shims share one runtime, and Python/JavaScript mutation engines require separate tooling. |
| OMNI-0334 | wont_fix | medium | xs | Treat missing lock files for separately published provider and template libraries as won't-fix: downstream wheel installations do not consume library locks, while service CI uses root `uv.lock`. |
| OMNI-0337 | wont_fix | medium | xs | Treat Sonar S8705/S8707 reports for reviewed operator-selected offline CLI paths and generic persistence boundaries as false-positive; none is reachable from model, plugin payload, or service request. |
| OMNI-0338 | wont_fix | medium | xs | Treat reports for public SHA-256 digests, bidi sanitization fixtures, sandbox-private `/tmp`, and seeded conformance randomness as false-positive. BPF socket access remains live under OMNI-0247. |

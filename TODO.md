# TODO

## Work groups

Items remain in the canonical lifecycle tables below; these ranges preserve the
audit's dependency order without weakening the required status schema.

- **Low-light delivery:** OMNI-0048, OMNI-0104–OMNI-0106
- **Profile model, pipeline, and acceptance:** OMNI-0085–OMNI-0098, OMNI-0101, OMNI-0108–OMNI-0115
- **System acceptance:** OMNI-0122–OMNI-0125
- **Privileged telemetry:** OMNI-0184, OMNI-0247
- **TPU lane:** OMNI-0198, OMNI-0235, OMNI-0289
- **Production pipeline consolidation:** OMNI-0258, OMNI-0259
- **Mutation gates:** OMNI-0296, OMNI-0343, OMNI-0297
- **Project split support:** OMNI-0344, OMNI-0348
- **New workloads:** OMNI-0351
- **Client workload defects (found end to end):** OMNI-0352–OMNI-0354
- **Workload tuning:** OMNI-0355, OMNI-0356
- **Runtime/client boundary:** OMNI-0357, OMNI-0358 (client half: xpuwlm D1–D2)
- **Architecture hygiene:** OMNI-0345–OMNI-0347
- **Ledger hygiene:** OMNI-0342
- **SonarCloud remediation:** OMNI-0334, OMNI-0337, OMNI-0338

## Findings

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| OMNI-0296 | open | medium | s | OMNI-0343 | Narrow `mutation-selectors.json` to parser and validator boundaries before spending another run. The recorded baseline (`mutation-baselines/2026-08-14.json`, 3,584 mutants, 92.0%) was taken at `88d9294` and 26 `src/` modules have changed since, so its per-callable figures no longer describe HEAD — but the applet's equivalent campaign showed where the spend pays: it measured 46 targets, raised ten above the threshold, and found no defect, with the value concentrated where a silently wrong value escapes (a date pattern, a digest, octet arithmetic) and ceremony over orchestration code the suite already asserts. So the re-scope is the work: keep `registry`, `contract`, `snapshot`, `tensorcontract`, `outputcontract`, `job_codec`-class selectors, drop orchestration-only ones, and record the rationale per dropped shard. One practical trap for the eventual run: `mutants/` is a gitignored working copy mutmut reuses, and a stale one fails every shard with "could not find any test case for any mutant" — remove it first. |
| OMNI-0343 | open | medium | m | OMNI-0296 | After OMNI-0296 lands, run the narrowed non-incremental baseline, archive it beside `mutation-baselines/2026-08-14.json` with the commit it describes, and burn any callable under 80% down or record its survivors as equivalent with domain reasoning — no regressions for logging-shape or prose-only mutants. The fail-fast shard runner stops at the first failure by design; a baseline sweep drives each shard once and continues, which is what the applet's rebaseline did. |
| OMNI-0351 | open | medium | l | OMNI-0344 | Integrate an OCR workload on the Vulkan lane by porting PaddleOCR models to ncnn, following the codexa-project evaluation (2026-08-15, checked against the live repos): Paddle has no Vulkan backend — `paddle/phi/backends` holds `cpu, custom, dynload, gpu, onednn, xpu` as of PaddleOCR v3.7.0 / Paddle v3.3.0 / Paddle-Lite v2.14-rc — so running PaddleOCR under this service's no-CPU-fallback rule needs a different runtime, and ncnn is the mature answer: a genuine Vulkan backend (`src/gpu.cpp`, `pipeline.cpp`, `command.cpp`), on PyPI (1.0.20260526), with existing PaddleOCR model ports, and already this service's GPU engine (`executors/vulkan.py` `VulkanGpuExecutor`), so the executor, confinement, and artifact machinery are reused rather than added. The rejected routes, recorded so they are not re-derived: Paddle's out-of-tree `backends/custom` plugin seam exists (`get_all_custom_device_type()` returns `[]` here) but means writing the kernels ourselves. Work shape: pick the detection+recognition model pair, obtain or build the ncnn ports with pinned sha256 artifacts, add a workload manifest with the tensor contracts, and gate acceptance on the GPU lane like every other profile. |
| OMNI-0345 | open | medium | s | — | Delete the `legacy_acceptance_value` indirection: 32 call sites (21 in `acceptance_cli.py`, 8 in `acceptance_checks.py`, 3 in `acceptance_probes.py`) resolve every collaborator through `sys.modules["omnitensor.acceptance"]` at run time solely so two test files can monkeypatch the facade (`test_acceptance_architecture.py:185-306`, `test_acceptance_install.py:398-430`). Retarget those `setattr`s at the owner modules, drop the indirection and the `__module__` rewriting (`acceptance_contracts.py:60-61`, and the loops at the foot of `acceptance_probes.py`), and keep `acceptance.py` as a plain re-export for the `omnitensor-verify-install` entry point. |
| OMNI-0346 | open | medium | m | — | Slim `OmniTensorService` to composition and lifecycle. The 663-line class still owns four concerns beyond them: admission policy (`_admits`/`_weight_of`/`_job_authorized`, `service.py:298-312`), executor-lane selection (`_scheduler_lane`/`_device_identity`/`_executor_for_device`, `:384-406`), and artifact resolution with its cache (`_resolve_artifact`/`_cached_resolution`/`_artifact_stamp`/`_declared_reference`, `:516-566`). Composition is already extracted (`composition.py`, `host.py`) and the snapshot body is delegated, so the pattern exists — extract the artifact-resolution cache and lane selection into their own modules and leave the async publish/lifecycle loops (`:619-803`) where they are. The plugin admission pool added in `104f354` is already its own module (`plugin_admission.py`); the service only wires and kicks it. |
| OMNI-0347 | open | low | s | — | Drop the alias re-export blocks that make `plugins/supervisor.py` and `plugins/loading.py` half facade, half implementation: `supervisor.py:14-52` re-exports about thirty ipc/protocol/diagnostics names (`import math as math`, `HandshakeOffer as HandshakeOffer`) and `loading.py:6-13` does the same for stdlib modules, while both files also hold their real classes. Only two external consumers use the aliases (`tests/test_jobs.py:627`, `tests/test_loading_architecture.py:25`) — retarget those two imports and delete the blocks. The underlying module split is clean and stays. |
| OMNI-0348 | open | medium | l | OMNI-0258, OMNI-0259 | Split `training/` out of the installed service: 9,564 lines and 15 of the 31 console scripts ship in the service wheel although nothing in the serving runtime imports them — the reverse dependency is one-directional and touches only leaf utilities (`atomicio`, `registry.validate_document`, `preparation`, `telemetry_types`, `executors.vulkan`). Move it to a separate `omnitensor-training` distribution or an optional extra, whichever keeps the recipe files co-versioned with the schemas they validate against; the desktop install then stops carrying trainer CLIs. OMNI-0258/0259 land in whichever package results, so decide this first. |
| OMNI-0344 | open | high | s | OMNI-0342 | Decide how the planned Python client (applet ledger XTPU-0193–XTPU-0205) consumes the canonical contracts, because this repository already solves the hard half: `schemas/` ships inside the wheel (`pyproject.toml` maps `omnitensor/schemas`) and `registry.py` loads and validates against the packaged copies. The options are a dependency on `omnitensor` itself, a slim extracted contracts package both depend on, or vendoring with a parity gate as the applet does today. The first two make the applet's ~2,727 lines of hand-written contract mirrors deletable rather than portable and shrink the cross-repository parity gates to one direction; vendoring keeps the drift class the gates exist to police. Whichever is chosen also decides where the client's D-Bus method constants and snapshot-path default live, which are today duplicated as prose in both repositories' contract tests. |
| OMNI-0352 | done | high | m | OMNI-0344 | `ask-selected-files` was rejected `worker-unavailable` on every submission because its worker never loaded: `00a98e8` raised the task's output budget from 1,024 to 2,048 tokens, which moved the task digest, and the qualification receipt still named the old one — so the provider refused the task, the supervisor exhausted the worker, and nothing logged it, because a worker that never starts has nowhere to log. Fixed in `db5ac23` by re-qualifying against the task that actually runs, and guarded in `d19d96f` by a test comparing the shipped receipt with the current tasks, which is the check whose absence let a task change kill a workload silently. Verified on the RX 6600 XT through the client's own window: a grounded answer with a citation, over one document, a longer one, and two at once. |
| OMNI-0353 | open | high | m | — | `file-organizer` fails in bursts, on inputs that succeed minutes later. Measured: one burst of four consecutive single-source failures (`plan-invalid`, then `provider-output-invalid` on a retry) where the same file submitted alongside a second one succeeded; a later burst of two-source failures (`evidence-invalid`) inside a test run while the same pair succeeded standalone; and then eleven consecutive successes, single and paired, over the following ten minutes with no change to the inputs or the service. So the defect is not the single-source case it first looked like — it is a worker that intermittently produces a plan its own contract refuses, and recovers on its own. Worth catching in that state: `plan-invalid` and `evidence-invalid` come from different validators, so the burst is likely upstream of both, in whatever the worker holds between jobs. `tests/test_client_workload_e2e.py::TestFileOrganizer` covers the single and paired cases and fails during a burst. |
| OMNI-0354 | open | medium | l | — | `event-extraction` refuses documents that meet its stated bar. A markdown file reading "Project review with Ana on 2026-09-03 at 14:00 in Lisbon" comes back `outcome: refused`, `code: no-event-supported`, "No named activity with explicit date and time found in the provided evidence" — a named activity, an explicit date, an explicit time and a location. It also fails outright as `evidence-invalid` on some documents in the same run where it refuses others, so the refusal is not stable either. The round trip is correct and the `.ics` adapter is wired, which leaves the model refusing its own contract; this is the same finding the client's ledger carries as F3, now reproducible from a test rather than by clicking. `tests/test_client_workload_e2e.py::TestEventExtraction` fails on it. |
| OMNI-0355 | open | medium | l | OMNI-0344 | Let a person tune a workload without breaking its qualification. The constraint that shapes this: the task — prompt, schema, token budget — is digest-bound to the qualification receipt, so a user-editable prompt cannot *be* the task; a worker whose task does not match its receipt refuses to start, which is exactly the failure OMNI-0352 was. So tuning is an *addition* the qualified task carries, never a replacement: `PluginContext.configuration` already exists, `plugins/settings.py` is already revisioned schema-validated configuration persistence, and each manifest already declares a `configuration` schema (empty today). Missing: a control command to set a workload's configuration, service wiring to pass it to the worker and restart that worker when it changes, providers reading it, and manifests declaring the tunables. The client end is nearly free — `xpuwlm/ui/workload_panels.py` has carried a translator for a non-empty `configurationSchema` since it was written, unused because no plugin publishes one. Start with two tunables that need no new qualification: extra answer guidance appended as private user instruction (never as system rules — the grounding and citation rules stay authoritative), and answer length. |
| OMNI-0356 | open | medium | l | OMNI-0355 | Let a person choose the model a workload runs, trading accuracy for speed. Three of the installed artifacts are Qwen3 at different sizes (`qwen3-8b-q4-k-m`, `qwen3-4b-q4-k-m`, `qwen3-0-6b-q8-0`) and only the 8B is qualified — the receipt binds one `modelId` per workload, and the provider hard-wires `QWEN_ARTIFACT_ID`, so a 4B answer today is a worker that refuses to start rather than a faster answer. The work is therefore mostly qualification, not plumbing: each (model, workload) pair needs a real GPU run recording its fully-offloaded layer count, context and cache shapes, and a passing result, exactly as the 8B has. Then the manifest declares which artifacts are selectable, the provider takes the chosen one, and the selection rides on OMNI-0355's configuration. Relevant beyond `ask-selected-files`: `selected-text-tools`, `file-organizer` and `event-extraction` all run the same shared Qwen runtime, so one qualification pass per model serves all four. `media-transcription` is separate — Whisper has one installed size — and the picture workloads pin their own tensor contracts. **Decide first** whether the smaller models are worth the qualification runs, since that is the bulk of the cost. |
| OMNI-0342 | open | low | s | OMNI-0297 | Decide what `related ids` means once a referenced row is removed, then repair the ledger. 34 of 47 rows cite at least one ID that no longer has a row; the prerequisite cluster OMNI-0036, OMNI-0038, OMNI-0082 alone is cited by 23 of them, and OMNI-0103 by two. Either restore the prerequisite rows those dependencies point at, or rewrite each dependency as prose in the description. The two rows whose entire dependency list was dangling — OMNI-0297 and the mutation baseline OMNI-0296 — were repaired in place; the rest need the policy decision first. |

## Blocked

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| OMNI-0297 | blocked | high | xl | OMNI-0247, OMNI-0258, OMNI-0259, OMNI-0296 | Run and archive a repository-complete mutation campaign with every callable at or above 80%. — blocked: first finish OMNI-0296, choose and implement OMNI-0247's socket-access policy, OMNI-0258's bounded parity fixture and real portable/native runners, and OMNI-0259's versioned producer-request/holdout contract and reviewed adapters; then define complete core, provider, and template mutation scope and archive per-shard plus consolidated commit/version/selector evidence. The former 54-entry dependency list was pruned to the four rows that still exist and that the prose actually names; see OMNI-0342. |
| OMNI-0048 | blocked | high | s | OMNI-0036 | Produce and publish the `low-light-enhancement` 0.2.0 Retinexformer ncnn artifact with exact graph/weights digests, tensor/output contracts, publisher identity, portable/native parity, and hardware Vulkan proof. — blocked: requires a licensed digest-pinned source/checkpoint, an authentic paired holdout, a concrete Retinex source with portable/native producer and runner, signed ncnn artifacts, and a no-CPU Vulkan execution report. |
| OMNI-0104 | blocked | medium | s | OMNI-0103 | Connect the 0.2.0 enhanced-image pipeline to the existing disjoint-folder, atomic no-overwrite publisher; preserve allowed metadata, expose progress/result evidence, and clean only partial outputs on cancellation or failure. — blocked: requires product decisions for output codec and naming, a conservative metadata/privacy allowlist, and the job/integration surface, plus a real OMNI-0048 artifact for closure. |
| OMNI-0105 | blocked | high | s | OMNI-0082, OMNI-0103 | Freeze an authentic, license-reviewed paired low/normal-light holdout and record 0.2.0 portable/native SSIM, paired-target SSIM, median CIEDE2000, noise, color, range, and artifact measurements with test-only corpus bytes and source provenance. — blocked: requires the authentic paired corpus, the missing CIEDE2000/noise/color/artifact metrics, and OMNI-0048 native evidence; the existing synthetic CC0 corpus is test coverage, not fidelity evidence. |
| OMNI-0258 | blocked | high | m | OMNI-0253 | Wire a `promote_main` console entry point for the five numeric trainers so a trained domain fixture can be promoted into a schema-valid binding; retain the separate forecast report path. — blocked: choose and implement a bounded parity fixture plus concrete portable ONNX and native Vulkan/OpenVINO-NPU runners; reports alone cannot prove parity, so a fake CLI is not an implementation. |
| OMNI-0259 | blocked | high | m | OMNI-0261 | After production-pipeline consolidation, add one recipe-dispatched `omnitensor-produce-source` CLI that reaches embedding, CLIP, Retinexformer, and foundation-forecast producers. — blocked: choose and version a producer-request/holdout contract and concrete reviewed adapters for text, CLIP, Retinex, and forecast; a fake-injected CLI is unusable. |
| OMNI-0247 | blocked | medium | s | — | Harden BPF helper IPC and bound client handling. — blocked: an `AF_UNIX` stream client needs write permission on the socket path, so root-owned mode `0644` would deny every unprivileged collector. Choose a dedicated group with `0660`, a scoped ACL, or socket activation/ownership before changing access; client timeouts remain pending that decision. |
| OMNI-0085 | blocked | high | s | OMNI-0036, OMNI-0043 | `hardware-health` model: define labeled fault/normal evaluation data and metrics, qualify a real accelerator-compatible anomaly model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0086 | blocked | high | s | OMNI-0038, OMNI-0085 | `hardware-health` pipeline: implement feature windows, preprocessing, anomaly inference, calibrated scoring, evidence extraction, and bounded alerts while keeping firmware/shutdown/repair actions deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0087 | blocked | high | s | OMNI-0082, OMNI-0086 | `hardware-health` acceptance: record fault-injection recall/false-positive, privacy, missing-sensor, latency, and named-hardware evidence before marking the plugin operational. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0088 | blocked | high | s | OMNI-0036, OMNI-0044 | `storage-intelligence` model: define labeled trend/anomaly data and metrics, qualify a real accelerator-compatible model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0089 | blocked | high | s | OMNI-0038, OMNI-0088 | `storage-intelligence` pipeline: implement feature windows, inference, calibrated trend/anomaly results, confidence/evidence, and cache/tiering/maintenance advice without destructive actions. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0090 | blocked | high | s | OMNI-0082, OMNI-0089 | `storage-intelligence` acceptance: record replay accuracy, device-loss recovery, privacy, false-positive, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0091 | blocked | high | s | OMNI-0036, OMNI-0045 | `resource-scheduler` model: define workload traces, target decisions, fairness/utility metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0092 | blocked | high | s | OMNI-0038, OMNI-0091 | `resource-scheduler` pipeline: implement preprocessing and placement/timing inference, expose bounded recommendations, and keep enforcement, starvation prevention, and safety limits deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0093 | blocked | high | s | OMNI-0082, OMNI-0092 | `resource-scheduler` acceptance: record utility, starvation/fairness, policy-change, overload, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0094 | blocked | high | s | OMNI-0036, OMNI-0046 | `build-advisor` model: define a provenance-safe labeled corpus and ranking/risk metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0095 | blocked | high | s | OMNI-0038, OMNI-0094 | `build-advisor` pipeline: implement feature extraction, inference, calibrated regression-risk/check ranking, and explainable advice without silently omitting mandatory checks. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0096 | blocked | high | s | OMNI-0082, OMNI-0095 | `build-advisor` acceptance: record corpus quality, ranking/risk accuracy, adversarial-path, privacy, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0097 | blocked | high | s | OMNI-0036, OMNI-0047 | `visual-library` models: define labeled classification/retrieval evaluation data and metrics, qualify real accelerator-compatible classifier/embedding models, and publish verified artifacts and tensor contracts. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0098 | blocked | high | s | OMNI-0038, OMNI-0097 | `visual-library` inference: implement deterministic resize/normalization, classification/embedding inference, calibrated tags, batching, and bounded failure handling. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0101 | blocked | high | s | OMNI-0082, OMNI-0100 | `visual-library` acceptance: record malformed-media isolation, index recovery, classification/retrieval quality, privacy, throughput, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0106 | blocked | high | s | OMNI-0104, OMNI-0105 | Record 0.2.0 performance on the named RX 6600 XT: hardware Vulkan execution with zero CPU fallback, warm p95 ≤50 ms, and ≥1.2 measured end-to-end speedup over the documented CPU enhancement baseline. Blocked until the artifact, pipeline, and fidelity corpus exist. |
| OMNI-0108 | blocked | high | s | OMNI-0036, OMNI-0049, OMNI-0107 | `network-peripherals` model: define privacy-reviewed anomaly data and metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0109 | blocked | high | s | OMNI-0038, OMNI-0108 | `network-peripherals` pipeline: aggregate bounded windows, preprocess, infer and calibrate anomalies, correlate sources, and extract evidence without per-packet inference. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0110 | blocked | high | s | OMNI-0078, OMNI-0109 | `network-peripherals` output: publish bounded alerts/recommendations and permit only explicitly granted, allowlisted, reversible responses outside model control. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0111 | blocked | high | s | OMNI-0082, OMNI-0110 | `network-peripherals` acceptance: record privacy, hotplug/replay recovery, false-positive/recall, restricted-response, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0112 | blocked | high | s | OMNI-0036, OMNI-0050 | `desktop-context` model: define consented redacted evaluation data and suggestion-quality metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0113 | blocked | high | s | OMNI-0038, OMNI-0112 | `desktop-context` pipeline: preprocess context, infer calibrated context/layout suggestions, suppress stale-session results, and attach bounded explanations without capturing content. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0114 | blocked | high | s | OMNI-0039, OMNI-0113 | `desktop-context` consumer: deliver only allowlisted suggestions to Cinnamon and require explicit user confirmation before any window-manager action; never manipulate windows silently. — blocked: needs coordinated work in the Cinnamon applet repository, which this repository cannot change. |
| OMNI-0115 | blocked | high | s | OMNI-0082, OMNI-0114 | `desktop-context` acceptance: record consent/revocation, privacy/redaction, session churn, suggestion quality, user-action safety, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0122 | blocked | high | s | OMNI-0052 | system acceptance provisioning: install, verify, configure, consent, and smoke-test every bundled plugin and artifact with representative real input and user-visible output. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0123 | blocked | high | s | OMNI-0122 | system acceptance resilience: exercise device/source loss and recovery, worker crash, backpressure, cancellation, service/Cinnamon restart, and in-flight cleanup across representative plugins. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0124 | blocked | high | s | OMNI-0122 | system acceptance lifecycle: verify plugin/artifact/service upgrade, configuration migration, rollback, revocation, cache cleanup, and offline startup without losing valid prior state. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0125 | blocked | high | s | OMNI-0087, OMNI-0090, OMNI-0093, OMNI-0096, OMNI-0101, OMNI-0106, OMNI-0111, OMNI-0115 | system acceptance hardware: run the reproducible TPU/NPU/GPU matrix, archive evidence, and prevent any profile from claiming operational readiness until its own model, pipeline, safety, and acceptance gates pass. — blocked: the GPU lane is available (Vulkan/ncnn) but no TPU or NPU device is present, so the full matrix cannot be run. Document Intelligence's named-GPU acceptance is complete; the remaining profile rows and absent TPU/NPU hardware still block the system-wide matrix. |
| OMNI-0184 | blocked | medium | s | OMNI-0183 | the eBPF helper's privileged path is unverified: the loader, map reads, and socket service were written and compile-checked, but nothing on this host can run them — loading needs CAP_BPF and `bpftool prog load` fails EPERM as this user, so `load_probes`/`read_histogram` have never executed against a real kernel; verify on a host where the helper can be installed as root before relying on the data. |
| OMNI-0198 | blocked | medium | l | OMNI-0186, OMNI-0221 | TPU lane parity — produce and accept an Edge TPU artifact: the fail-closed compiler adapter now exists, but every recipe still needs a representative-calibrated fully-int8 TFLite export, the externally distributed `edgetpu_compiler`, 100% Edge TPU operation mapping, portable/native semantic parity, and named Coral execution evidence. Decide whether releases use a pinned compiler container or trusted precompiled artifacts. This host has neither the compiler nor a Coral device, so production and execution remain unverified. |
| OMNI-0235 | blocked | medium | l | OMNI-0198, OMNI-0233 | Coral event-extraction alternative: Qwen2.5-VL is not a realistic Edge TPU artifact, so evaluate a small fully-int8 candidate-line/token classifier combined with deterministic calendar parsing under the same event result contract. Qualify it only with representative calibration, full compiler mapping, multilingual grounded-event quality, parity of safety/refusal semantics, and named Coral evidence; blocked here on a selected model/corpus, external compiler, and hardware. |
| OMNI-0289 | blocked | low | xs | — | Decide whether generation intentionally excludes TPU despite the global TPU>NPU>GPU preference. Until the product decision is made, document the absent lane at `GenerationRouter` and do not change routing. |

## Rejected / Won't fix

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| OMNI-0349 | rejected | low | xs | — | Architecture review non-findings, kept so later audits do not re-derive them. **Trainer duplication is false:** the five trainer CLIs are two-line wrappers over one parameterized path (`training_train_cli.py` `dispatch_trainer` + `TRAINER_DISPATCH`). **Validation is single-engine:** `registry.validate_document` is the only schema engine; `snapshot.py:109` and `contract.py:93` both delegate to it. **Providers are correctly separated:** four of six are five-line identity shims over the shared `omnitensor-qwen-vulkan-runtime` dependency, with zero copy-pasted files by digest comparison. **Mutation tooling stays per-language:** 176 lines of JS around Stryker and 555 of Python around mutmut wrap engines that cannot be shared; only the selector-file concept overlaps, and a shared format would couple the repositories for no behavioural gain. |
| OMNI-0334 | wont_fix | medium | xs | OMNI-0333 | Sonar reports missing lock files for the seven separately published provider and external-template library projects. Retain their ranged package metadata: Python library locks are not consumed when downstream applications install wheels, and each deployment environment must resolve those ranges into its own lock. The service and repository CI now use the root `uv.lock`; mark the seven library findings won't-fix after the next scan. |
| OMNI-0337 | wont_fix | medium | xs | OMNI-0335, OMNI-0336 | Retain the reviewed caller-selected paths behind the remaining Sonar S8705/S8707 reports: mutation manifests and source roots plus media-fixture targets/tool discovery are explicit offline developer CLI inputs; `atomicio` is the generic path-owning persistence boundary; `file_digest` reads operator-selected artifacts; and privileged BPF helpers now receive only installed constants from `main` while tests inject temporary paths. None is reachable from a model, plug-in payload, or service request; mark any surviving reports false-positive after the next scan. |
| OMNI-0338 | wont_fix | medium | xs | OMNI-0247 | Retain the reviewed semantic hotspots: the reported `tokenizer.json` value is a public SHA-256 digest, bidi controls are the sanitization denylist and its regression fixture, sandbox `/tmp` is a new private tmpfs inside the worker namespace, and `random.Random(seed)` builds a reproducible bounded conformance corpus rather than a secret. Mark those six findings false-positive. Keep the BPF socket-mode finding open under OMNI-0247 until group/ACL/socket-activation ownership is chosen. |

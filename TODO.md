# TODO

## Findings

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| OMNI-0048 | blocked | high | s | OMNI-0036 | `low-light-enhancement` artifact: supply a checksummed, signed, fully quantized `low-light-tonal-curve` model plus tensor contract and compiler report proving its target mapping. — blocked: targets the Edge TPU specifically, and no Coral device is present. Retargeting this profile to the available Vulkan GPU is a design decision. |
| OMNI-0079 | blocked | medium | s | OMNI-0077, OMNI-0078 | Cinnamon contract parity: add shared fixtures and compatibility documentation proving old snapshots still render and new health/result states map deterministically in the applet. — blocked: needs coordinated work in the Cinnamon applet repository, which this repository cannot change. |
| OMNI-0085 | blocked | high | s | OMNI-0036, OMNI-0043 | `hardware-health` model: define labeled fault/normal evaluation data and metrics, qualify a real accelerator-compatible anomaly model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0086 | blocked | high | s | OMNI-0038, OMNI-0085 | `hardware-health` pipeline: implement feature windows, preprocessing, anomaly inference, calibrated scoring, evidence extraction, and bounded alerts while keeping firmware/shutdown/repair actions deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0087 | blocked | high | s | OMNI-0082, OMNI-0086 | `hardware-health` acceptance: record fault-injection recall/false-positive, privacy, missing-sensor, latency, and named-hardware evidence before marking the plugin operational. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0088 | blocked | high | s | OMNI-0036, OMNI-0044 | `storage-intelligence` model: define labeled trend/anomaly data and metrics, qualify a real accelerator-compatible model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0089 | blocked | high | s | OMNI-0038, OMNI-0088 | `storage-intelligence` pipeline: implement feature windows, inference, calibrated trend/anomaly results, confidence/evidence, and cache/tiering/maintenance advice without destructive actions. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0090 | blocked | high | s | OMNI-0082, OMNI-0089 | `storage-intelligence` acceptance: record replay accuracy, device-loss recovery, privacy, false-positive, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0091 | blocked | high | s | OMNI-0036, OMNI-0045 | `resource-scheduler` model: define workload traces, target decisions, fairness/utility metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0092 | blocked | high | s | OMNI-0038, OMNI-0091 | `resource-scheduler` pipeline: implement preprocessing and placement/timing inference, expose bounded recommendations, and keep enforcement, starvation prevention, and safety limits deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0093 | blocked | high | s | OMNI-0082, OMNI-0092 | `resource-scheduler` acceptance: record utility, starvation/fairness, policy-change, overload, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0094 | blocked | high | s | OMNI-0036, OMNI-0046 | `build-advisor` model: define a provenance-safe labeled corpus and ranking/risk metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0095 | blocked | high | s | OMNI-0038, OMNI-0094 | `build-advisor` pipeline: implement feature extraction, inference, calibrated regression-risk/check ranking, and explainable advice without silently omitting mandatory checks. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0096 | blocked | high | s | OMNI-0082, OMNI-0095 | `build-advisor` acceptance: record corpus quality, ranking/risk accuracy, adversarial-path, privacy, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0097 | blocked | high | s | OMNI-0036, OMNI-0047 | `visual-library` models: define labeled classification/retrieval evaluation data and metrics, qualify real accelerator-compatible classifier/embedding models, and publish verified artifacts and tensor contracts. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0098 | blocked | high | s | OMNI-0038, OMNI-0097 | `visual-library` inference: implement deterministic resize/normalization, classification/embedding inference, calibrated tags, batching, and bounded failure handling. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0101 | blocked | high | s | OMNI-0082, OMNI-0100 | `visual-library` acceptance: record malformed-media isolation, index recovery, classification/retrieval quality, privacy, throughput, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0102 | blocked | high | s | OMNI-0048, OMNI-0066 | `low-light-enhancement` input: validate/decode bounded images, normalize orientation/color, resize to 256×256, construct exact quantized tensors, and preserve metadata needed for output. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0103 | blocked | high | s | OMNI-0038, OMNI-0102 | `low-light-enhancement` pipeline: run Edge-TPU inference, validate tonal-curve outputs, apply bounded curve/denoise/color management, and reject invalid or unsafe results deterministically. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0104 | blocked | medium | s | OMNI-0103 | `low-light-enhancement` delivery: encode atomically to an authorized destination, preserve allowed metadata, expose progress/result details, and clean partial outputs on cancellation or failure. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0105 | blocked | high | s | OMNI-0082, OMNI-0103 | `low-light-enhancement` fidelity: freeze the float/quantized evaluation corpus and record SSIM ≥0.95, median CIEDE2000 ≤3, noise, color, and artifact measurements. — blocked: targets the Edge TPU specifically, and no Coral device is present. Retargeting this profile to the available Vulkan GPU is a design decision. |
| OMNI-0106 | blocked | high | s | OMNI-0104, OMNI-0105 | `low-light-enhancement` performance: record 100% Edge-TPU operation mapping, warm p95 ≤50 ms, and ≥1.2 end-to-end speedup over the documented CPU baseline on named hardware. — blocked: targets the Edge TPU specifically, and no Coral device is present. Retargeting this profile to the available Vulkan GPU is a design decision. |
| OMNI-0108 | blocked | high | s | OMNI-0036, OMNI-0049, OMNI-0107 | `network-peripherals` model: define privacy-reviewed anomaly data and metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0109 | blocked | high | s | OMNI-0038, OMNI-0108 | `network-peripherals` pipeline: aggregate bounded windows, preprocess, infer and calibrate anomalies, correlate sources, and extract evidence without per-packet inference. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0110 | blocked | high | s | OMNI-0078, OMNI-0109 | `network-peripherals` output: publish bounded alerts/recommendations and permit only explicitly granted, allowlisted, reversible responses outside model control. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0111 | blocked | high | s | OMNI-0082, OMNI-0110 | `network-peripherals` acceptance: record privacy, hotplug/replay recovery, false-positive/recall, restricted-response, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0112 | blocked | high | s | OMNI-0036, OMNI-0050 | `desktop-context` model: define consented redacted evaluation data and suggestion-quality metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0113 | blocked | high | s | OMNI-0038, OMNI-0112 | `desktop-context` pipeline: preprocess context, infer calibrated context/layout suggestions, suppress stale-session results, and attach bounded explanations without capturing content. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0114 | blocked | high | s | OMNI-0039, OMNI-0113 | `desktop-context` consumer: deliver only allowlisted suggestions to Cinnamon and require explicit user confirmation before any window-manager action; never manipulate windows silently. — blocked: needs coordinated work in the Cinnamon applet repository, which this repository cannot change. |
| OMNI-0115 | blocked | high | s | OMNI-0082, OMNI-0114 | `desktop-context` acceptance: record consent/revocation, privacy/redaction, session churn, suggestion quality, user-action safety, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0117 | blocked | high | s | OMNI-0036, OMNI-0116 | `document-intelligence` models: define privacy-reviewed classification/retrieval/layout corpora and metrics, qualify real accelerator-compatible models, and publish verified artifacts and tensor contracts. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0118 | blocked | high | s | OMNI-0038, OMNI-0117 | `document-intelligence` inference: preprocess extracted content, run bounded classification/embedding/layout inference, calibrate outputs, batch safely, and discard stale source versions. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0121 | blocked | high | s | OMNI-0082, OMNI-0120 | `document-intelligence` acceptance: record malicious-document isolation, index recovery, privacy, classification/retrieval/layout quality, throughput, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0122 | blocked | high | s | OMNI-0052 | system acceptance provisioning: install, verify, configure, consent, and smoke-test every bundled plugin and artifact with representative real input and user-visible output. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0123 | blocked | high | s | OMNI-0122 | system acceptance resilience: exercise device/source loss and recovery, worker crash, backpressure, cancellation, service/Cinnamon restart, and in-flight cleanup across representative plugins. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0124 | blocked | high | s | OMNI-0122 | system acceptance lifecycle: verify plugin/artifact/service upgrade, configuration migration, rollback, revocation, cache cleanup, and offline startup without losing valid prior state. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0125 | blocked | high | s | OMNI-0087, OMNI-0090, OMNI-0093, OMNI-0096, OMNI-0101, OMNI-0106, OMNI-0111, OMNI-0115, OMNI-0121 | system acceptance hardware: run the reproducible TPU/NPU/GPU matrix, archive evidence, and prevent any profile from claiming operational readiness until its own model, pipeline, safety, and acceptance gates pass. — blocked: the GPU lane is available (Vulkan/ncnn) but no TPU or NPU device is present, so the full matrix cannot be run. |
| OMNI-0172 | blocked | medium | s | OMNI-0073 | applet parity for transport refusals: `BusGuard` now answers an over-quota, oversized, or identity-asserting call with a versioned envelope (`{version,status:"rejected",code,message,method}`) that is *not* a job acknowledgement, so an applet that parses every reply against `runtime-job-acknowledgement.schema.json` will treat a refusal as a malformed reply; the envelope is now published as `runtime-refusal.schema.json` and every refusal is validated against it, so what remains is applet-side: parse a reply against it before assuming a job acknowledgement, and handle the `rate-limit-exceeded`/`payload-too-large`/`identity-asserted`/`concurrency-limit-exceeded`/`method-unknown` codes. |
| OMNI-0173 | open | medium | s | OMNI-0191 | the remaining eight profiles still bundle no model: `visual-library` now serves a real ncnn classifier on the GPU, proving the path end to end, but the others need models that do not exist in a runnable form — `document-intelligence` needs a text embedder converted to ncnn (no conversion path, OMNI-0186), `low-light-enhancement` declares tflite-edgetpu and needs Edge TPU hardware, and the five telemetry profiles (hardware-health, storage-intelligence, resource-scheduler, build-advisor, desktop-context, network-peripherals) need models trained on history this host has only just started recording. Note that until OMNI-0191 is resolved no image or audio model can receive input over the bus anyway. |
| OMNI-0174 | open | low | s | OMNI-0170 | uid scoping is a weak boundary on a session bus: every peer on a session bus is normally the same uid, so owner scoping separates almost nothing there and only becomes meaningful when the socket is reachable by another uid; decide whether per-connection scoping should be offered for callers that want it, and document which guarantee the applet is relying on. |
| OMNI-0175 | open | medium | s | OMNI-0171 | seccomp on kernels that cannot install a filter: the worker fails closed and refuses to start when `seccomp(2)` or `no_new_privs` is unavailable (a kernel without `CONFIG_SECCOMP_FILTER`, or a restrictive container), and `--no-seccomp` is the only escape but nothing selects it automatically; decide whether the supervisor should probe once at startup and report an unconfined runtime in the snapshot rather than every plugin failing to launch. |
| OMNI-0176 | open | low | s | OMNI-0171 | seccomp syscall tables cover only x86_64 and aarch64: any other architecture raises `SeccompUnsupportedError` and, with the fail-closed default, no plugin worker starts at all there; add tables for the architectures the project intends to support or state the supported set in the packaging metadata. |
| OMNI-0183 | open | medium | m | OMNI-0045 | surface kernel telemetry in the runtime snapshot and the scheduler profile: `kernel_telemetry` reads run-queue and block-I/O histograms from the privileged helper, but nothing consumes them yet — the snapshot has no field for them and `resource-scheduler` still reasons from PSI alone; add a bounded `kernelTelemetry` block to the snapshot contract (both repositories) and feed the histograms into the scheduler's features. |
| OMNI-0184 | open | medium | s | OMNI-0183 | the eBPF helper's privileged path is unverified: the loader, map reads, and socket service were written and compile-checked, but nothing on this host can run them — loading needs CAP_BPF and `bpftool prog load` fails EPERM as this user, so `load_probes`/`read_histogram` have never executed against a real kernel; verify on a host where the helper can be installed as root before relying on the data. |
| OMNI-0187 | open | medium | s | OMNI-0181 | document the eBPF helper's install and uninstall in `docs/`: the build script, the privileged system unit, and the `/usr/lib/omnitensor/bpf` layout are only described in this session's transcript and the module docstring, and the attach step is still unverified — write it up beside the service install once the helper has been loaded successfully on a host with CAP_BPF. |
| OMNI-0197 | open | medium | m | OMNI-0186 | NPU lane parity — produce an OpenVINO artifact: the GPU lane can now convert (ONNX to ncnn via pnnx, `[convert]` extra) but the NPU lane has no equivalent. `openvino` ships `ovc`, which converts ONNX to IR, and the wheel downloads cleanly on this host (openvino 2026.3.0, verified); the artifact side is already prepared, since `openvino` now declares its `.bin` companion. Add an OpenVINO converter behind the existing `ModelConverter` port and an `[convert-npu]` extra. Note the result cannot be executed here — there is no NPU on this host — so this delivers a produced, digested, installable artifact and not a tested inference. |
| OMNI-0198 | open | medium | l | OMNI-0186 | TPU lane parity — produce an Edge TPU artifact: unlike the other two lanes this cannot be reached with a pip install. It needs a fully int8-quantised TFLite model and then the Edge TPU compiler (`edgetpu_compiler`), which is distributed by Google for specific Debian releases and is not a Python package, so it cannot go behind the `ModelConverter` port the way pnnx and ovc can. Decide whether the project requires that toolchain in a packaging container, vendors precompiled Edge TPU artifacts from upstream, or declares the TPU lane out of scope for bundled profiles. No Coral device is present here either, so neither production nor execution is verifiable on this host. |
| OMNI-0199 | open | low | s | OMNI-0197, OMNI-0198 | conversion parity is asymmetric and should be stated where users look: `docs/installation.md` documents the GPU conversion path only. Once the NPU path exists, the doc needs a table of which lanes can be produced on which hosts and what each requires, so nobody reads GPU-only tooling as the whole story. |
| OMNI-0201 | open | low | s | OMNI-0200 | a referenced buffer is digested twice per job: admission streams the file to prove the declared sha256 and the infer stage reads and digests it again, because only the bytes actually read can be trusted to be the ones executed. Correct but not free — a 64 MiB reference is hashed twice — so if profiling shows it matters, hold the verified read for the job's lifetime under a bound instead of re-reading, and re-verify only on change. |
| OMNI-0202 | open | low | s | OMNI-0200 | admission proves a referenced buffer's containment, size, and digest but not its values, so a reference to a file of NaNs is still admitted and refused only in the infer stage, which is the shape of failure OMNI-0200 removed for the other cases. Finiteness can be checked while the digest is streamed, chunk by chunk, without retaining the buffer. |
| OMNI-0203 | open | low | s | OMNI-0173 | the snapshot now resolves every declared artifact on each publish: `_profile_artifact_ready` calls the store per profile per snapshot, which re-reads and re-digests each installed model file every publish interval. Correct, and cheap while one profile declares a 7 KB graph, but it scales with catalogue size times artifact size and the publish interval is two seconds. Cache the resolution against the artifact's mtime and size, the way the manifest validator already caches. |








| OMNI-0211 | open | high | m | OMNI-0209 | Which Vulkan device runs a job changes the answer, and the service does not always run on the one it selected. Measured on this host with one byte-identical input, one model, and a fresh Net each time: RX 6600 XT (discrete) `zebra 0.999`, Radeon 610M (integrated) `zebra 0.680`, llvmpipe (software, already excluded by `_DEVICE_PREFERENCE`) `zebra 0.676` — each device deterministic on its own, across five runs. Through the service the same submission returns 0.999 eight times in ten and 0.658 otherwise, and 0.658 is the integrated device's exact answer, so work is landing on a device `_select_device` did not choose. Caching the enumeration helped (6/10 to 8/10 correct) because the snapshot publisher was probing Vulkan twice a second from another thread while inference ran, so that was one cause and not the only one. Two questions remain: what still moves execution off the selected device, and why two AMD devices disagree by 0.3 on the same graph at all — an fp16 path is the obvious suspect and is worth confirming before trusting any accuracy measurement. Until this is settled no acceptance number from the GPU lane means anything, because the same input scores differently run to run. |

## Rejected / Won't fix

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |

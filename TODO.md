# TODO

## Work groups

Items remain in the canonical lifecycle tables below; these ranges preserve the
audit's dependency order without weakening the required status schema.

- **Low-light delivery:** OMNI-0048, OMNI-0103–OMNI-0106
- **Audit behavior and contracts:** OMNI-0242–OMNI-0257
- **Audit wiring and consolidation:** OMNI-0258–OMNI-0272
- **Audit module splits:** OMNI-0273–OMNI-0284
- **Audit architecture hygiene:** OMNI-0285–OMNI-0289
- **Audit regression and mutation gates:** OMNI-0290–OMNI-0297
- **Mutation signal quality:** OMNI-0298–OMNI-0303

## Findings

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| OMNI-0296 | open | medium | l | OMNI-0264, OMNI-0295 | Re-measure and burn down remaining acceptance, event-workload, document-binding, and grounded-answer survivor hotspots until every changed callable meets the 80% mutation threshold; do not re-add already-fixed low-light hotspots without fresh evidence. |
| OMNI-0307 | open | medium | s | OMNI-0306 | Build the runtime snapshot (or at least the kernel-telemetry read) off the event loop or under an overall deadline: `_build_runtime_snapshot()` runs on-loop every 2 s (service.py:460; only the publish at :461 is offloaded via to_thread) and includes a blocking unix-socket read whose 2 s timeout applies per recv with no total bound (telemetry_observation.py:203; kernel_telemetry.py:344-360 `settimeout` bounds each recv in a chunked loop up to 256 KB, DEFAULT_TIMEOUT_SECONDS = 2.0 at :37), so a slow or drip-feeding helper stalls the loop for many seconds each tick. |
| OMNI-0308 | open | medium | xs | — | Coerce `weight` via `_integral_weight` in the `apply-profiles` batch path: `_apply_batch` assigns `policy.weight = change["weight"]` raw (control.py:160-166) while the single-command path coerces (control.py:138), and JSON Schema draft 2020-12 accepts `2.0` for `"type": "integer"` (verified with the shipped validator against schemas/runtime-command.schema.json), so a float weight is accepted, persisted as `2.0`, and on next service start `_clamp_weight`'s int check (state.py:22-25) silently resets the profile to its default weight — the exact failure `_integral_weight`'s docstring documents for the single path. |
| OMNI-0309 | open | medium | xs | — | Expanduser the grants path at the GrantLedger (or service constructor) boundary and delete the stray `~/` directory at the repo root: `DEFAULT_GRANTS_PATH` is the literal string `"~/.local/state/omnitensor/grants.json"` (composition.py:17; only composition.py:26 expands) and service.py:150/:181 passes it to `GrantLedger`, which does `Path(path)` verbatim (grants.py:87) while storelock.py:34-37 mkdirs the parent on first lock — so any direct construction not going through `build_service_from_env` (tests, embedders, scripts) creates a literal `./~/.local/state/omnitensor/` under the CWD and silently reads/writes consent state there. The repo root already contains this accident: `omnitensor/~/.local/state/omnitensor/.grants.json.lock` (accidental content only — safe to remove). |
| OMNI-0311 | open | high | s | OMNI-0312 | Add a cross-cycle restart counter (with decay) to worker recovery: `recover_worker`'s `for attempt in range(1, max_restarts + 1)` only bounds consecutive failed launches — on any successful launch, supervisor_recovery.py:194 `_install_ready_slot` (supervisor.py:361-364) arms a fresh `monitor_worker` and :205 pops `_recoveries`, so the next crash starts a brand-new recovery at attempt 1 with the 0.1 s initial backoff (supervisor_recovery.py:109-114, 186-207). A plugin that completes handshake+ready then crashes is relaunched forever at ~10 spawns/second; `WorkerState.EXHAUSTED` (:219-226) is unreachable for a crash loop, and `WorkerStatus.restart_attempts` is overwritten with the current cycle's attempt (supervisor.py:353-359). |
| OMNI-0313 | open | medium | l | — | Wire per-worker resource-budget enforcement into the actual launch path — every declared budget is currently inert: `WorkerBudgetEnforcer` (budgets.py:110), `ProcfsWorkerUsageProbe` (:239), `CgroupWorkerUsageProbe` (:312), and `create_worker_cgroup`/`join_worker_cgroup` (:350-370) have zero call sites outside budgets.py and the lazy re-export table (plugins/__init__.py:202-205, 328-330); `AsyncioSubprocessLauncher.launch` (supervisor_process.py:96-107) passes no preexec_fn, cgroup, or rlimits, and `PluginWorkerSupervisor.execute` (supervisor.py:156-184) has no call timeout or output-size check — so `call_timeout_seconds`, `max_processes`, `max_memory_bytes`, `max_descriptors`, `max_output_bytes`, and `max_concurrency` are unenforced for every real worker, including the daemonise-escape case the cgroup probe's own docstring (:312-323) says it exists to close. |
| OMNI-0314 | open | medium | s | — | Add the `accelerator:tpu` mapping so a TPU-preferred worker doesn't silently run with no accelerator: `ACCELERATOR_PERMISSIONS = frozenset({"accelerator:gpu", "accelerator:npu"})` (loading_accelerator.py:9) means `accelerator_lease_path` (:24) and `accelerator_paths` (:42) yield no device path or lease for a granted `accelerator:tpu`, contradicting sandbox.py:27-29 (allowlists `/dev/apex_[0-9]+`), discovery.py:22 (detects `apex_*`), and the README's TPU lane; `plugin_accelerator_devices` (artifact_readiness.py:128-135) likewise only emits gpu/npu keys. The omission is silent in both directions — `accelerator_paths:45` drops missing entries and `external_worker_spec` (worker_specs.py:90-107) never checks that a granted accelerator permission produced a device — so the sandbox exposes no accelerator node and the plugin runtime falls back to CPU with nothing recorded, violating the no-CPU invariant. |
| OMNI-0315 | open | medium | m | — | Make the pipeline resolve and label the accelerator-preference-selected lane instead of `models[0]`: orchestration.py:163 takes `workload.model` (declaration order, registry.py:171-180) and uses it at :177 (`_resolve_stage`), :180 (`_label_reader`), :259 (`declared_output`), while the executor lane is chosen by `select_backend`/`runnable_model` walking tpu>npu>gpu (scheduler.py:62-115). For multi-lane `requirements.models` profiles — the shape the schema documents as existing "so `acceleratorPreference` can actually be honoured" (schemas/workload-manifest.schema.json:242) — RESOLVE (:234-241) gates on the wrong artifact and raises `artifact-unavailable` when `models[0]` is missing even though the preferred lane is installed, and `_label_reader` (:293-306) reads `labels.txt` from `models[0]`'s directory, returning index-numbered results when that lane is absent; INFER (:249-253) never corrects it. |
| OMNI-0316 | open | low | s | — | Force-stop the worker on a protocol mismatch instead of leaving it READY on a desynchronised channel: when `read_result` sees a frame naming another request it raises `worker-protocol-failed` (supervisor_session.py:148-151) and `supervisor.execute` re-raises without stopping the process (supervisor.py:178-184) — unlike the cancellation path, which force-stops (supervisor_session.py:198-199) — so the slot stays installed, `WorkerState.READY` persists, and `InstalledPluginRuntime.admit` (loading.py:185-186) keeps admitting jobs onto an off-by-one channel where each job burns one stale frame and fails until the pipe happens to close. |
| OMNI-0317 | open | high | s | — | Stop a schema-invalid snapshot from tearing down the entire service: service.py:469 catches only `OSError` but snapshot.py:109-111 raises `ValueError` on any contract violation, the exception escapes the `loops` gather, and `run()` shuts down D-Bus, dispatch, and the scheduler — the outcome the comment at service.py:452-455 says must not happen. Concretely reachable: `input_roots_document` (snapshot.py:68-71) copies configured roots verbatim while runtime-snapshot.schema.json:236 declares `inputs.roots` `uniqueItems: true`, and `OMNITENSOR_INPUT_ROOTS` is parsed without dedup (composition.py:36), so a repeated root makes every publish tick raise (verified: `['/a','/a']` → non-unique-elements error); the >8-root `ValueError` (snapshot.py:63) reaches the same path despite its docstring claiming startup-only. Catch contract violations in the publish loop (degrade, don't die) and dedup/validate roots at composition time. |
| OMNI-0318 | open | medium | xs | — | Validate the event-extraction result before emission: `grounded_event_document` (plugins/event_workload.py:506-538, emitted at :377) builds a NEW public `event-extraction-result` document (duplicates were dropped in `parse_grounded_event_result`) and returns it with no `validate_document("event-extraction-result.schema.json", …)` call, unlike document_qa.py:518, selected_text.py:300, file_organizer.py:313, and media_transcription.py:618; runtime-job-result.schema.json types `output` only as `["object","null"]`, so nothing downstream re-checks it — the one public plugin document emitted unvalidated, violating the schemas-as-contract invariant. |
| OMNI-0319 | open | medium | s | — | Align media-transcription input acceptance with its own result schema so bad inputs fail up front instead of after all work is done: `_validated_media` permits `duration == 0` (plugins/media_transcription.py:375) but media-transcription-result.schema.json `$defs/source/durationMs` requires `minimum: 1`, and `_LANGUAGE` (:42) allows unbounded `-subtag` repetitions while `$defs/speech/language` caps `maxLength: 35` — verified: `MediaInfo(AUDIO, 0, …)` passes validation, then `media_result` fails schema validation, surfacing as a generic `result-invalid` refusal after probing, transcription, and digesting. |
| OMNI-0320 | open | low | xs | — | Validate bus refusal documents at the emission boundary: guard.py:51-61 builds and serializes the refusal returned on the bus (runtime_api.py:87, :95, :110) with no `validate_document` against runtime-refusal.schema.json — the only wire schema in acceptance_checks.py's canonical list (:44) with no emission-boundary check. Current codes and `method` values happen to satisfy the schema's `code` enum and `method` pattern, so this is an unenforced invariant rather than present drift. |
| OMNI-0321 | open | high | s | — | Pin absolute paths in the privileged eBPF helper: helpers/bpf/omnitensor-bpf-helper.py invokes `bpftool` as a bare name (:41, :47, :59, :78) with a `#!/usr/bin/env python3` shebang (:1), while helpers/bpf/omnitensor-bpf.service:8 runs it with no `User=` (UID 0) and no `Environment=PATH=`, leaving systemd's default PATH whose earlier elements can be writable by non-root principals (`/usr/local/bin` is `root:staff 2775` on Debian-family bases) — root code execution inside a unit holding CAP_BPF+CAP_PERFMON, i.e. arbitrary kernel-memory read via BPF. Use `/usr/sbin/bpftool` and `#!/usr/bin/python3`, and pin `Environment=PATH=` in the unit. |
| OMNI-0322 | open | medium | xs | OMNI-0321 | Handle client write errors in the eBPF helper so a disconnecting client can't wedge it into a permanent failed state: `connection.sendall(...)` (omnitensor-bpf-helper.py:126) sits outside the `try/except (CalledProcessError, ValueError)` at :122-125, so a client that closes before reading raises `BrokenPipeError`/`ConnectionResetError`, which propagates out of `serve()`/`main()` and terminates the process; with `Restart=on-failure`/`RestartSec=5` (omnitensor-bpf.service:9-10) and no `StartLimit*` override, repeats trip systemd's default start limit, the unit goes `failed`, and `UnixSocketAggregateSource.read()` silently degrades to `HELPER_ABSENT` forever. Distinct from the tracked socket-mode/accept-timeout items — this is an unhandled write error. |
| OMNI-0323 | open | medium | s | OMNI-0321 | Bound the BPF histogram bucket list and validate pins by more than existence: omnitensor-bpf-helper.py:87 builds `[buckets.get(slot, 0) for slot in range(max(buckets, default=-1) + 1)]` from `_packed()`-decoded keys with no upper bound (a 4/8-byte key yields a `range(2**32)`/`range(2**64)` materialization), reachable because `load_probes` short-circuits on `pin_dir.exists()` (:54-56) and `verify_pins` (:35-50) checks only that pins exist — never type, `max_entries`, or provenance — so a stale or foreign pin at `/sys/fs/bpf/omnitensor/runq_latency_us` is served as-is, contradicting the "partial prior load fails closed" comment at :54. Clamp to the declared `MAX_SLOTS` (27, runq_latency.bpf.c:13) and validate map type/size in `verify_pins`. |
| OMNI-0324 | open | medium | m | — | Purge the overlap between training and holdout windows in the hardware-health gate: `_window_examples` (src/omnitensor/training/hardware.py:404-419) emits stride-1 sliding windows and hardware.py:350 splits the windows 80/20 via `chronological_split` (tabular/split.py:13-20) with no purge, so training window `s-1` and holdout window `s` share up to `window-1` observations (127 at `MAX_WINDOW`), making the AUC/recall/FPR gates at :361-369 optimistic. storage.py:454-467 shows the intended fix (`train_before = split_date - timedelta(days=horizon_days)`); hardware has no equivalent. |
| OMNI-0325 | open | low | xs | — | Fix docs/plugin-sdk.md and docs/extension-guide.md directing plugin authors to symbols the SDK does not export: plugin-sdk.md:3 says external plugins import from `omnitensor.sdk`, then plugin-sdk.md:43,46 and extension-guide.md:72 reference `LiveGrantView` and `ConsentGuard.run()`, neither of which is in src/omnitensor/sdk/__init__.py (imports :3-87, `__all__` :89-174; verified `hasattr(omnitensor.sdk, "ConsentGuard") is False`) — they exist only in service internals (plugins/revocation.py:34, :77) the same doc says are deliberately unexported. Either export them or correct the guidance. |
| OMNI-0326 | open | medium | s | OMNI-0306 | Move `LiveGrantView` refreshes off the event loop: `ConsentGuard.run()` calls `revoked()` synchronously before execution and after every polling timeout, which reaches `LiveGrantView.granted` and blocking `GrantLedger.reload()` on-loop (plugins/revocation.py:57-62, :101-105, :121-124); add an async refresh boundary while preserving fail-closed revocation checks. |
| OMNI-0327 | open | low | xs | OMNI-0296 | Make the service architecture owner check mutation-aware: mutmut's generated `x_main__mutmut_orig` and `x_main__mutmut_*` wrappers are reported as production owners by `test_service_drops_only_private_compatibility_owners`, so any mutation run that includes `service.py` fails during the clean-test phase before exercising a mutant. |
| OMNI-0310 | open | low | xs | — | Pass `allow_nan=False` in `_result_reply`/`_record_reply` and map validator failures to a rejected reply: both serialize with json.dumps defaults (job_codec.py:134-150, :153-189) unlike `_acknowledgement_reply` (:119), so a non-finite float in a stored in-process-runner output serializes as bare `NaN`; the self-check `_validated_result_reply` (:122-131) then raises RuntimeError which `job_result_text` does not catch — the calls at job_lifecycle.py:379-380 sit outside the try/except spanning :351-365, and `RuntimeAPI._guarded` (runtime_api.py:97-110) only catches GuardRefusedError — surfacing a raw D-Bus error instead of a contract-valid reply. |
| OMNI-0303 | open | low | s | OMNI-0296 | Once mutation execution is re-enabled, re-baseline survivor statistics after operator and assertion cleanup before further burn-down; do not chase prose survivors removed by the operator change. |

## Blocked

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| OMNI-0297 | blocked | high | xl | OMNI-0242, OMNI-0243, OMNI-0244, OMNI-0245, OMNI-0246, OMNI-0247, OMNI-0248, OMNI-0249, OMNI-0250, OMNI-0251, OMNI-0252, OMNI-0253, OMNI-0254, OMNI-0255, OMNI-0256, OMNI-0257, OMNI-0258, OMNI-0259, OMNI-0260, OMNI-0261, OMNI-0262, OMNI-0263, OMNI-0264, OMNI-0265, OMNI-0266, OMNI-0267, OMNI-0268, OMNI-0269, OMNI-0270, OMNI-0271, OMNI-0272, OMNI-0273, OMNI-0274, OMNI-0275, OMNI-0276, OMNI-0277, OMNI-0278, OMNI-0279, OMNI-0280, OMNI-0281, OMNI-0282, OMNI-0283, OMNI-0284, OMNI-0285, OMNI-0286, OMNI-0287, OMNI-0288, OMNI-0290, OMNI-0291, OMNI-0292, OMNI-0293, OMNI-0294, OMNI-0295, OMNI-0296 | Run and archive a repository-complete mutation campaign with every callable at or above 80%. — blocked: first finish OMNI-0296, choose and implement OMNI-0247's socket-access policy, OMNI-0258's bounded parity fixture and real portable/native runners, and OMNI-0259's versioned producer-request/holdout contract and reviewed adapters; then define complete core, provider, and template mutation scope and archive per-shard plus consolidated commit/version/selector evidence. |
| OMNI-0048 | blocked | high | s | OMNI-0036 | Produce and publish the `low-light-enhancement` 0.2.0 Retinexformer ncnn artifact with exact graph/weights digests, tensor/output contracts, publisher identity, portable/native parity, and hardware Vulkan proof. — blocked: requires a licensed digest-pinned source/checkpoint, an authentic paired holdout, a concrete Retinex source with portable/native producer and runner, signed ncnn artifacts, and a no-CPU Vulkan execution report. |
| OMNI-0104 | blocked | medium | s | OMNI-0103 | Connect the 0.2.0 enhanced-image pipeline to the existing disjoint-folder, atomic no-overwrite publisher; preserve allowed metadata, expose progress/result evidence, and clean only partial outputs on cancellation or failure. — blocked: requires product decisions for output codec and naming, a conservative metadata/privacy allowlist, and the job/integration surface, plus a real OMNI-0048 artifact for closure. |
| OMNI-0105 | blocked | high | s | OMNI-0082, OMNI-0103 | Freeze an authentic, license-reviewed paired low/normal-light holdout and record 0.2.0 portable/native SSIM, paired-target SSIM, median CIEDE2000, noise, color, range, and artifact measurements with test-only corpus bytes and source provenance. — blocked: requires the authentic paired corpus, the missing CIEDE2000/noise/color/artifact metrics, and OMNI-0048 native evidence; the existing synthetic CC0 corpus is test coverage, not fidelity evidence. |
| OMNI-0258 | blocked | high | m | OMNI-0253 | Wire a `promote_main` console entry point for the five numeric trainers so a trained domain fixture can be promoted into a schema-valid binding; retain the separate forecast report path. — blocked: choose and implement a bounded parity fixture plus concrete portable ONNX and native Vulkan/OpenVINO-NPU runners; reports alone cannot prove parity, so a fake CLI is not an implementation. |
| OMNI-0259 | blocked | high | m | OMNI-0261 | After production-pipeline consolidation, add one recipe-dispatched `omnitensor-produce-source` CLI that reaches embedding, CLIP, Retinexformer, and foundation-forecast producers. — blocked: choose and version a producer-request/holdout contract and concrete reviewed adapters for text, CLIP, Retinex, and forecast; a fake-injected CLI is unusable. |
| OMNI-0247 | blocked | medium | s | — | Harden BPF helper IPC and bound client handling. — blocked: an `AF_UNIX` stream client needs write permission on the socket path, so root-owned mode `0644` would deny every unprivileged collector. Choose a dedicated group with `0660`, a scoped ACL, or socket activation/ownership before changing access; client timeouts remain pending that decision. |
| OMNI-0085 | blocked | high | s | OMNI-0036, OMNI-0043 | `hardware-health` model: define labeled fault/normal evaluation data and metrics, qualify a real accelerator-compatible anomaly model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0086 | blocked | high | s | OMNI-0038, OMNI-0085 | `hardware-health` pipeline: implement feature windows, preprocessing, anomaly inference, calibrated scoring, evidence extraction, and bounded alerts while keeping firmware/shutdown/repair actions deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0087 | blocked | high | s | OMNI-0082, OMNI-0086 | `hardware-health` acceptance: record fault-injection recall/false-positive, privacy, missing-sensor, latency, and named-hardware evidence before marking the plugin operational. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0088 | blocked | high | s | OMNI-0036, OMNI-0044 | `storage-intelligence` model: define labeled trend/anomaly data and metrics, qualify a real accelerator-compatible model, and publish its immutable verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0089 | blocked | high | s | OMNI-0038, OMNI-0088 | `storage-intelligence` pipeline: implement feature windows, inference, calibrated trend/anomaly results, confidence/evidence, and cache/tiering/maintenance advice without destructive actions. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0090 | blocked | high | s | OMNI-0082, OMNI-0089 | `storage-intelligence` acceptance: record replay accuracy, device-loss recovery, privacy, false-positive, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0091 | blocked | high | s | OMNI-0036, OMNI-0045 | `resource-scheduler` model: define workload traces, target decisions, fairness/utility metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0092 | blocked | high | s | OMNI-0038, OMNI-0091 | `resource-scheduler` pipeline: implement preprocessing and placement/timing inference, expose bounded recommendations, and keep enforcement, starvation prevention, and safety limits deterministic. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0093 | blocked | high | s | OMNI-0082, OMNI-0092 | `resource-scheduler` acceptance: record utility, starvation/fairness, policy-change, overload, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0094 | blocked | high | s | OMNI-0036, OMNI-0046 | `build-advisor` model: define a provenance-safe labeled corpus and ranking/risk metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0095 | blocked | high | s | OMNI-0038, OMNI-0094 | `build-advisor` pipeline: implement feature extraction, inference, calibrated regression-risk/check ranking, and explainable advice without silently omitting mandatory checks. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0096 | blocked | high | s | OMNI-0082, OMNI-0095 | `build-advisor` acceptance: record corpus quality, ranking/risk accuracy, adversarial-path, privacy, latency, and named-hardware evidence before operational readiness. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0097 | blocked | high | s | OMNI-0036, OMNI-0047 | `visual-library` models: define labeled classification/retrieval evaluation data and metrics, qualify real accelerator-compatible classifier/embedding models, and publish verified artifacts and tensor contracts. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0098 | blocked | high | s | OMNI-0038, OMNI-0097 | `visual-library` inference: implement deterministic resize/normalization, classification/embedding inference, calibrated tags, batching, and bounded failure handling. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0101 | blocked | high | s | OMNI-0082, OMNI-0100 | `visual-library` acceptance: record malformed-media isolation, index recovery, classification/retrieval quality, privacy, throughput, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0106 | blocked | high | s | OMNI-0104, OMNI-0105 | Record 0.2.0 performance on the named RX 6600 XT: hardware Vulkan execution with zero CPU fallback, warm p95 ≤50 ms, and ≥1.2 measured end-to-end speedup over the documented CPU enhancement baseline. Blocked until the artifact, pipeline, and fidelity corpus exist. |
| OMNI-0108 | blocked | high | s | OMNI-0036, OMNI-0049, OMNI-0107 | `network-peripherals` model: define privacy-reviewed anomaly data and metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0109 | blocked | high | s | OMNI-0038, OMNI-0108 | `network-peripherals` pipeline: aggregate bounded windows, preprocess, infer and calibrate anomalies, correlate sources, and extract evidence without per-packet inference. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0110 | blocked | high | s | OMNI-0078, OMNI-0109 | `network-peripherals` output: publish bounded alerts/recommendations and permit only explicitly granted, allowlisted, reversible responses outside model control. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0111 | blocked | high | s | OMNI-0082, OMNI-0110 | `network-peripherals` acceptance: record privacy, hotplug/replay recovery, false-positive/recall, restricted-response, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0112 | blocked | high | s | OMNI-0036, OMNI-0050 | `desktop-context` model: define consented redacted evaluation data and suggestion-quality metrics, qualify a real accelerator-compatible model, and publish its verified artifact and tensor contract. — blocked: needs a labeled evaluation corpus and a publisher signing identity. GPU inference is available (Vulkan/ncnn), so hardware is no longer the blocker; the data and the key custody decision are. |
| OMNI-0113 | blocked | high | s | OMNI-0038, OMNI-0112 | `desktop-context` pipeline: preprocess context, infer calibrated context/layout suggestions, suppress stale-session results, and attach bounded explanations without capturing content. — blocked on its model row: the tensor contract and quantization parameters this stage encodes do not exist until the artifact is qualified and published. |
| OMNI-0114 | blocked | high | s | OMNI-0039, OMNI-0113 | `desktop-context` consumer: deliver only allowlisted suggestions to Cinnamon and require explicit user confirmation before any window-manager action; never manipulate windows silently. — blocked: needs coordinated work in the Cinnamon applet repository, which this repository cannot change. |
| OMNI-0115 | blocked | high | s | OMNI-0082, OMNI-0114 | `desktop-context` acceptance: record consent/revocation, privacy/redaction, session churn, suggestion quality, user-action safety, latency, and named-hardware evidence. — blocked on its model and pipeline rows: there is nothing qualified to measure yet. GPU hardware for the measurements is available. |
| OMNI-0122 | blocked | high | s | OMNI-0052 | system acceptance provisioning: install, verify, configure, consent, and smoke-test every bundled plugin and artifact with representative real input and user-visible output. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0123 | blocked | high | s | OMNI-0122 | system acceptance resilience: exercise device/source loss and recovery, worker crash, backpressure, cancellation, service/Cinnamon restart, and in-flight cleanup across representative plugins. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0124 | blocked | high | s | OMNI-0122 | system acceptance lifecycle: verify plugin/artifact/service upgrade, configuration migration, rollback, revocation, cache cleanup, and offline startup without losing valid prior state. — blocked on the bundled artifacts, which are not published yet. |
| OMNI-0125 | blocked | high | s | OMNI-0087, OMNI-0090, OMNI-0093, OMNI-0096, OMNI-0101, OMNI-0106, OMNI-0111, OMNI-0115 | system acceptance hardware: run the reproducible TPU/NPU/GPU matrix, archive evidence, and prevent any profile from claiming operational readiness until its own model, pipeline, safety, and acceptance gates pass. — blocked: the GPU lane is available (Vulkan/ncnn) but no TPU or NPU device is present, so the full matrix cannot be run. Document Intelligence's named-GPU acceptance is complete; the remaining profile rows and absent TPU/NPU hardware still block the system-wide matrix. |
| OMNI-0184 | blocked | medium | s | OMNI-0183 | the eBPF helper's privileged path is unverified: the loader, map reads, and socket service were written and compile-checked, but nothing on this host can run them — loading needs CAP_BPF and `bpftool prog load` fails EPERM as this user, so `load_probes`/`read_histogram` have never executed against a real kernel; verify on a host where the helper can be installed as root before relying on the data. |
| OMNI-0198 | blocked | medium | l | OMNI-0186, OMNI-0221 | TPU lane parity — produce and accept an Edge TPU artifact: the fail-closed compiler adapter now exists, but every recipe still needs a representative-calibrated fully-int8 TFLite export, the externally distributed `edgetpu_compiler`, 100% Edge TPU operation mapping, portable/native semantic parity, and named Coral execution evidence. Decide whether releases use a pinned compiler container or trusted precompiled artifacts. This host has neither the compiler nor a Coral device, so production and execution remain unverified. |
| OMNI-0235 | blocked | medium | l | OMNI-0198, OMNI-0233 | Coral event-extraction alternative: Qwen2.5-VL is not a realistic Edge TPU artifact, so evaluate a small fully-int8 candidate-line/token classifier combined with deterministic calendar parsing under the same event result contract. Qualify it only with representative calibration, full compiler mapping, multilingual grounded-event quality, parity of safety/refusal semantics, and named Coral evidence; blocked here on a selected model/corpus, external compiler, and hardware. |
| OMNI-0289 | blocked | low | xs | — | Decide whether generation intentionally excludes TPU despite the global TPU>NPU>GPU preference. Until the product decision is made, document the absent lane at `GenerationRouter` and do not change routing. |

## Rejected / Won't fix

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |

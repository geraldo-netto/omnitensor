[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "omnitensor-qwen-file-organizer"
version = "0.1.0"
description = "Pinned Qwen/Vulkan provider for OmniTensor review-only file plans"
requires-python = ">=3.11"
license = "MIT"
dependencies = ["omnitensor-qwen-vulkan-runtime==0.1.0"]

[project.entry-points."omnitensor.workloads"]
file-organizer = "omnitensor_qwen_file_organizer:create"

[tool.hatch.build.targets.wheel]
packages = ["src/omnitensor_qwen_file_organizer"]

[tool.hatch.build.targets.wheel.force-include]
"../../plugin-manifests/file-organizer.json" = "omnitensor_qwen_file_organizer/omnitensor-plugin.json"
